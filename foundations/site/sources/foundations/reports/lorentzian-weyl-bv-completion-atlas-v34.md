# Lorentzian Weyl BV completion atlas v34

**Result:** `FOUNDATIONAL_LORENTZIAN_WEYL_BV_COMPLETION_ATLAS_V34`
**Dependencies:** `LOCAL-ALGEBRAIC`, `LORENTZIAN-CAUSAL`

## Decision

Atlas V34 removes the residual M5 coefficient package from the construction frontier. Fifteen primal modes, fifteen normalized dual modes, the complete SO(4,2) tensor, all residual representation matrices and q_res^(0) are now portable and independently replayed. They are not yet common-bound, so Gate A still accepts only one of seven hashes. The highest-value tractable next certificate is the exact centered H3/H4/H5 representative payload; it should then be integrated with the harder support-local residual SDR and full cyclic contraction in one freeze.

The new residual snapshot contains 15 primal and
15 dual modes, 120
nonzero SO(4,2) coefficients and 15
representation matrices, with **0** replay
defects.  Gate V16 now has 15 of 20
exports receiver-verified in declared scopes and four typed replacement
packages instead of five.

Gate A remains `FAIL_CLOSED`.  The zero-mode hash is a candidate,
not an accepted common-freeze hash; only 1
of seven top-level hashes is accepted.

## Ranked routes

1. `STRICT_CENTERED_H3_H4_H5_REPRESENTATIVE_PAYLOAD` — Generate the centered complex from an ansatz and serialize normalized coefficient vectors for H3, W_+^2/W_-^2 in H4, and H5 without hard-coding the cohomology answer.
2. `STRICT_COMMON_FREEZE_SNAPSHOT_AND_FINAL_CYCLIC_CONTRACTION` — After M6, bind the field dictionary, q1/q2/q3, D, exact residual payload, SDR and pairing to one manifest; accept hashes only when the final cyclic contraction replays.
3. `STRICT_RESIDUAL_SDR_COMMON_CARRIER` — Extend or reconstruct iota_cl, pi_cl and s_cl beyond the D-finite control and replay every common-carrier identity.
4. `STRICT_FULL_CYCLIC_PAIRING` — Bind the full pairing/sign convention to every nonminimal, auxiliary and residual row.
5. `STRICT_LAMBDA2_GENERAL_SOURCE_COCYCLE_CLOSURE` — After nonlinear identity, prove Noether closure of the general lambda-squared source.
6. `STRICT_CANDIDATE_Q2_Q3_GREEN_LAMBDA2_RESPONSE` — Compose accepted q2/q3 with both Green orientations and verify response identities.
7. `DIRECT_SPACETIME_Q26_HADAMARD` — Keep the analytically mature Berger Hadamard route as a different-theory control, not as the strict pure-Weyl carrier map.
8. `STRICT_D_CARTAN_AND_CHARGE_DECISION` — Classify the nonlinear D-Cartan homotopy and proper-gauge/charge status on the strict carrier.
9. `STRICT_ANALYTIC_MOLLER_CONVERGENCE` — Only after all-order source closure, derive seminorm majorants and a nonzero convergence domain.
10. `STRICT_MIXED_WEIGHTED_CAUSAL_DOMAIN` — Test weighted opposite-polarity domains against the scalar zero-mode witness; polarized recursion does not require this.

## Boundary

The exact residual payload closes an export gap.  It does not construct the
support-local residual SDR, centered representatives, final cyclic
contraction, causal nonlinear compatibility, Hadamard state or QME.
