# Lorentzian Weyl BV completion atlas v35

**Result:** `FOUNDATIONAL_LORENTZIAN_WEYL_BV_COMPLETION_ATLAS_V35`
**Dependencies:** `LOCAL-ALGEBRAIC`, `REDUCED-MODE`, `LORENTZIAN-CAUSAL`

## Decision

Atlas V35 removes M6 from the construction frontier. The finite centered complex and its two normalized degree-four classes are portable and independently replayed, but their hash is not common-bound. Gate A still accepts only one of seven hashes. The highest-value dependency is now the common support-local residual SDR; it is needed before the full cyclic pairing can be closed and before the final all-object freeze can honestly accept hashes.

The centered snapshot has cochain dimensions
`[727, 3084, 8532]`, reconstructs
85,091 nonzero coefficients with
ranks `[636, 2446]`, and proves `dim H4 =
2` with Gram `[[1, 0], [0, 1]]` and
**0** declared defects.

Gate V17 now has 17 of 20 exports
receiver-verified in declared scopes and three typed replacement packages.
Gate A remains `FAIL_CLOSED`: the representative hash is a
candidate, and only 1 of seven top-level
hashes is accepted.

## Ranked routes

1. `STRICT_RESIDUAL_SDR_COMMON_CARRIER` — Extend or reconstruct iota_cl, pi_cl and s_cl on the complete support-local 386-row carrier, including residual and nonminimal rows, and replay every contraction and intertwining identity against the M5/M6 bytes.
2. `STRICT_FULL_CYCLIC_PAIRING` — Once the common residual SDR carrier exists, extend the pairing/sign convention to every row and replay q1/q2/q3 adjointness plus all SDR cyclic side conditions.
3. `STRICT_COMMON_FREEZE_SNAPSHOT_AND_FINAL_CYCLIC_CONTRACTION` — Bind q1/q2/q3/D, zero modes, centered representatives, SDR and full pairing to one manifest; accept hashes only after all ten identities and the final cyclic contraction replay on those exact bytes.
4. `STRICT_LAMBDA2_GENERAL_SOURCE_COCYCLE_CLOSURE` — After nonlinear identity, prove Noether closure of the general lambda-squared source.
5. `STRICT_CANDIDATE_Q2_Q3_GREEN_LAMBDA2_RESPONSE` — Compose accepted q2/q3 with both Green orientations and verify response identities.
6. `DIRECT_SPACETIME_Q26_HADAMARD` — Keep the analytically mature Berger Hadamard route as a different-theory control, not as the strict pure-Weyl carrier map.
7. `STRICT_D_CARTAN_AND_CHARGE_DECISION` — Classify the nonlinear D-Cartan homotopy and proper-gauge/charge status on the strict carrier.
8. `STRICT_ANALYTIC_MOLLER_CONVERGENCE` — Only after all-order source closure, derive seminorm majorants and a nonzero convergence domain.
9. `STRICT_MIXED_WEIGHTED_CAUSAL_DOMAIN` — Test weighted opposite-polarity domains against the scalar zero-mode witness; polarized recursion does not require this.

## Boundary

This closes a finite coefficient export.  It does not construct the common
support-local SDR, the full cyclic contraction, causal nonlinear
compatibility, a Hadamard state, or the QME.
