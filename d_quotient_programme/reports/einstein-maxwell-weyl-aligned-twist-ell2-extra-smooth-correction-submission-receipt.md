# Aligned twist--extra smooth-correction registration receipt

The Einstein team registers the coefficient-explicit mixed part of the
compact global--extra smooth extension.  A direct four-dimensional source
fixture classifies all sixteen aligned twist-position/velocity times
`ell=2` extra-primary `L=1,3` channels.  Thirteen nonzero sources have exact
printed polynomial right inverses and three vanish identically.  The
exceptional `L=1` solutions satisfy all four unreduced action rows after the
complete gauge slice is lifted.

Evidence is content-addressed at commit `3407c70b` with certificate SHA-256
`5788853a60c4fc0caada8b62c2f1e7328b4255a76a9c02dedef564548353cf13`.

This advances only the smooth exponential-polynomial mixed block.  The full
arbitrary-orbit coefficient list, bounded and causal correction classes,
all-orders integration and downstream residual/observer/quantum maps remain
fail-closed.
