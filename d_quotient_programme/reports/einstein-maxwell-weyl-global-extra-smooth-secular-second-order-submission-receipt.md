# Global--extra smooth-secular registration receipt

The Einstein team registers the complementary positive result on the complete
nonzero global--extra common-zero orbit.  Its quadratic output closes in
`L=0,...,4` and the three frequency families `0`, `+/-omega_e`, and
`+/-2*omega_e`.  The aligned twist--extra `L=1,3` blocks are off shell; the
`ell=0`--extra `L=2` block is p-resonant but has a finite secular inverse.
Every other propagation block is off shell or has a finite exponential-
polynomial right inverse.  The only remaining compact cokernels are the five
stabilizer moment maps, already zero on the certified orbit.

Thus every orbit point admits a real smooth spatially periodic finite
exponential-polynomial second-order correction.  Evidence is content-addressed
at commit `3407c70b` with certificate SHA-256
`961548b8e8b8f850e044b55c9b6b82536ba83724b019a4a1137a916a81a21177`.

The aligned twist--extra `L=1,3` mixed block is now coefficient-explicit:
thirteen printed exact polynomial corrections have zero full action-row
remainder and three sources vanish.  The complete arbitrary-orbit coefficient
list remains open.

This result is deliberately paired with, not substituted for, the bounded
obstruction: bounded/finite-quasiperiodic corrections remain `OBSTRUCTED`,
smooth exponential-polynomial corrections are `CERTIFIED`, and the causal or
retarded class remains `NO_CERTIFIED_MAP`.
