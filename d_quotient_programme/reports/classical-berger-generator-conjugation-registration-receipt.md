# Classical Berger generator correction registration

The dossier now records that the frozen dressed-frame unary generator is
`K=D-omega R`.  Raw `D` is affine about the rotating clock background.  The
existing Cartan certificates are therefore scoped to `K`; affine `D`-Cartan
remains open.

