# Exceptional `ell=1` frequency-isolation submission receipt

Exact spectral inequalities separate every generic `ell>=2` `k=0` primary
from `omega_e^2=4/3`; the physical `ell=1` shell is `4`, and standard global
data are generalized zero-frequency modes.  The complete `ell>=1` shell at
`omega_e` is therefore the axial-plus-polar exceptional block already proved
obstructed for all `m`.  Angular selection makes an unclassified `ell=0`
target irrelevant to the obstructing `L=2` channel without an `ell=2`
partner.

Evidence is content-addressed at commit `211b121f` with certificate SHA-256
`12b068a00192f231d247fcf4ef86731cd0a8f15d74e015ca988aa06db20909f1`.
Unequal-frequency sums and opposite nonzero momenta remain separate gates.
