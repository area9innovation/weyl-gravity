# Retained mixed ell3 transfer

The retained contact term has 25,950 exact coefficients, and the retained relative arity-three identity holds on all 36 rows.

## Exchange-vanishing lemma

Let `I2_g=-S q2_g(iota,iota)` and `I2_m=-S q2_m(iota,iota)`. The gravity second inclusion is supported in full rows 37 and 38, while the mixed second inclusion is supported only in full row 38. The composition `q2_g(I2_m,iota)` has 342 raw graded-unshuffle coefficients, all in full output row 38; the retained projection annihilates this contractible row. Both `q2_m(I2_g,iota)` and `q2_m(I2_m,iota)` vanish before projection. Therefore all three transferred exchange sectors vanish coefficientwise and retained mixed ell3 equals the contact pullback `pi q3_m(iota,iota,iota)`.

This is a local algebraic transfer theorem. Independent quantum acceptance remains downstream.
