# Paper IX theorem freeze

Paper IX is frozen for exactly two linked classical gravity--clock results:

1. the positive Berger clock has nonzero but fixed first-order internal
   momentum at fixed couplings, so raw cylinder translation is
   presymplectically null on the declared compact linearized tangent;
2. the complete 54-row classical BV complex has a causal cyclic Cartan
   contraction for the background stabilizer `K_Berger = D - omega R`
   through arity three.

The ten-claim ledger contains no Maxwell certificate.  Maxwell signal and
redshift results, the observer apparatus and its 84-row extension, affine raw
`D`, arity four/all orders, Hadamard data, QME restoration and every quantum
claim remain outside the theorem.

An immutable Git archive of commit `bd3ece1c` supplied the clean replay.
The authoritative Paper IX rail passed 47/47 tests in 135.33 seconds; the
main paper (15 pages) and supplement (7 pages) compiled twice without a
LaTeX error.  The frozen nonlinear and post-freeze quantum scope reviews also
replayed independently.

For transparency, the broader 174-test classical package returned 170 passes
and four classified drifts: two superseded pre-freeze signoff tests, one
excluded Maxwell persisted-output mismatch, and one quantum-consumer field
list mismatch around an otherwise passing classical Green certificate.  That
broader rail was not used to promote the theorem.

The post-freeze classical priority is the observer team's backreacted 84-row
apparatus background and causal unary complex.  The requested retained-36
gravity/Maxwell residual branch basis is a separate quantum-consumer export,
not an amendment to Paper IX.
