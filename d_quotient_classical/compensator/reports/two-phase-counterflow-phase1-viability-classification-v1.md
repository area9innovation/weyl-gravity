# Two-phase counterflow Phase-1 viability classification

The declared same-field stationary counterflow programme ends with
`OBSTRUCTED_NO_ROBUST_STATIONARY_SAME_FIELD_CLOCK`.

The branch decision is exact and precedes any all-isotype rebuild.  On the
entire connected trace-healthy stationary family, the complete physical
spin-`1/2` divisor contains

```text
disc_w(F2) = 256*q^5*(9*q-8) < 0.
```

Hence every admissible point retains a nonradical Hamiltonian--Hopf quartet.
No open stable sublocus or protected stable collision exists.  The selected
repaired fixture still has a support-local causal BV parent, but its certified
`j=0,1/2,1` quotient is already physically unstable.  A familywide Green
homotopy and the higher-isotype census remain `NO_CERTIFIED_MAP`; neither is
needed to decide this branch under the work package's explicit stop condition.

This closes only the same-field, same-derivative-order clock candidate.  It
does not establish a no-go for a different action architecture, nonlinear
instability, Hadamard, QME, particles, observers, positivity or unitarity.
The machine certificate carries the Phase-1 paper disposition.  The existing
Paper 9 supplement was already concurrently modified in the shared tree, so
this scoped lease deliberately does not absorb or overwrite that manuscript
work.

Replay:

```bash
python3 d_quotient_classical/compensator/two_phase_counterflow_phase1_viability_classification.py --check
python3 d_quotient_classical/compensator/verify_two_phase_counterflow_phase1_viability_classification.py
python3 -m pytest -q d_quotient_classical/compensator/tests/test_two_phase_counterflow_phase1_viability_classification.py
```

CLOSE-OUT: OBSTRUCTED — the declared counterflow branch has no robust stationary same-field linearly healthy clock candidate.
