# Counterflow charge-clock complementarity

The fixed- and variable-charge theories obey an exact dichotomy.  For
(T^*R^n,Omega=sum dQ_i wedge dpsi_i), a time generator with phase velocity
(v) satisfies (i_D Omega=v^T dQ).  Reduction at (CQ=q_0), followed by the
corresponding phase translations, makes (D) null exactly when

\[
v\in\operatorname{im}C^T.
\]

But the surviving clock direction is the class ([v]) in
(R^n/\operatorname{im}C^T).  Hence making (D) null necessarily removes its
clock; no intermediate regular charge reduction can do both.

For the selected counterflow background, the fixed-(Q_rel) branch therefore
has no relative clock.  The unrestricted branch retains the Darboux pair
((delta psi_0,delta Q_rel)); (R_rel) is charged, raw (D) is physical with
(H_D=Omega Q_rel), and (K=D-Omega R_rel) alone stabilizes the background.

The unrestricted clock has positive charge curvature, but its exact
linearized matrix is a size-two zero Jordan block:

\[
\delta Q(t)=q_1,\qquad \delta\psi(t)=\psi_1+tq_1/I,
\quad I=12\pi^2\sqrt{10}/5.
\]

There is no exponential growing root, yet bounded/quasiperiodic linear
stability fails because the secular phase is physical and cannot be quotiented
as gauge.  This is the first failed physical sector; later all-Hodge sign and
gradient gates are not promoted.

This is classical `LOCAL-ALGEBRAIC`/`REDUCED-MODE` evidence with unary
`LORENTZIAN-CAUSAL` data imported.  It makes no observer, nonlinear, Hadamard,
QME, particle, scattering or unitarity claim.

CLOSE-OUT: DONE — the complete stop condition is met
EVIDENCE: TWO_PHASE_COUNTERFLOW_CHARGE_CLOCK_COMPLEMENTARITY_V1_TIER_RECEIPT
