# Global exterior-complex-scaled Fredholm completion

## Result

On the fixed two-ended \(\theta=\pi/4\) contour, the complete axial
six-state pencil

\[
\mathscr L_\theta(\omega)Y
=Y'-x_\theta'\mathbb A(x_\theta,\omega)Y
:
H^1(\mathbb R;\mathbb C^6)\to L^2(\mathbb R;\mathbb C^6)
\]

is analytic Fredholm of index zero on a neighborhood of the certified
\(\ell=2\) QNM.

At the right end, the three outgoing phases are stable.  At the left end,
the three horizon-ingoing phases are unstable in the forward coordinate
and therefore decay as the coordinate tends to minus infinity.  The index
is

\[
\dim E^u_-+\dim E^s_+-6=3+3-6=0.
\]

The coefficient remainders tend to zero at both ends.  They are compact
from \(H^1\) to \(L^2\), so the asymptotic dichotomy gives Fredholmness.
Analytic tail elimination leaves the finite core with the exact Jost
transparent planes, hence the already certified connection matrix.
Therefore

\[
\mathscr L_\theta(\omega)^{-1}
=\frac{\Pi_{-2}}{(\omega-\omega_n)^2}
+\frac{\Pi_{-1}}{\omega-\omega_n}+O(1),
\qquad
\operatorname{rank}\Pi_{-2}=1.
\]

The differentiated infinity germ is bounded by a polynomial times the
selected exponential.  If \(\rho>0\) is the uniform ECS decay margin, then

\[
\int_0^\infty (1+t)^2e^{-2\rho t}\,dt
=\frac{2\rho^2+2\rho+1}{4\rho^3}<\infty.
\]

Thus the generalized Jost tangent lies in the same fixed domain as the
ordinary resonant state.  This closes the reduced global-domain issue on
the complex-scaled contour.

## Claim boundary

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

The result does not establish an uncut real-axis outgoing inverse, a
Lorentzian-causal spacetime resolvent, threshold or branch-cut control, a
physical inverse-Laplace contour deformation, a complete QNM expansion, or
global retarded ringdown.
