# Fixed child-grid projective Evans continuation v8

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

The unchanged 1/1024 projective transport evaluated child panels `214` through `245` with `8` workers and accepted only the ordered contiguous prefix. It accepted `32/32` panels. Exact coverage ends at `123/512` and the next honest gap begins at `246/1024`. Full-contour, argument-principle, root-count, QNM, Smith and EP2 flags remain false.
