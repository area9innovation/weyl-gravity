# Projective Evans child-only repair v7b

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

The rail reused the hash-linked failed parent `106/512` from v7 without recomputing it and evaluated only `212/1024` and `213/1024`. Accepted children: `2/2`. Exact contiguous coverage ends at `107/512`; the next honest gap begins at `107/512`. Full-contour, argument-principle, root-count, QNM, Smith and EP2 flags remain false.
