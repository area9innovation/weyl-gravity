# Adaptive dyadic boundary chunk v2

This content-addressed run continues at parent `100/512`. Parents are
evaluated sequentially; only a failing parent is replaced by its two dyadic
children. The compute launch budget is 42 seconds.

All root-count, QNM-location, Smith-selector, defective-fibre, and EP2 gates
remain fail-closed.
