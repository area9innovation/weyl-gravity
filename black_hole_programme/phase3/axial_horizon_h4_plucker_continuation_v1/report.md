# Bounded Plücker continuation report

## Question

Can the chart-free q00 Plücker representation continue beyond the first
boundary missed by the historical Grassmann-graph rail, and where does its
next certified coordinate shortfall occur?

## Outcome

The exact joined run reproduces the predecessor boundary, passes the
remaining three quarters of shell 3, and passes shell 4 through segment 2.
At shell 4, segment 3 it returns typed refusal code 32.

Code 32 means that no individual real or imaginary interval component of
the 20 complex Plücker coordinates excludes zero. It does not certify that
the transported three-plane has lost rank. The last certified raw pivot is
coordinate 5, whose margin has declined to approximately
\(8.76358\times10^{-8}\).

## Interpretation

The chart-free representation materially extends the old graph rail, but
the present componentwise projective normalization eventually loses a
certified pivot through interval wrapping. The next coordinate remedy
should use either:

- a correlated projective patch based on a certified linear functional;
- a QR/exterior-frame normalization that retains correlations; or
- a split of the q00 frequency cell.

It should not force a division by a raw coordinate whose enclosure contains
zero.

## Boundary

The run stops at the first refusal. Shell 5 is intentionally not executed,
and no output state is emitted by the refusing shell-4 chunk. No paper,
lifecycle event, or older evidence artifact is changed.
