# Axial QNM horizon reciprocal checkpoint transport v1

This package continues the certified horizon reciprocal chart from `r=4`
through geometric checkpoints `r=8`, `r=16`, and `r=32`. It preserves the
shared `p`, `p_tau`, and `p_omega` dependency rules and emits a certified
conversion back to the `q` projective line at every checkpoint.

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

```bash
python3 -m black_hole_programme.phase3.axial_qnm_horizon_reciprocal_checkpoint_transport_v1.produce
```

No QNM, EP2, Evans, outgoing-match, or Lorentzian-causal claim is made.
