# Correlated multipanel throughput shortfall

Dependency tag: `REDUCED-MODE`

Status: `THROUGHPUT_SHORTFALL_NO_MULTIPANEL_CLAIM`

The 33-step correlated multipanel prototype was terminated after 344 seconds
under the repository fast-rail rule. It wrote no run artifact. Accordingly,
there is no certified multipanel radius, pivot, residual, crossing of the
former Cartesian obstruction, or claim that wrapping has been removed.

The successful one-step correlated theorem remains valid. Its normalized
successor at `rho=65/268435456` is the last durable model and the required
resume source.

The code audit identifies two missing persistence boundaries:

1. The joint radial/omega derivative kernel is exact but regenerated in every
   process. It must become a separately verified, content-addressed cache.
2. The prototype accumulates all checkpoints in memory and writes only after
   all 33 steps finish. Each invocation must instead consume one verified
   parent, execute exactly one radial step, and atomically write one child
   checkpoint within the 60-second rail.

A lightweight driver may then link already verified children. A timeout emits
a shortfall at the last durable checkpoint and is never a pass.

This result does not establish any multipanel continuation, transport to
`r=4`, H4, `T_+`, a Gram or Stokes identity, or any `LORENTZIAN-CAUSAL`
theorem.
