# Canonical horizon \(K_H\) and checkpoint-frame gate

This package separates the endpoint-normalizer theorem from radial transport.
The exact spin-two Frobenius recurrence fixes
\(\ell_H^T f_0=1\) independently of the intrinsic parameter.  The mixed
Levelt recurrence fixes the spin-one quotient vector \((1,-1)\) and its sole
order-one homogeneous free coefficient independently of that parameter.
Both allowed upper-triangular normalization derivatives therefore vanish:

\[
K_H=\begin{pmatrix}0&0\\0&0\end{pmatrix}
\]

in this declared canonical analytic horizon frame.

An independent exact-frequency `python-flint` rail transports the pure
spin-two and mixed spin-one partial jets with a common dual-scalar
projective normalization.  It accepts 27 panels and then refuses because the
mixed projective pivot enclosure contains zero.  The pure spin-two pivot
still excludes zero.  Consequently the complete frame at \(r=4\), H4 and
\(T_+\) remain fail closed.
