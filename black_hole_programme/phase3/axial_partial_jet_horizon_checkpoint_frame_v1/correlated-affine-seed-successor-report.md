# First correlated affine/Taylor horizon successor

Dependency tag: `REDUCED-MODE`

Status: `ONE_CORRELATED_RADIAL_SUCCESSOR_CERTIFIED`

The mixed Levelt seed was regenerated at
`rho=1/4194304` before fixed-frequency substitution. The new checkpoint is a
degree-four Taylor model on the complex disk

```text
|omega - 4097/8192| <= 1/4194304.
```

The same frequency parameter is shared by the base and intrinsic
dual-`tau` tangent rails. Its linear coefficient is serialized as one common
affine generator, higher Taylor terms remain explicit, and all unresolved
seed/transport error is held in one coupled complex-vector
`l_infinity` residual. No independent component remainder boxes are used.

The initial projective normalization is performed symbolically. The base
pivot is exactly one, the tangent pivot is exactly zero, and the full
pre-normalization denominator has certified lower modulus approximately
`0.999873998017`.

One radial Taylor step of length `1/268435456` was then certified with:

```text
radial Cauchy scaled norm  0.480078558946
radial tail                0.000626096429
```

The successor pivot lower modulus is approximately `0.997894006661`.
After successor normalization the coupled residual radius is approximately
`0.005531865404`, up from `0.001302278904` at the seed. Both models are
serialized in full and form a content-addressed parent/child chain.

This is one successor substep, not completion of the original `1/64` base
panel. It does not establish transport to `r=4`, H4, `T_+`, a Gram or Stokes
identity, or any `LORENTZIAN-CAUSAL` theorem.
