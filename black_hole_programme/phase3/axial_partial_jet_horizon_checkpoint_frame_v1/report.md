# Canonical future-horizon normalizer and checkpoint disposition

## Exact endpoint result

The moving-phase spin-two residue has zero intrinsic derivative.  With the
left pairing fixed to one, the leading tangent coefficient is exactly zero,
so \(k_{2,H}=0\).

The mixed Levelt recurrence has one compatible resonance at order one.
Holding the spin-one quotient leading vector \((1,-1)\) fixed and choosing
the free homogeneous coefficient to be zero for every value of the
intrinsic parameter gives zero tangent coefficients at orders zero and one.
Thus \(h_H=0\).  In the canonical filtered frame,

\[
K_H=\begin{pmatrix}0&0\\0&0\end{pmatrix}.
\]

This is a frame-specific exact value, not a claim that every admissible
endpoint normalization has zero derivative.

## Radial checkpoint

At \(\omega=4097/8192\), the new rail uses 192-bit complex balls, order-16
Taylor panels, Cauchy self-map tails, and dual-complex projective
normalization.  It preserves the common base/tangent scalar amplitude.

The rail accepts 27 panels.  At
\(\rho=91/268435456\), the mixed spin-one pivot ball contains zero.  The pure
spin-two pivot still excludes zero.  The calculation stops at that first
coordinate/pivot obstruction.  It does not classify the underlying line as
singular and does not construct the three-column frame at \(r=4\).

## Claim boundary

Established: a canonical analytic future-horizon endpoint frame and its
exact \(K_H=0\).

Not established: a complete \(r=4\) horizon frame, H4, \(T_+\), Stokes
matching, or bounded all-frequency transport.
