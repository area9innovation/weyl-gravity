# Phase 3 outgoing-infinity reduced-phase preflight

The previous mixed endpoint rail expanded the wrong object.  This successor
keeps the full selected oscillatory factor \(e^{-2i\omega r_*}\) symbolic and
places only its reduced factor amplitude in the shared frequency-Taylor,
intrinsic-dual arithmetic.

The exact outgoing factor line is

\[
R_+=XI2-\frac{i(16\omega^2-4i\omega-5)}{\omega}XI3.
\]

After accounting for the extra inverse-radius power of `XI3`, its exact
factor-coordinate projection has zero spin-one component.  Its carrier and
metric rows therefore supply a correlated base/tangent finite head.

The finite head residual has valuation six in the base RW equation and
valuation four in the intrinsic tangent equation.  Conjugating the formal
infinity factor to \(e^{-2i\omega r_*}\) gives the ordinary outgoing scalar
RW Volterra equation.  Its exact potential integral from \(r=32\) is
\(189/1024\), so on \(\omega\geq1/2\) the contraction constant is
\(189/512<1\).  The base and tangent residuals and the two tangent-source
coefficients are integrable and yield explicit rational remainder bounds.

Those all-order remainders are attached to the common frequency-Taylor,
intrinsic-dual seed before transport.  The first inward macropanel from
\(r=32\) to \(r=1023/32\) then has a finite validated exponential enclosure.
The direct repeated-block calculation and the dual-number calculation agree
in all frequency-Taylor coefficients through the selected order, and their
padded interval difference contains zero.

This certifies one repeated-spin-two outgoing Jost column, not an outgoing
connection theorem.  The complementary factor columns, endpoint shear and
transport to the common matching radius have not been joined.  Accordingly
\(T_+\) remains fail closed.

The next gate is to iterate the same bounded phase-reduced transport to the
matching radius and construct the complementary outgoing columns in the
same endpoint normalization.
