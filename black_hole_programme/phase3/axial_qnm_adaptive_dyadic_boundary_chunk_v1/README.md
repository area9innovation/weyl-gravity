# Adaptive dyadic boundary chunk v1

This package continues the certified boundary prefix at parent panel
`99/512`. Parents are evaluated in order. A parent is subdivided only when
its own common-affine mismatch enclosure contains zero; both dyadic children
must then pass or the run stops fail-closed.

The compute phase has a 42-second launch budget. No argument principle,
root count, QNM location, Smith selector, or EP2 claim is made.
