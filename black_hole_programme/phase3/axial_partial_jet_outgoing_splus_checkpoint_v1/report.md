# Outgoing S one-panel checkpoint report

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

The endpoint export’s large tangent enclosure is a property of the imported
rectangular all-order XI3 box. An exact factor-512 tangent normalization makes
the internal panel calculation smaller, while the final physical tangent is
restored before serialization.

The positive gate requires:

- generator `7315` throughout;
- one complete inward panel from `32` to `8191/256`;
- coefficient equality between partial-dual and directly stacked transport;
- interval overlap after the rigorous exponential tail;
- a content-addressed base/tangent checkpoint.

The certificate reports both normalized and physical tangent widths. It does
not claim that coordinate normalization improves the physical enclosure.
A genuinely sharper long-range rail requires a directly correlated XI3
Volterra correction rather than the existing rectangular endpoint box.
