# Projective Evans/Riccati rail v4

This aggregate joins the content-addressed common-affine chunks for panels
0--15 and 16--31.  It independently rechecks every typed two-sided row at
`r=32`, including the common omega generator, moving endpoint phases,
direct-`q` chart, `Delta`, `Delta_tau`, and `Delta_omega`.

All 32 contiguous boundary panels have nonzero `Delta`.  The local-QNM gate
remains fail-closed at panel 32 because the closed 512-panel boundary is
incomplete.  The current sensitivity balls also still contain zero, so no
Newton denominator or intrinsic defect selector is certified.

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.
