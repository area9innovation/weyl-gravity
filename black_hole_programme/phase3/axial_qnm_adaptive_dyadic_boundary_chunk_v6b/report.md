# Projective Evans child-only repair v6b

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

This append-only rail reused the hash-linked failed parent `104/512` from v6a without recomputing it and evaluated only `208/1024` and `209/1024`, under the unchanged stable-root and typed nonvanishing thresholds. Accepted children: `2/2`. Exact contiguous coverage ends at `105/512`; the next honest gap begins at `105/512`. Full-contour, argument-principle, root-count, QNM, Smith and EP2 flags remain false.
