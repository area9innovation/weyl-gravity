# Projective Evans/Riccati rail v10

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

The stable interval-root rail extends the contiguous typed two-sided prefix through panel 97 at `r=32`.  Every admitted Delta excludes zero.  The minimum lower bound is `[2.4737853874064623124053769570765176e-7 +/- 1.28e-42]` on panel `97`.  Ordered transport stopped at panel `98` with `COMMON_AFFINE_DELTA_ENCLOSURE_CONTAINS_ZERO`.  No threshold was lowered and no Newton, QNM, Smith, or EP2 claim is made.
