# Transport-free axial finite-flux channel classification

Dependency tags: `LORENTZIAN-CAUSAL`, `REDUCED-MODE`

## Certified common cell

The independent radial pilot covers
\([1/2,129/256]\), the exact endpoint Grams cover \([1/2,3/4]\), and
the outgoing-population certificate covers
\([0.49995,0.50005]\). Their exact common interval is

\[
I_*=\left[\frac12,\frac{10001}{20000}\right].
\]

The sixteen-child \(r=32\)-to-\(r=4\) radial cover is now durably bound
by its committed content-addressed manifest. That cover remains an
interior transport certificate; it is not silently promoted to a physical
endpoint connection.

## Physical finite-flux channels

For \(\alpha_{\rm W}>0\), the incoming null, outgoing null, and
future-horizon outward Lee--Wald forms are each nondegenerate Hermitian
forms with

\[
\operatorname{inertia}=(1,2,0).
\]

Thus all three coefficient spaces are three-channel Krein spaces, each
congruent to

\[
J=\operatorname{diag}(1,-1,-1).
\]

The incoming factor frame resolves its signs further. The repeated
spin-two metric/carrier plane is hyperbolic with inertia \((1,1,0)\), and
the unit spin-one quotient has norm

\[
-\pi\alpha_{\rm W}\frac{32}{15\omega}.
\]

This exact factor attribution is certified for the incoming frame. The
outgoing and horizon result certifies matching inertia, not equality of
their raw bases.

## Population and one-sided scattering

The incoming connection \(T_-\) is invertible for every real
\(\omega>0\). Boundary dévissage and the certified scalar outgoing
coefficients prove that \(T_+\) is invertible throughout \(I_*\).
Consequently every outgoing trace on \(I_*\) is produced by a unique
future-horizon-regular coefficient vector. In particular, the negative
classical flux directions are genuinely populated; they are not merely
formal endpoint directions.

In incoming coordinates,

\[
R=T_+T_-^{-1},\qquad A=T_-^{-1},
\]

and the oriented Stokes identity is

\[
G_-=R^\dagger G_+R+A^\dagger H_{\rm out}A.
\]

Therefore the stacked one-sided map
\(\mathscr S=(R,A)^T\) is a pseudo-isometric embedding. On the compact
cell \(I_*\), multiplication by \(T_+\) is a bounded isomorphism and the
same identity holds after integration on
\(L^2(I_*;\mathbb C^3)\).

Analyticity further gives outgoing population on an open dense,
full-measure subset of the positive real axis, with only a locally finite
set of possible scalar reflection zeros. This generic statement is not a
uniform all-axis inverse bound.

## Exact claim boundary

No explicit entries of \(T_+\) or extension mixing amplitudes are produced.
Outgoing invertibility is not certified on all of \([1/2,3/4]\).
The result does not establish limiting absorption, resolvent bounds,
time-domain boundedness or decay, a Green-resolvent pole theorem, positive
energy, particles, ghosts, CPT, or quantum unitarity.

CLOSE-OUT: DONE — the exact common cell carries matching populated three-channel Krein spaces and a band-limited one-sided pseudo-isometric scattering relation, with the radial pilot and physical endpoint proof kept provenance-distinct.
EVIDENCE: black_hole_programme/phase3/axial_global_finite_flux_channel_classification_v3/receipt.json
