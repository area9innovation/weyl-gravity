# Exact-point axial infinity carrier planes at \(r=4M\)

At \(M\omega=4097/8192\), the two four-real-dimensional Ricci-carrier
planes initialized by \((XI0,XI1)\) and \((XI2,XI3)\) are transported from
\(r=32M\) to \(r=4M\) through 348 certified radial factors.  Each remains a
rank-four Grassmann graph, and their common invertible flow preserves the
rank-eight endpoint direct sum.

This is outgoing/reflection infrastructure, not a connection theorem.  The
future-horizon-regular plane has not been certified at \(r=4M\), so no
intersection, \(C^+\), \(T^+\), reflection, scattering-channel, flux-sign,
stability or CPT claim follows.

Verify with:

```bash
python3 -m \
  black_hole_programme.phase3.axial_infinity_carrier_plane_to_r4_exact_point.verify
```
