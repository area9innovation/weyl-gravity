# Correlated midpoint-Hermitian remedy: bounded negative result

## Question

The exact q00 frequency split left both child cells at the same
shell-4/segment-3 refusal: no individual real or imaginary Pluecker
coordinate interval excluded zero.  This experiment asks whether a
correlated projective chart selected from the current midpoint Pluecker
vector certifies nonvanishing at that boundary.

## Construction

For each panel, let `p_j(0)=a_j+i b_j` be the exact rational Taylor midpoint
of the current Pluecker vector and let `q_j=x_j+i y_j` denote its interval
Taylor enclosure.  The replay constructs

```text
Re sum_j conjugate(p_j(0)) q_j
  = sum_j (a_j x_j + b_j y_j)
```

with the exact Forge interval-Taylor kernel.  At the midpoint this equals
`sum_j (a_j^2+b_j^2)`, so it is the canonical Hermitian functional selected
by the midpoint vector.  The normalization proceeds only if its full
interval enclosure excludes zero.

Only the formerly refused call is replaced.  The independent verifier
requires the preceding 19 segment heartbeat records to match the original
split-child logs byte for byte.

## Result

Both exact children retain a typed refusal at shell 4 segment 3:

| child | frequency cell | correlated enclosure | projective norm |
|---|---|---|---:|
| 0 | `[1/2, 4097/8192]` | `[-2.8247010038695976e-07, 2.8248499631960077e-07]` | `0.7575348795807845` |
| 1 | `[4097/8192, 2049/4096]` | `[-2.832958532646906e-07, 2.8331075497301994e-07]` | `0.7597025238436113` |

Each interval strictly straddles zero.  The refusal is code 35, distinct
from the original code-32 raw-coordinate refusal.

## Interpretation

The midpoint-Hermitian chart is geometrically better aligned than an
individual coordinate, but its current correlated interval enclosure is
still too wide to certify a nonzero projective section on either q00 half.
This is an enclosure obstruction, not a proof of rank loss or of a zero
Pluecker vector.  A successful continuation would need stronger correlated
remainder control or a different validated projective representation; mere
frequency bisection plus this fixed local functional is insufficient at
the declared boundary.

The next materially distinct remedies are correlated QR/orthogonal-basis
propagation (or its exterior-power analogue) and a tighter validated radial
enclosure.  Another scalar pivot variant is not justified by this result.

No later shell, paper, or lifecycle state is changed by this result.
