# Bplus4 successor chunk 01 report

## Certified successor

The content-addressed chunk

```text
cbfbfea907e067b53d9074177a08ab6e077130ac025d4b9adc022a0bd40a632b
```

resumes the correlated `R/S` state at `r=247/8`.  The primary adaptive branch
passed:

```text
step             -1/8
order            96
box radius       1/16
successor radius 123/4
tail             0.0016783057410934246
width            2.51499998794968
elapsed          25.801149628998246 seconds
```

The shared frequency generator remains `7315`.  The independently expanded
direct sixteen-state boundary value has exactly the same Taylor
coefficients, and the interval difference contains zero.  The spin-one
tangent coefficients remain structurally zero, with numerical padding
containing zero.

No generated source or raw model stdout is retained.  Their hashes, compiler
and runtime exits, elapsed times, selected adaptive branch, model hashes, and
successor payload hash are recorded in `run_manifest.json`.

## Boundary

This certifies one resumable successor only.  It does not reach `r=4` and
does not establish `Bplus4`, `T_plus`, Stokes, scattering, or flux.

CLOSE-OUT: DONE — one content-addressed adaptive successor panel completes within 60 seconds and passes its independent direct boundary gate.
EVIDENCE: black_hole_programme/phase3/axial_partial_jet_outgoing_bplus4_chunk01_v1/certificate.json
