# Affine midpoint-recentered ECS projective transport v1

This package advances the centered infinity initializer using a singleton
midpoint reference trajectory plus simultaneous zero-centered remainder
radii for the shared frequency panel and the variables `q`, `eta`, and
`xi`.

The decisive reconditioning is the backward scalar logarithmic norm
`Re[-c(2 i omega-2 q)]`.  Replacing the absolute coefficient norm by this
signed scalar bound prevents the artificial exponential wrapping seen in
the rectangular q-box rail.

All 16 panels reach the common match radius `r=32`.  The computation
continues inward until the q-remainder Riccati self-map fails, leaving a
quantified panel-dependent obstruction rather than promoting a root claim.
