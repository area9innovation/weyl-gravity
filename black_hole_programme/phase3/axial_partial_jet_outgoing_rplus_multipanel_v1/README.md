# Outgoing R+ correlation-preserving multipanel

Continues the certified repeated-spin-two `R+` Jost enclosure for the first
16 panels, from `r=32` to `r=63/2`, in
`IvTaylor4_omega tensor dual_tau`.  Each completed panel compares the
dual-number transport with the direct repeated block.

The uncheckpointed 896-panel run toward `r=4` exceeded the scoped runtime
budget and was stopped after more than 360 seconds without a terminal result.
That event is recorded as a throughput shortfall, not a mathematical refusal.
The next gate is deterministic serialization of the mixed seed at chunk
boundaries.

This is one selected outgoing column.  Complementary columns, `K_plus`,
`T_plus`, reflection, scattering and flux remain fail closed.
