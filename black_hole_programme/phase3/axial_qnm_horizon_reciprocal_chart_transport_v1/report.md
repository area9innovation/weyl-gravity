# Horizon reciprocal-chart transport

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

The predecessor q-chart obstruction was tested on 16 panels. The full-panel reciprocal denominator excludes zero on 16/16 panels, and the reciprocal rail reached the bounded common checkpoint `r=4` on 16/16 panels.

The chart change uses `p=1/q` with the shared derivative laws `p_tau=-q_tau/q^2` and `p_omega=-q_omega/q^2`. The artifact is fail-closed and makes no QNM, Evans, EP2, outgoing-frame, or causal claim.
