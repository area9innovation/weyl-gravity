# Axial QNM common affine export contract v1

This fail-closed audit specifies the missing serialization contract required
to subtract horizon and outgoing centered omega polynomials before enclosing
only independent residuals.

It also records the first bounded singleton-centered horizon obstruction. No
long transport, boundary nonvanishing, root count, QNM, or EP2 claim is made.

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.
