# Projective Evans/Riccati rail v2

This successor binds the moving-phase horizon line to the same refined
frequency generator used by the outgoing v1 rail.  It certifies:

- the typed horizon chart
  `q_H=(partial_x P_H)/P_H` for
  `psi=exp(+I*omega*r_star) P_H`;
- a nonzero reduced-amplitude pivot and finite post-normalization
  `(q_H,q_H_tau,q_H_omega)` seed;
- one validated fixed-chart radial step away from the horizon;
- the common-generator panel-0 mismatch at `r=32`,
  `Delta=q_H-q_out+2*I*omega`, including its tau and omega sensitivities.

The panel-0 mismatch excludes zero.  This remains a one-panel result: no
closed-contour count, QNM, Smith selector, EP2, or physical `T_plus` follows.

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.
