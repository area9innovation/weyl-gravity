# Projective Evans/Riccati rail v2

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

The moving-phase horizon line is typed as `q_H=(partial_x P_H)/P_H` with `psi=exp(+I*omega*r_star)P_H`.  Its refined panel-0 pivot excludes zero, the post-normalization `(q_H,q_H_tau,q_H_omega)` state is finite, and one fixed-chart radial step is certified.

At `r=32`, the horizon and outgoing exports share one omega generator.  The independently reassembled mismatch `Delta=q_H-q_out+2*I*omega` excludes zero on panel 0, with typed tau and omega sensitivity enclosures.

This covers one of 512 panels.  It does not establish a closed contour count, QNM, Smith selector, EP2, or physical `T_plus`.
