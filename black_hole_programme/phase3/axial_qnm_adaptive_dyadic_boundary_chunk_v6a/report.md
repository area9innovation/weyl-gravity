# Projective Evans parent observation v6a

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

The unchanged generic adaptive rail observed only parent panel `104/512`; the child-launch guard stopped the run before any subdivision. The parent status is `FAIL_CLOSED` with certified mismatch lower bound `0`. Exact contiguous coverage ends at `13/64` and the next honest gap begins at `104/512`. Full-contour, root-count, QNM, Smith and EP2 flags remain false.
