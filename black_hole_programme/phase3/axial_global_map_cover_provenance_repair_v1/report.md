# Axial global-map cover provenance repair

Dependency tag: `LORENTZIAN-CAUSAL`

The completed microfactor-v6 report and residual-atlas fragment cite the
sixteen-child global-map cover manifest with SHA-256

```text
d5b2e4ddef623136155b315be5ff847f14597440b21e5bd1c4beb800d2ba16d8
```

The cited manifest and its verification receipt existed in the shared
worktree but were absent from committed `HEAD`. This repair lands those two
pre-existing files without regenerating or modifying any child factor,
tail join, global map, transport operator, or scientific input.

The independent verifier certifies the exact contiguous `q00` through `q15`
frequency cover, global-map hashes, prefix/tail provenance links, replay
hashes, and exact cover membership. Nine mutation tests reject a duplicate
child, extra artifact, gap, missing child, global-hash mutation,
prefix-payload mutation, replay-hash mutation, and tail-link swap.

This repair establishes only the durable, content-addressed cover seal for
the already completed radial transport pilot. It does not establish a
future-horizon-to-infinity physical connection, endpoint population, flux
sign, stability, CPT positivity, or unitarity.

CLOSE-OUT: DONE — the exact manifest cited by the v6 report is hash-matched, independently verified, mutation-tested, and durably committed with its receipt.
EVIDENCE: black_hole_programme/phase3/axial_global_connection_matrix_v5/chunks/artifacts/global_map_cover_receipt.json
