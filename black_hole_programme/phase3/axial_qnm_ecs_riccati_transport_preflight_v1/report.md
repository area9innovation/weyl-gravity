# Riccati chart-enclosure obstruction

The spin-two outgoing line was transported in the two projective charts
\[
q=v_x/v,\qquad p=v/v_x
\]
over all sixteen frequency panels.  Order-14 Taylor steps of length \(1/20\)
were enclosed with a quadratic Picard self-map and a Cauchy remainder.

Every panel fails between \(r=43.2\) and \(r=43.35\).  At that point the
current \(q\) ball contains zero, so inversion to \(p\) is not certified, and
the \(q\)-chart majorant discriminant is nonpositive.

This does not locate a physical zero.  It proves that the coarse,
center-zero derivative ball from the norm-only ECS argument has already
lost the correlation needed by a projective atlas.  A centered evaluation
of the ECS Volterra fixed point with a small residual ball is the next gate.
