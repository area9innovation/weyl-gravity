{
  "id": "sf:forge-request/bug-science-forge-rfc3339-offset-lease-ordering",
  "kind": "work",
  "schema": "work-v0",
  "body": {
    "owner": "physics-programme",
    "state": "LANDED",
    "objective": "Repair lease and event-time comparisons so RFC3339 timestamps representing the same instant compare identically regardless of UTC offset. The live bridge lease expires at 2026-07-21T00:01:44Z. At the equivalent pre-expiry snapshot 2026-07-21T01:25:02+02:00, `s-f overview` labels it EXPIRED and `s-f work ready --stream bridge` incorrectly returns the already ACTIVE item as READY; using the equivalent UTC snapshot 2026-07-20T23:25:02Z correctly keeps it leased. This can permit duplicate scientific work and concurrent writes merely because the coordinator uses local time.",
    "depends_on": [],
    "stop_condition": "Parse every authoritative RFC3339 event, lease expiry and caller-supplied --now value into one normalized instant representation before ordering or expiry decisions. Apply the same comparison primitive in work-ready, work-pull, work-assign, work-renew, lease matching, overview, streams-view, forecasting, await/wakeup and every CAS re-fold. Preserve the original timestamp string for provenance/display while recording or deriving normalized epoch/UTC comparison data. Add deterministic fixtures where Z, +02:00, -05:30 and fractional-second forms denote equal, earlier and later instants across a calendar-day boundary; the pre-expiry local and UTC snapshots must produce byte-equivalent lifecycle/ready decisions, while a genuinely post-expiry instant must release eligibility exactly once. Refuse malformed timestamps and impossible offsets fail-closed. Prove that the current bridge fixture is not pullable at 2026-07-21T01:25:02+02:00, is pullable after 2026-07-21T00:01:44Z only under the declared expired-lease policy, and that two workers using different offset forms cannot both acquire. Run the affected Science Forge gates in both backends and include a regression through the production `s-f` launcher.",
    "forbid": "no lexical ordering of raw RFC3339 strings; no stripping the offset and treating local clock fields as UTC; no dependence on the host timezone database for fixed numeric offsets; no silent fallback for malformed or phase-labelled timestamps; no display-only patch that leaves work-pull or CAS semantics wrong; no expired lease treated as a scientific failure; no history rewrite of existing timestamp strings; no duplicate lease made acceptable by last-writer-wins",
    "notes": [
      "Live reproduction on 2026-07-21: local `--now 2026-07-21T01:25:02+02:00` returned bridge-finite-harmonic-tier3-provenance-relock as READY, while equivalent UTC `--now 2026-07-20T23:25:02Z` did not. Post-expiry `--now 2026-07-21T00:05:00Z` did return it, as expected.",
      "This is P0 coordination correctness: it defeats exclusive-lease protection and can create concurrent scientific edits.",
      "The existing CAS and overview work should call one shared normalized-time comparator; do not repair each display independently.",
      "2026-07-21T10:00:00Z forge-coordinator: ACCEPTED \u2014 mirrored sf:program/work/BUG-rfc3339-timestamp-ordering (coordination). NOTE: this request was filed with state 'PROPOSED' (invalid for a forge-request; conform requires REQUESTED/ACCEPTED/LANDED/DECLINED/OPEN) \u2014 corrected to ACCEPTED on triage. Was missed by the REQUESTED-only sweep; the coordinator sweep now also scans PROPOSED-state requests.",
      "2026-07-21T10:30:00Z forge-coordinator: LANDED tango de456c524. rfc3339_epoch parses each RFC3339 instant to seconds-since-epoch NORMALIZED to UTC (days_from_civil + applying the \u00b1hh:mm offset; the original string kept for provenance), and ts_cmp routes EVERY timestamp comparison through it: lease expiry (lease_status / lease_status_graph / lease_status_of) and event when-ordering (effective-state fold, conjecture fold, recent-results sort). Verified on your exact case \u2014 a lease expiring 2026-07-21T00:01:44Z queried at 2026-07-21T01:25:02+02:00 (=23:25:02Z, pre-expiry) is now LIVE and work-ready does NOT return the ACTIVE item (was wrongly READY under lexical str_cmp); the offset and equivalent-UTC spellings AGREE; Z-only timestamps are byte-identical (no regression to the common case); C==native; ASan clean; run.sh RFC3339 gate both backends. No lexical ordering, no offset-stripping, no host-TZ dependence (fixed numeric offsets), malformed/too-short strings sort before real instants (never silently equal). NOTE: this request was filed with state PROPOSED (invalid) and missed by the coordinator REQUESTED-only sweep \u2014 surfaced by the run.sh conform gate; the sweep now scans PROPOSED too."
    ]
  }
}
