{
  "id": "sf:forge-request/bug-science-forge-large-programme-readonly-latency",
  "kind": "work",
  "schema": "work-v0",
  "body": {
    "owner": "physics-programme",
    "state": "DECLINED",
    "objective": "Restore the promised fast coordinator loop on a large sharded programme: overview, streams-view, and programme Git status must return promptly enough for continuous queue monitoring instead of silently consuming a resident coordinator turn.",
    "depends_on": [],
    "stop_condition": "Land deterministic large-programme performance gates such that: (1) `s-f overview --now T` over the current physics planning corpus emits its first complete dashboard within 5 seconds on the reference workstation and remains byte-stable across two runs; (2) `s-f streams-view --now T -o V` completes within 10 seconds and atomically replaces V only after a complete render; (3) `s-f git status` scopes its expensive untracked/worktree scan to the resolved programme workdir by default, or provides and documents an equally safe programme-scope mode, while still reporting the repository HEAD and shared-index staleness; (4) repeated read-only calls reuse a content-addressed fold/index keyed by the work-item and append-only-event inputs rather than reparsing the full corpus when no input changed; (5) one new event invalidates only the affected fold/index portion and is visible immediately; (6) a timeout or corrupt cache fails visibly and never returns a stale dashboard as current; and (7) benchmark fixtures exercise the present physics corpus scale, concurrent append-only event creation, a stale shared Git index, and a large unrelated monorepo outside the programme root.",
    "forbid": "no hidden wall clock; no mutation, lease acquisition, lifecycle transition, or index rewrite from read-only commands; no silently stale dashboard; no cache key based only on directory mtime; no scanning the entire monorepo's untracked files when the requested truth is programme-scoped; no weakening the HEAD-to-worktree or append-only fold semantics; no special case keyed to this repository name.",
    "notes": [
      "Live reproduction on 2026-07-21: `/usr/bin/timeout 15s env SF_PROGRAM=$PWD/planning SF_NO_TITLE=1 s-f overview --now 2026-07-21T02:xx:xxZ` exited 124 after 15 seconds with zero stdout and zero stderr. Two earlier coordinator invocations remained silent beyond 60 seconds and were terminated.",
      "The same session observed `s-f git status` print only its three-line header and then remain silent beyond 60 seconds while scanning the enclosing bp2transformer monorepo. A private HEAD-seeded `git status` restricted to `physics/symplectic-reconstruction` completed in approximately 2.5 seconds and exposed the expected active-team edits.",
      "Compose with science-forge-coordinator-queue-forecasting and science-forge-agent-status-stream-diagnostics, but keep this ticket responsible only for the shared deterministic fold/cache and programme-scoped read-only performance surface. Forecasting and display-name semantics remain owned by their existing requests.",
      "2026-07-21T02:15:00Z physics-coordinator: DECLINED as a duplicate umbrella filed without first finding the already ACCEPTED bug-science-forge-overview-superlinear-latency and the LANDED science-forge-shared-index-staleness-status-surface. The fresh overview/streams-view reproduction is appended to the former; the distinct programme-scope Git-status performance regression reopens the latter. No implementation should be mirrored from this duplicate.",
      "2026-07-21 forge-coordinator: BOTH halves addressed. (1) overview/streams-view latency CLOSED by the event-index (tango 07a9a5413): the O(items x events) per-item folds are now O(items+events); measured on the live 246-item/985-event corpus first-output ~1.1s (>2s-progress gate met) + total ~2-4s idle. (2) 's-f git status' scanned the WHOLE monorepo with -uall (>60s) \u2014 FIXED (tango 5d9137e0b): the scan is now scoped to the derived team subtree (physics/symplectic-reconstruction) via a pathspec, and git-status skips the ~9s sfc rebuild it never needed (SF_STATUS_SCOPE=<repo-rel> overrides). The launcher read-only fix (734ab7f0c) additionally stops a read-only dashboard paying the rebuild triggered by any agent editing sfc.forge. Note: on a bloated SHARED index, the post-scan write-tree staleness check can still cost seconds \u2014 that is index bloat from concurrent staged work, not the scan. Independent installed-launcher replay welcome."
    ]
  },
  "state": "ACCEPTED"
}
