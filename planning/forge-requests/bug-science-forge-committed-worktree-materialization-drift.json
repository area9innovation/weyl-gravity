{
  "id": "sf:forge-request/bug-science-forge-committed-worktree-materialization-drift",
  "kind": "work",
  "schema": "work-v0",
  "body": {
    "owner": "physics-programme",
    "state": "ACCEPTED",
    "objective": "Detect and safely repair divergence between authoritative committed Science Forge deliveries and the shared physical worktree. The new read-only `s-f git status` correctly exposed a distinct failure mode beyond a stale shared index: completed Observer artifacts are present in `origin/master` and local HEAD, but 274 tracked files are physically absent from the shared worktree (approximately 557,382 deleted lines versus `origin/master`). This can break later atlas, paper, certificate and reverse-coverage rebuilds even though provenance in Git is intact. The current safe-actions text offers no scoped materialization operation, while raw restore/reset is forbidden in the concurrent shared tree.",
    "depends_on": [
      "sf:program/work/programme-global-paper-coverage-baseline",
      "sf:program/work/programme-phase1-viability-classification-freeze"
    ],
    "stop_condition": "First preserve the landed read-only truth surface. Add a typed committed-worktree drift audit that compares a selected authoritative revision (normally the programme integration head or fetched origin/master) with the physical programme-root worktree independently of the shared index, and distinguishes: missing committed path; worktree blob equal to an older base; genuine uncommitted edit; untracked collision; live leased path; and safely materializable committed blob. Attribute affected paths to WORK_COMMITTED/work-report changed-path manifests where possible. Add a coordinator-only scoped materialization command, preferably by work item, delivery commit or explicit manifest, that reads blobs from the named immutable commit and writes only paths proven absent or byte-equal to the declared older base; it must use compare-and-swap preconditions, touch no Git index, refuse every modified/untracked/live-lease collision, and emit an append-only WORKTREE_MATERIALIZED or typed refusal receipt with before/after hashes. Integrate a compact health finding into overview when terminal committed deliverables are physically absent and no live owner is present. Make work commit/submit/integrate verify after sealing that every declared delivery path still has a typed physical disposition; do not silently delete or dematerialize producer files. Add deterministic two-agent regressions for new files, modified files, intentional deletions, concurrent edits, stale local HEAD, fetched-ahead origin, partial materialization, crash/resume and byte-stable refusal. Use the current completed Observer delivery as a large-programme regression: the audit must identify the 274 missing tracked files without classifying Bridge/Classical live edits as safe repairs, and a dry-run must propose only conflict-free Observer paths.",
    "forbid": "no git reset, checkout, restore, clean, broad read-tree, shared-index replacement or add-all; no overwrite of a modified/untracked path; no materialization across a live lease without holder acknowledgement; no assumption that origin/master is authoritative without a fetched revision and explicit programme policy; no automatic work transition or scientific claim promotion; no deleting historical events; no treating a clean Git commit as proof that the physical worktree is usable; no global serialization of independent agents",
    "resource_hint": "SCIENCE FORGE GIT/WORKTREE PAPERCUT. Reuse SF16's private-index truth scan and SF9's WORK_COMMITTED manifests. Detection is immediately useful; mutation may land later if the CAS proof is too risky. The current large fixture is read-only reproducible with `git diff --stat origin/master -- physics/symplectic-reconstruction/closed_universe_observers` and selected `git cat-file -e origin/master:<path>` checks.",
    "notes": [
      "AUDIT + SAFE-MATERIALIZE LANDED 2026-07-21 (master 3f3196f28): `s-f drift-audit [--rev R] [--scope P]` (read-only, index-independent) classifies committed deliverables as MISSING_COMMITTED (absent -> safely materializable) or MODIFIED (present but differs -> a local edit/older base, NOT auto-restored); exit 3 on drift. `s-f drift-materialize [--rev R] [--scope P] [--dry-run]` (COORDINATOR) restores ONLY absent committed blobs by writing the file directly (git cat-file blob -> path) — NEVER a present/diverged file (no clobber), NEVER the index or a ref (no restore/reset/checkout; safe in the shared tree); idempotent. Reproduction closed; gate DRIFT-AUDIT (clean + classify + safe-materialize + index-untouched). DEFERRED (still in the mirrored SF-committed-worktree-drift item): the FINER classification (worktree-equals-older-base vs genuine-uncommitted-edit vs untracked-collision vs live-leased-path) and WORK_COMMITTED-manifest attribution of each drifted path — the current audit gives the actionable MISSING vs MODIFIED split and a safe repair for the reproduction's whole class.",
      "ACCEPTED 2026-07-21 (science-forge). Mirrored as sf:program/work/SF-committed-worktree-drift. The read-only truth surface it builds on (`s-f git status`) already exists and correctly exposed the drift. Remaining work (a typed committed-vs-physical-worktree drift audit that classifies missing-committed / older-base / uncommitted-edit / untracked-collision / leased / safely-materializable, attributes paths to WORK_COMMITTED manifests, + a COORDINATOR-ONLY scoped materialization command by item/commit/manifest that restores committed blobs without a raw restore/reset in the shared tree) is a git-heavy launcher feature scheduled as its own phase. Forbids noted: preserve the landed read-only surface; no raw restore/reset in the concurrent tree.",
      "Live reproduction at 2026-07-21T10:13Z: `s-f git status` reports real deletions under closed_universe_observers while `git cat-file -e origin/master:physics/symplectic-reconstruction/closed_universe_observers/certificates/PHASE1_RELATIONAL_OBSERVABLE_DISPOSITION_SYNTHESIS_V1.json` succeeds and the physical file is absent.",
      "The same scan shows Bridge and Classical have active worktree differences, so a broad restoration is unsafe. The required recovery must be manifest-scoped and fail closed on any collision.",
      "This is distinct from sf:forge-request/science-forge-shared-index-staleness-status-surface: SF16 correctly reports HEAD-to-worktree truth, but does not provide provenance-aware attribution, overview health, or a safe way to rematerialize committed terminal deliveries.",
      "SAFETY-CRITICAL CLI REPRODUCTION 2026-07-21T12:34Z: the coordinator ran `s-f drift-materialize --rev origin/master --scope physics/symplectic-reconstruction/closed_universe_observers` without `--dry-run`. The command physically materialized all 160 absent committed blobs (confirmed by fresh file mtimes and a follow-up audit reporting 0 missing), but its banner said `[DRY RUN]` and every row/final summary said `would be materialized`. Repair the mode label and summary so dry-run and mutating execution are unambiguous and truthful; add a fixture proving dry-run leaves files absent while the default or explicit apply mode writes them, and print an explicit final `MATERIALIZED N` versus `WOULD_MATERIALIZE N`. This wording defect is safety relevant because coordinators must know whether shared-worktree state changed."
    ]
  }
}
