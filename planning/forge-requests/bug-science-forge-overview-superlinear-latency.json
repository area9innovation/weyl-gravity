{
  "id": "sf:forge-request/bug-science-forge-overview-superlinear-latency",
  "kind": "work",
  "schema": "work-v0",
  "body": {
    "owner": "physics-programme",
    "state": "ACCEPTED",
    "objective": "Make `s-f overview` satisfy its advertised fast coordinator-loop role on the live physics programme.  At 2026-07-20T23:45:00+02:00 with seven streams, 106 open work items and 108 terminal completions, the command emitted its first table rows only after roughly 60 seconds and completed after roughly 150 seconds.  This is too slow for a 15-minute monitoring loop and caused earlier 10-30 second invocations to appear as empty successful output while the underlying process was still running.",
    "depends_on": [
      "sf:program/work/SF8-overview"
    ],
    "stop_condition": "Profile the fold and remove repeated whole-program scans so one invocation parses/folds each work item and event at most once, or uses a content-addressed incremental snapshot whose validity is checked before display.  Add a realistic fixture with at least 120 work items, 500 events, seven streams, terminal-dependency suffixes, live/expired leases, reports, blocks and retirements.  On the project host the warm overview must complete within 5 seconds and begin emitting only after the internally consistent snapshot is complete; the test must enforce a generous machine-independent operation-count or scan-count ceiling in addition to a non-gating timing receipt.  Preserve exact ready ordering, effective states, dependency modes and fail-closed parsing.  Add a visible progress/error message when the command exceeds two seconds so a coordinator never mistakes a running fold for empty output.",
    "forbid": "no truncating the programme to achieve the target; no omitting blocked or recent-result folds; no stale cache accepted without content/hash validation; no wall-clock-only CI gate; no background process left running after the caller times out; no partial table presented as a complete snapshot",
    "notes": [
      "Priority A coordination papercut.  The command is functionally correct once it finishes; this request is about bounded latency and honest progress visibility.",
      "Reproduction: `SF_PROGRAM=/home/alstrup/area9/bp2transformer/physics/symplectic-reconstruction/planning/work-items s-f overview --now 2026-07-20T23:45:00+02:00`.",
      "2026-07-21T01:30:00Z forge-coordinator: ACCEPTED \u2014 mirrored sf:program/work/BUG-overview-latency. Confirmed: the overview fold calls effective_work_state_dir / lease_status / deps_satisfied per item, EACH of which re-lists + re-reads the ENTIRE events dir -> O(items x events). Fix: read all events ONCE into an in-memory index keyed by target, then fold each item state/lease/deps from that (single pass); emit only after the snapshot is internally consistent; realistic 120-item/500-event fixture + a machine-independent event-read/scan-count gate (not wall-clock). No programme truncation; no stale cache without content/hash validation; no partial table.",
      "2026-07-21T00:02:00Z physics-coordinator regression measurement after the graph-fold commits through Tango origin 4c828e4f8: on the live 116-open-item/120-completion physics programme, a warm no-rebuild overview still took approximately 36 seconds and emitted no progress before the final snapshot; a cold/source-rebuild invocation took 74 seconds. The output is correct once complete, but the accepted <5-second warm stop condition and >2-second progress requirement are not yet met on the real corpus. Keep BUG-overview-latency open; add a real-corpus operation-count profile covering the remaining blocked/recent-result/transition-lag paths before lifecycle closure.",
      "2026-07-21T00:14:23Z physics-coordinator reproduction after Tango origin edc8809f1, which moves streams-view inbox folds onto the single event graph: the live 116-open-item/120-completion overview remained correct but took 45.73 seconds warm, used 24,228 KiB maximum RSS and emitted only the initial caller timestamp until the complete table arrived. This independently confirms that parsing the event directory once has not closed the real-corpus latency or progress gates. Profile per-item dependency resolution and any remaining graph reconstruction/cache rebuild; retain the request as ACCEPTED but not LANDED until the <5-second and >2-second-progress criteria pass on this corpus.",
      "2026-07-21T00:20:14Z Forge implementation update: Tango 348c094cf replaces up to five per-item lease folds with one pass, and programme closeout e55e1b6e7 reports approximately 3.9 seconds for overview and 4.5 seconds for streams-view on the live programme with byte-identical, C/native and ASan gates. Treat the implementation as delivered upstream, but keep this physics request ACCEPTED pending an independent replay from the installed launcher: the local Tango worktree carries unrelated active edits and cannot safely be fast-forwarded or reset merely to manufacture acceptance evidence. The upstream note also leaves the O(items+events) event-index and scan-count gate as headroom rather than a current-size blocker."
    ]
  }
}
