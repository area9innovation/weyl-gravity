{
 "id": "sf:forge-request/bug-science-forge-precommit-report-new-files",
 "kind": "work",
 "schema": "work-v0",
 "body": {
  "owner": "physics-programme",
  "state": "ACCEPTED",
  "objective": "Close the bootstrap gap between `s-f work report` and `s-f work commit` for ordinary scientific deliveries that create new files. The documented workflow requires a WORK_REPORTED event before `work check` or `work commit` can obtain changed_paths, but the SF5 report containment gate refuses a resolvable current_commit that does not already contain every reported path. A normal physics result creates a new certificate, schema, verifier, tests, receipt and report, so those paths cannot be honestly reported before the commit that `s-f work commit` is supposed to create. Using an unresolvable placeholder commit bypasses the gate but defeats the provenance guarantee.",
  "depends_on": [],
  "stop_condition": "Provide one auditable two-phase transaction for new and modified files. A worker must be able to declare the intended changed paths and verification metadata without claiming that uncommitted paths are already contained in a commit; `s-f work check` must dry-run exactly that intent; `s-f work commit --push` must use the isolated index and prospective-tree verification gate, create the commit, append the exact SHA and verification receipt, and seal or supersede the intent with a checkpoint whose current_commit is that SHA. A crash between intent, commit, event append and push must be resumable without duplicating a commit or losing provenance. Add a deterministic nested-programme regression containing at least one new file, one modified file, concurrent unrelated staged/unstaged/untracked files, a failed prospective-tree verifier, a successful retry and a moved-HEAD compare-and-swap retry. The final report/close containment gate must reject any event whose sealed commit lacks the delivered blobs.",
  "forbid": "no unresolvable or fake current_commit placeholder; no weakening of final commit containment; no use of the shared Git index; no broad staging; no silent omission of new files; no report mutation or history rewrite; no producer-time worktree test substituted for prospective-tree verification; no global serialization of all resident streams",
  "notes": [
   "Live reproduction, 2026-07-20: `s-f work show --item bridge-finite-harmonic-tier3-provenance-relock` reports no checkpoint, and `s-f work check --item bridge-finite-harmonic-tier3-provenance-relock --agent bridge-1` refuses because no WORK_REPORTED event exists. The advertised next step is `work report`, but `tools/science-forge/s-f:560-590` verifies every reported path against current_commit before authoring that event. Any newly created deliverable therefore makes the report fail with exit 8.",
   "The landed SF5 isolated-index, nested-path, prospective-tree and containment machinery otherwise passes the physics dry-run: `s-f work check --item bridge-kernel-direction-incidence-normal-form --agent bridge-1` maps the nested programme correctly, yields zero outside-allowlist paths and leaves hundreds of unrelated live changes under not-in-report-but-dirty.",
   "A suitable interface could be `work intent` followed by `work commit`, or `work commit --changed-paths ... --report-fields ...` as one transaction. The exact spelling is not prescribed; the invariant is that an uncommitted intention and a committed checkpoint are distinct append-only event types.",
   "2026-07-20 forge-coordinator: ACCEPTED \u2014 an SF5 follow-on refinement. The bootstrap chicken-and-egg: work report needs changed_paths, but the SF5 containment gate refuses a current_commit that doesn't YET contain the (new) deliverables. Mirrored as sf:program/work/SF9-precommit-newfiles. Fix: allow report/check to reason about a PROSPECTIVE new-file delivery (the files exist in the worktree, not yet in any commit) \u2014 check containment against the WORKTREE/prospective tree for not-yet-committed new files, and only require commit-containment once a current_commit is claimed; the isolated-index commit then makes them real. Sequenced after PC1 clears sfc.forge/s-f.",
   "2026-07-21T03:04:00Z live lifecycle-only performance reproduction: after the Bridge Sobolev scientific commit was already pushed, committing five small append-only JSON report/close/lease events through a private HEAD-seeded index spent about 2.5 minutes inside `git-lfs filter-process` before succeeding as d78b1f8bd. No selected event path was LFS-managed. Add a bounded event/small-delivery fast path that runs the Science Forge prospective-tree and containment gates explicitly, constructs the selected tree without traversing unrelated LFS-managed blobs, creates the commit with compare-and-swap semantics, and then appends/seals the exact SHA. If a selected path is genuinely LFS-managed, invoke its clean filter deliberately and record that fact. Add the current large-monorepo five-event fixture with an operation/filter-invocation gate and a non-gating target below two seconds before push; do not solve this by bypassing prospective verification or Git attributes.",
   "2026-07-21 coordinator design resolution: expose a low-level `work intent` -> `work commit` -> `work report` protocol and a high-level atomic/resumable `work submit` wrapper. INTENT records item, agent, base commit, normalized intended paths, verification commands/metadata and allowlist verdict, but no false current commit. WORK_COMMITTED records the real content-bearing SHA, prospective tree hash, exact committed paths and verification receipt. The default gate requires intended paths to equal the materialized changed set after normalization; a missing, added or skipped generated file refuses and emits an append-only mismatch, after which the worker appends a superseding intent. Never silently intersect or rewrite intent. Downstream affected-chain, publication-coverage and close logic reads committed paths from WORK_COMMITTED; INTENT is only authorization/staging input. Retain the existing containment-gated `work report` path for already-landed/relock work. DONE requires a resolvable WORK_COMMITTED checkpoint plus a final report referring to it; an INTENT alone can never close DONE. RETIRED/NOT_ACTIVATED may remain coordination-only terminal transitions, while any OBSTRUCTED result that creates an evidence artifact must likewise commit it honestly. Crash recovery keys the transaction by item, lease, intent id and prospective tree hash so retry adopts an existing matching commit instead of duplicating it.",
      "2026-07-21 physics-coordinator disposition: Forge commit ab5526f15 lands SF9 P1 only: append-only WORK_INTENT and `work-intent` record the base, transaction, intended paths, verification plan and allowlist verdict without a false commit claim. Keep this request ACCEPTED, not LANDED, until WORK_COMMITTED plus mismatch handling, DONE close-out enforcement, resumable submit/adoption recovery, strict rejection of unresolvable current_commit values and the bounded small-delivery/LFS path all pass their declared regressions.",
      "2026-07-21 physics-coordinator disposition: Forge commit 6b30b50f3 lands the separable SF9 P5 Git/LFS fast path. Declared non-LFS paths are now hashed and installed into an isolated index without scanning unrelated LFS blobs; genuinely LFS-managed selected paths still pass deliberately through the clean filter, and the prospective-tree gate remains intact. This closes the measured small-event latency subproblem, but not the provenance transaction: P2 WORK_COMMITTED/WORK_MISMATCH, P3 DONE enforcement, P4 resumable work-submit/adoption, and strict refusal of unresolvable current_commit placeholders remain required before this request is LANDED.",
      "2026-07-21T12:14Z live P4 durability reproduction: quantum-paper12-relative-offshell-action-obstruction-update-v3 pushed the ten-path scientific delivery as 9f5ad8cdf and the local fold displayed DONE with its lease released, but origin/master contained only the pre-work ACTIVE/LEASE events. The generated INTENT, WORK_COMMITTED, WORK_REPORTED, DONE and LEASE-RELEASE events remained worktree-only; a fresh clone would see the scientific files but not the terminal lifecycle or report. The coordinator sealed those five exact events separately as 732c381d6. The high-level submit/close transaction must not return success or display a durable terminal state unless every event required to reproduce that state is committed and pushed (or an explicit LOCAL_UNSEALED state is shown). Crash/adoption replay must include the post-content-commit event-seal phase, and overview should distinguish worktree-only command events from the configured committed baseline rather than allowing them to masquerade as durable DONE.",
      "2026-07-23 live regression after the partial SF9 delivery: the Phase-3 axial microfactor worker appended a superseding WORK_INTENT naming six tracked paths plus three newly created batch-runner scripts. `s-f git status` saw all nine paths, but `s-f work commit` classified exactly the three new scripts as MISSING and refused. A second intent restricted to the already tracked paths committed as c09416b3e, leaving the required new scripts safely untracked while the live batch continued. Add this mixed tracked-plus-new-file case to the SF9 regression and ensure the intended materialized set is computed from the prospective worktree/tree rather than only the current HEAD/index. The command must either commit all nine declared paths atomically or emit a typed prospective-tree mismatch explaining why a specific new path is absent; it must not report a present untracked file as MISSING merely because an earlier WORK_REPORTED checkpoint exists."
  ]
 }
}
