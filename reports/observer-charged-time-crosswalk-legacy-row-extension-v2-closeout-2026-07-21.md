# Observer charged-time crosswalk legacy-row extension v2

The charged-time receiver census now covers exactly seven Observer atlas rows.
The original five dispositions are unchanged. The two later legacy Berger rows
are classified `NO_CERTIFIED_MAP` from their own exact source certificates:
the receiver replay supplies no action-derived receiver quotient, descended
nonradical pairing or separately typed D/R/K receiver actions, while the
frequency-ratio theorem has empty operational domain and labels its exact
coordinate ratio one as non-operational.

The extension avoids a content-hash cycle. Both legacy certificates were
certified against the exact five-row base-contract blob. The new verifier
reconstructs that historical blob from Git, proves all contract-semantic
fields and the original five dispositions unchanged, then verifies the two
legacy source hashes and their classifications. They are historical extension
sources, not recursive dependency_refs of the current seven-row contract.

The producer, independent verifier, eleven scoped mutations, atlas generator
and fragment validator pass. The two unchanged legacy verifiers predictably
fail if run against the current path because that path now contains the
seven-row extension rather than their historical base hash; this non-pass is
recorded and belongs to the dedicated post-extension Tier-3 successor. No
complete Observer suite was run by this narrow owner.

No physical receiver, detector, operational redshift or new observer
architecture is promoted.

EVIDENCE: closed_universe_observers/receipts/CHARGED_TIME_RECEIVER_ADMISSIBILITY_CROSSWALK_V1_TIER_RECEIPT.json

CLOSE-OUT: DONE — exact source-derived seven-row census with historical-base boundary
