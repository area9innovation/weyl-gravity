# Phase 3 horizon reciprocal geometric-checkpoint transport

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

The validated reciprocal horizon rail was continued from \(r=4\) through
\(r=8\), \(r=16\), and \(r=32\). All 16 frequency panels reached every
checkpoint without a rejected self-map trial or a second Möbius recentering.

At each checkpoint the full \(p\)-ball excludes zero, so the artifact also
emits the certified horizon projective line

\[
q=\frac1p,\qquad q_\tau=-\frac{p_\tau}{p^2},\qquad
q_\omega=-\frac{p_\omega}{p^2}.
\]

At \(r=32\), the base \(p\)-radii range from about \(0.104\) to \(0.205\).
The shared sensitivities remain valid but deliberately broad: the
\(p_\tau\)-radii range from about \(1.06\) to \(7.65\), and the
\(p_\omega\)-radii from about \(1.26\) to \(4.62\).

This establishes a certified base horizon projective line at the intended
matching radius. It does not compare that line with the outgoing rail and
does not establish an Evans boundary theorem, QNM, EP2, sharp tangent bound,
time-domain stability, or Lorentzian-causal result.
