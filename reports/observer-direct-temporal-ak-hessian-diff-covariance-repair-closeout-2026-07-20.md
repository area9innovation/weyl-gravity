# Observer direct temporal A--K and Diff-covariance repair close-out

The predecessor scalar-orbit result, complete \(q_1\), source-labelled
\(q_2\), and order-three action module were imported from commit
`fbc422cfa` by exact content hash.  The bounded symmetry-constrained ansatz is
tested inside a larger ambient module: all 932 imported IBP-closed
order-three scalar-density actions per emitter, both reflection parities and
all differentiated \(\chi/K/A\) placements, plus electric and magnetic
clock-profile lines.  Every column is the exact Hessian of a serialized local
action.

The direct temporal \(A\)--\(K\) projection is repaired.  For each
source-isolated emitter, the action image and source-augmented image both have
rank 934 on the \(\tau^*\) rows.  Consequently
\[
 \tau^*\leftarrow(e_0e_1A_0,K_{b,01})
\]
is not itself an obstruction in the enlarged module.

The complete covariance projection is obstructed.  On the joint
\(\tau^*,A^+_0\) rows, the action image has rank 934 and adjoining the typed
source raises it to 935.  The quotient has 42 coordinates per emitter, with
first exact coefficient
\[
 A^+_0\leftarrow(\tau,e_0e_1K_{0,01})=-3g_0h_0
\]
and its emitter-1 analogue.  Freeing the inherited emitter Diff--BV
normalization produces ranks \(935<936\), so the class is not a normalization
artifact.  Removing either clock-profile representation is independently
detected.

The next missing representation is a temporal Maxwell/emitter-antifield
covariance module beginning with \(A^+/\tau/K\) and cyclic
\(K^+/\tau/A\) descendants.  This is the first representation capable of
reaching the Maxwell-cotangent quotient witness.

The work item's requested quartic \(q_3\), \(K_{\rm Berger}\), apparatus and
observer replays were not silently narrowed: they are unreachable because
the common-action arity-two locus is empty.  The five physical metric
\(q_1\)-preimage routes to the spatial ghost-antifield row are recorded
fail-closed, and the imported \(-4g_0h_0\) arity-three coefficient remains a
diagnostic rather than a promoted obstruction for a nonexistent quartic
descendant.  No detector, redshift, memory, recoil, tangent-cone, branch or
quantum claim is made.

CLOSE-OUT: OBSTRUCTED — the direct temporal Hessian is repairable, but every action in the complete ambient scalar-density module retains an exact Maxwell-cotangent covariance defect

EVIDENCE: `closed_universe_observers/certificates/BERGER_DIRECT_TEMPORAL_AK_DIFF_COVARIANCE_REPAIR.json`
