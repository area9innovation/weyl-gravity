# Observer material-parent-56 background-readout interface close-out

The abstract \(F_a\) symbol is replaced by the exact compact functional
\(F_a(A)=Q_a[dA]\) on Maxwell rows 55--58.  Both normalized detector
profiles, polarizations, source/target rows, signed adjoints, support
categories and constant-potential zero-mode restrictions are serialized.

Direct action differentiation gives four ordered mixed
\(\lambda_a\)--\(F_a\) entries of coefficient \(-1\).  The independent
normalized mixed-unary rail has the same four blocks with zero nilpotency
and cyclicity defects.  Profile, sign, support and zero-mode mutations are
rejected.

CLOSE-OUT: DONE — the row-indexed background-readout profile and all four mixed Hessian blocks are certified

EVIDENCE: closed_universe_observers/certificates/BERGER_MATERIAL_PARENT56_BACKGROUND_READOUT_INTERFACE.json

## Verification receipt

- Tier 0 parsing and scoped diff check — PASS.
- Tier 1 producer, independent verifier and five focused tests — PASS
  (0.08 s, 0.35 s, 0.08 s pytest/0.30 s process).
- Tier 2 atlas generation, validation and independent verification — PASS
  (0.63 s, 0.57 s, 1.29 s).
- Tier 2 direct atlas tests — PASS, 62 tests (24.77 s).
- Paper 09 compiled twice to a temporary output directory — PASS (0.51 s,
  0.46 s).
- Tier 3 is not run: this is a scoped interface gate, not a freeze, shared
  core change, release or theorem promotion.
