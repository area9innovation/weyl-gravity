# Compact Einstein--Maxwell axial master complex

Date: 2026-07-16

## Result

`COMPACT_EM_AXIAL_MASTER_COMPLEX` closes the standard axial harmonic block for
every `S1` momentum, every `ell>=1`, and every `m` on the fixed compact
`U(1)` bundle.  The result is `G1_AXIAL_ALL_N_ELL_M`, with dependency tags
`LOCAL-ALGEBRAIC` and `REDUCED-MODE`.

The polar complex, covariant symplectic normalization, and possible extra
fourth-order adjoint classes remain open.

## Gauge fixing

Before gauge fixing, the axial sector contains

```text
h_A X_a, h_2 X_(ab), q_A Y, b D_aY
```

and axial-diffeomorphism/Maxwell parameters `(s,r)`.  Their transformations
are

```text
delta h_A = partial_A s,
delta h_2 = 2s,
delta b   = s+r,
delta q_A = partial_A r.
```

For `ell>=2`, `X_(ab)` is nonzero.  Regge--Wheeler gauge `h_2=0` fixes `s`
uniquely and Maxwell-angular gauge `b=0` then fixes `r`.  The remaining
`h_A,q_A` block has no residual smooth gauge.  For `ell=1`, `X_(ab)=0`, so
the combined residual `r=-s` remains and acts by

```text
delta h_A=partial_A s,
delta q_A=-partial_A s.
```

## Exact Fourier block

For `exp(i(kx-omega t))`, the full tensor computation with arbitrary
spherical eigenvalue `lambda` produces the metric constraint

```text
omega h_t+k h_x=0
```

and the Maxwell angular equation then gives

```text
omega q_t+k q_x=0.
```

The four base equations have determinant

```text
lambda (lambda-2)
[(omega^2-k^2-lambda)^2-2lambda].
```

For `ell>=2`, introduce transverse masters by

```text
(h_t,h_x,q_t,q_x)=(kH,-omega H,kQ,-omega Q).
```

They obey

```text
(partial_t^2-partial_x^2) (H,Q)^T
  + [[lambda,2],[lambda,lambda]] (H,Q)^T = 0.
```

Hence, with `k_n=2 pi n/L`,

```text
omega_+^2=k_n^2+lambda+sqrt(2lambda),
omega_-^2=k_n^2+lambda-sqrt(2lambda).
```

Both branches are non-tachyonic for `ell>=2`.

## Exceptional `ell=1` quotient

The factor `lambda-2` is exactly the residual gauge vector

```text
(-omega,k,omega,-k).
```

For nonzero Fourier momentum it is generated by a smooth periodic parameter
and is removed.  The physical dispersive branch is

```text
omega^2=k_n^2+4.
```

At `omega=k=0`, however, the Fourier gauge vector vanishes and cannot produce
the constant `H_x=-q_x` mode.  Producing it requires a parameter proportional
to `x`, which is not periodic.  The global twist therefore remains precisely
at the zero Fourier block.

## Reduced current

The master matrix is symmetrized by `W=diag(lambda,2)`, giving the local
conserved current

```text
j^A(u,v)=u^T W partial^A v-(partial^A u)^T W v.
```

Its `S1` integral is a conserved reduced Cauchy pairing.  This establishes a
local reduced symplectic candidate, but its normalization and sign have not
yet been matched to the covariant Einstein--Maxwell presymplectic current.

## Interpretation and next gate

All axial Fourier modes are now organized before the residual quotient.  The
ordinary photon/graviton-like modes are present linearly; the second-order
Taub question is a separate projection of their quadratic sources.

The next gate is the polar/even master complex.  After it lands, both reduced
currents must be matched to the covariant symplectic form and the remaining
fourth-order adjoint classes solved before the full quadratic coefficient
table can be promoted.

## Verification

The generator performs a full arbitrary-`lambda`, arbitrary-`k`,
arbitrary-`omega` Einstein--Maxwell tensor reduction.  An independent verifier
checks the determinant, residual gauge vector, symmetrizer, provenance, and
scope.  Eight scoped tests cover the theorem and fail-closed boundaries.
Generator verification passed in `19.77 s`, the independent verifier in
`0.66 s`, and the eight-test suite in `18.79 s`.  Tier 2 is unnecessary
because the imported certificates are unchanged content-addressed inputs;
Tier 3 criteria are not met because polar and full-adjoint gates remain open.
