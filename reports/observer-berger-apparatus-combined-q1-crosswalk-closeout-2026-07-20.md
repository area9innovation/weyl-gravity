# Observer Berger apparatus combined-q1 crosswalk close-out

The complete 108-row unary carrier, the action-derived 56-row parent, the
combined-q1 contract and the earlier exact \(K_{\rm Berger}\) gate were
imported by content hash.

The typed candidate pushout identifies only the two memory fields, two memory
multipliers and their four cotangents.  The remaining 48 parent rows have
different roles from the global scalar rods and massive two-form emitters, so
the candidate has 156 rows rather than a raw 164-row concatenation.

This candidate fails at the first required symmetry interface.  The six
global scalar rods have time-translation closure rank eight and admit no
constant internal \(6\times6\) completion.  Both detector backgrounds have
explicit nonzero affine \(K_0\) witnesses.  The new material orientation rows
cannot supply the two missing directions: their principal symbol is
first-order \(sA_4\), while the global scalar-rod symbol is second-order
\(s^2I_3\).  Independently solving \(sA_4B=Cs^2\) gives rank 24 on 24
constant mixing variables and nullity zero per detector.

Consequently no background-preserving linear \(K_{\rm Berger}\) matrix
commuting with \(q_1\) exists in the declared typed constant-identification
class while the certified 108-row base is left unchanged.  The minimal repair
is two new global degree-zero rods and two degree-one cyclic cotangents,
followed by recomputation of the co-rotating background, rod stress,
\(\Phi_2\), 112-row unary complex and background quotient.  The prospective
identified union would then have 160 rows, but is not yet certified.

No combined cohomology, descended pairing, reduced detector rank, Z2 response,
memory or redshift is promoted.

Verification receipts: `py_compile` passed in 0.03 s; exact generation in
0.43 s; the method-distinct verifier in 0.50 s; atlas generation in 0.81 s
and independent atlas verification in 1.69 s.  The five package tests and
complete observer-atlas test module passed 60 tests in 26.09 s.  Two
`pdflatex` passes completed in 0.54 s and 0.51 s, and scoped
`git diff --check` passed.  Tier 2 used exact hashes for the unchanged
108-row unary and K-gate inputs.  Tier 3 was not run because this scoped
obstruction is not a freeze, tag or release.

CLOSE-OUT: OBSTRUCTED — the unchanged six-rod 108-row base has no background-preserving linear K_Berger completion, and parent material rods cannot supply the two missing global directions

EVIDENCE: `closed_universe_observers/certificates/BERGER_DYNAMICAL_APPARATUS_COMBINED_Q1_CROSSWALK_OBSTRUCTION.json`
