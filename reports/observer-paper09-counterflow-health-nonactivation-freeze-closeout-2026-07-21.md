# Referee report: Paper 9 Phase 1 publication freeze

Decision: `DRAFT_ALLOWED` with two exact evidence gates. The ten classical
claims retain their prior scientific scope, but this publication-current
edition is not promoted to a new `THEOREM_FROZEN` state while required rails
are red.

## Scientific disposition

The new authoritative superset classifies all 22 material statements. It
preserves the charged-time event, finite-resolution and comparison results as
conditional theorems; preserves the legacy G0 and three rank-two responses on
their declared carriers; distinguishes the standalone local BV class from its
obstructed ambient action integration; and records no operational observable.

The terminal counterflow conclusion is unambiguous. Every certified
repaired-q70 physical block at `j=0,1/2,1` is nonradical but unstable. Fixing
`Q_rel` removes the relative clock. For `j>=3/2` the quotient, divisor and
pairing remain `NO_CERTIFIED_MAP`. Consequently the thirteen-field
operational frequency-ratio domain is empty at the tested point. No detector
or redshift value is inferred.

All 33 material imports have exact hashes, materiality annotations and
result-to-paper edges. They include both terminal observer results, their
payloads, receipts and lifecycle triplets, the maximal health assembly and
its payload/receipt, and all six transitive action-derived health inputs. The
coverage replay is `NONVACUOUS_PASS` with no uncovered material result.

## Independent verification

The independent verifier rechecks the legacy ten certificates and their
required true/false flags without importing the new generator. It also checks
all publication hashes, terminal lifecycle evidence and exact health typing.
Four claim-map tests and five terminal-health consumer tests pass. The stale
healthy-carrier, coordinate-ratio-as-redshift and `K=D` mutations are all
rejected. Both TeX targets have two consecutive warning-free builds.

## Exact remaining gates

1. `LEGACY_TEN_CLAIM_SOURCE_BINDING_SUPERSESSION` is open. The old
   `PAPER_09_BERGER_CLAIM_TABLE` verifier pins the pre-update TeX source hashes
   and rejects the publication-current manuscript. Its classical owner must
   repin those sources or formally retire that source-binding role in favour
   of the superset. The ledger path is outside this observer lease.

2. `OBSERVER_STREAM_TIER3_GREEN` is open. The full observer suite reached 150
   passes and one failure before interruption. The failure is pre-existing
   persisted-certificate drift in `test_berger_84_row_apparatus_handoff.py`:
   `BERGER_84_ROW_APPARATUS_HANDOFF` no longer matches its producer's imported
   dependency references. That apparatus is forbidden scope here. Its owner
   must reconcile the artifact and an uninterrupted full observer suite must
   then pass.

These are evidence-integrity gates, not invitations to invent a receiver,
suspension, compensator, retuned background or new observable. The scientific
nonactivation at the tested point is terminal and unchanged.

EVIDENCE: paper/09-relational-clocks-berger-d-cartan-claim-map.json;
closed_universe_observers/receipts/PAPER09_COUNTERFLOW_HEALTH_NONACTIVATION_FREEZE_V1_TIER_RECEIPT.json;
planning/paper-coverage/observer-phase1-relational-observable-dispositions-2026-07-21.json

CLOSE-OUT: `DRAFT_ALLOWED` — two exact evidence gates remain open.
