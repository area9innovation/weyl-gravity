# Compact-Cauchy constraint-algebroid/Kuranishi carrier

The smallest certified replacement for the failed fixed-group AMM model is a
two-jet derived carrier, not a linear moment-map-zero subspace. Its
lapse--shift bracket retains the phase-space-dependent inverse metric. Around
`h_xx=1+epsilon*q`, the `sin(x),cos(x)` bracket has shift
`-1+epsilon*q-epsilon^2*q^2`; deleting the linear structure function exactly
reproduces the rejected fixed-group mutation.

On the declared Sobolev slice let `K=ker DC` and
`O=ker(DC)^*=span{H,P_x,J1,J2,J3}`. The quadratic Kuranishi operation is

```text
l2(u,v)=P_O D^2C[u,v],       kappa_2(u)=l2(u,u)/2.
```

The two-jet derived zero fibre is the Koszul model with five generators
`d eta_A=kappa_2,A`. Its differential squares to zero, and its finite-harmonic
restriction is exactly the certified five Taub/moment maps.

The ambient linear Einstein--Weyl cofiber sequence does not pull back as a
sequence of individual derived zero fibres. The certified balanced
Einstein-minus/extra fixture has total `mu_H=0`, while its Einstein projection
has `48*(-6+5*sqrt(3))/5` and its extra projection has the exact negative.
Thus the extra projection sends a common-zero point outside the extra zero
fibre. The correct successor must be a derived correspondence/fibre product
that retains charge cancellation, not a quotient of separately neutral
sectors. The ambient Schur-complement form remains available along chosen
inclusions but does not become a quotient pairing through the absent map.

Higher Kuranishi operations, an all-orders groupoid slice, bounded or causal
spacetime solvability, particles and quantum claims remain open.

EVIDENCE: bridge/certificates/EINSTEIN_WEYL_CONSTRAINT_ALGEBROID_KURANISHI_CARRIER_V1.json
CLOSE-OUT: OBSTRUCTED — the two-jet five-charge derived carrier exists, but the linear cofiber projection cannot descend to it because balanced Einstein-extra charge cancellation is intrinsically mixed.
