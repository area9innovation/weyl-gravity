# Observer temporal-curl all-jet disposition close-out

The terminal mapping-cone certificate, payload, close-out and lifecycle were
imported by exact hashes.  Its nonzero second-jet quotient selects the
all-jet branch: for each labelled emitter the action rank is 2,641 and the
source-augmented rank is 2,642.

The work package forbids another isolated derivative-order computation.
Closing the replacement gate requires an exact finite presentation of the
all-finite-order local \(B^+\tau K_b\) Hessian module modulo integration by
parts, restricted to connected Berger-\(U(1)\) invariants over
\(\mathbb Q(\sqrt{10})\).  It also requires module syzygies, a certified lift
from the associated graded object through lower-filtration Berger
commutators, and an independently checked source membership or
non-membership witness.

The audited Forge snapshot does not yet provide that complete layer.  Exact
commutative Groebner membership and selected PBW straightening are available,
but Forge's own completeness ledger leaves differential-PBW component
crosswalks and syzygies open.  The missing capability has therefore been
filed as
`sf:forge-request/observer-differential-pbw-module-membership`, with the
Berger source-class calculation as its first consumer and explicit mutation
requirements.

This disposition is fail-closed.  It does not identify the finite
second-jet representative as an all-jet cokernel generator, does not claim
source non-membership at every derivative order, and does not promote
\(K_{\rm Berger}\), \(q_3\), detector response, redshift, recoil or
tangent-cone observables.

## Verification receipt

- Tier 0: `python3 -m py_compile` on the new generator, independent verifier
  and atlas generator passed in 0.05 s.  JSON generation passed in 0.03 s,
  and scoped `git diff --check` passed.
- Tier 1: the independent shortfall verifier passed in 0.07 s; the five new
  tests plus 55 atlas tests passed in 22.14 s; residual-atlas schema
  validation passed in 0.58 s; independent atlas replay passed in 1.28 s.
- Paper 9 compiled twice with
  `pdflatex -interaction=nonstopmode -halt-on-error` in 0.46 s and 0.45 s.
  `latexmk` was unavailable, so it was not counted as a pass.
- Tier 2 was not run because no mathematical input, shared operator or
  existing certificate claim changed.  Tier 3 was not run because this
  `SHORTFALL` promotes no theorem, freeze, tag or release.

CLOSE-OUT: SHORTFALL — exact differential-PBW module presentation and membership machinery is missing; the all-jet source verdict remains OPEN

EVIDENCE: `closed_universe_observers/certificates/BERGER_TEMPORAL_CURL_ALL_JET_DISPOSITION_SHORTFALL.json`

DEPENDENCY: `planning/forge-requests/observer-differential-pbw-module-membership.json`

MISSING-DEP: sf:forge-request/observer-differential-pbw-module-membership
