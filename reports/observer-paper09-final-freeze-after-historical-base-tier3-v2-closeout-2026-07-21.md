# Paper 9 final disposition after historical-base repair and Tier 3 v2

Decision: `DRAFT_ALLOWED`. The 22-claim publication map was regenerated with
48 exact imports, including the terminal Classical source-binding package, the
terminal predecessor publication package, the immutable historical-base
repair, and the fresh post-repair Observer Tier-3 obstruction. Independent
coverage is `NONVACUOUS_PASS`; all 22 claims have material evidence edges and
no claim is classified as an operational observable.

Three exact evidence boundaries prevent `THEOREM_FROZEN`.

1. The regenerated map imports the current
   `PAPER_09_BERGER_CLAIM_TABLE` hash, but that legacy table pins the
   pre-regeneration map hash. The map verifier passes only after regeneration,
   while the independent legacy-table verifier then fails at
   `publication_claim_map`. This is a recursive current-path content-hash edge,
   not a scientific disagreement. An acyclic source-binding authority is
   required.
2. The single fresh post-repair Observer traversal passed 300 tests and then
   stopped at a test-harness/materialization-interface defect: the exact archive
   lacked Git administrative metadata required by the detector-record producer.
   It is not a complete green Tier-3 run, and none of its partial passes are
   credited as green.
3. The affected health-nonactivation consumer exposes three stale persisted
   dependency hashes: legacy receiver replay, legacy ratio nonactivation, and
   receiver admissibility. Their current hashes are exactly those certified by
   the historical-base repair. The persisted health certificate's scientific
   `NO_CERTIFIED_MAP` disposition is unchanged, but its evidence chain is not
   current.

The immutable historical five-row contract remains the Git blob at commit
`aa5ca7814798dfbcc92ee52e462d25af74806515`; the repaired seven-row census and
both legacy `NO_CERTIFIED_MAP` dispositions remain unchanged. Every certified
repaired-q70 physical block remains unstable, fixed `Q_rel` still removes the
clock, higher isotypes remain `NO_CERTIFIED_MAP`, and no operational frequency
ratio or relational redshift is defined at the tested point.

Scoped verification passed: the generator, independent map verifier, v2 final
disposition verifier, nonvacuous coverage replay, and 13 mutation/unit tests.
The two affected-consumer failures above are recorded as fail-closed
obstructions, not passes. A resident-tree replay of the seven-row crosswalk was
also not counted as a pass: one committed atlas dependency is physically
absent, while the sealed exact archive is not Git-attached and therefore cannot
answer the verifier's immutable Git-object query. The terminal exact repair
receipt is imported by hash; this lease did not materialize an out-of-scope
artifact. PDFs were not rebuilt because the work package makes
two warning-free builds conditional on a green theorem-freeze gate.

EVIDENCE:
`closed_universe_observers/receipts/PAPER09_FINAL_FREEZE_AFTER_HISTORICAL_BASE_TIER3_V2_OBSTRUCTION.json`;
`closed_universe_observers/receipts/PAPER09_FINAL_FREEZE_AFTER_HISTORICAL_BASE_TIER3_V2_TIER_RECEIPT.json`;
`paper/09-relational-clocks-berger-d-cartan-claim-map.json`;
`planning/paper-coverage/observer-phase1-relational-observable-dispositions-2026-07-21.json`.

CLOSE-OUT: OBSTRUCTED — final `DRAFT_ALLOWED` disposition; no theorem or
observer-science promotion.
