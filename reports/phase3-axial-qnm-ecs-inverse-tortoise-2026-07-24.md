# Phase 3 axial QNM ECS inverse-tortoise advance

The package
`black_hole_programme/phase3/axial_qnm_ecs_inverse_tortoise_v1/`
closes the first two replacement gates left by the noncontractive formal
infinity recurrence.

On the ray
\[
x=x(45)+e^{i\pi/4}t
\]
the exact inverse-tortoise branch stays in
\(\operatorname{Re}r\ge45,\operatorname{Im}r\ge0\), grows at real-part rate
strictly greater than \(2/3\), and therefore avoids \(r=0,2\).  Exact
rational potential majorants then prove uniform Volterra contractions for
both scalar diagonal channels on the proposed radius-\(0.025\) QNM disk:
\[
\|\mathcal V_1\|\le0.306682,\qquad
\|\mathcal V_2\|\le0.310263.
\]

This produces coarse reduced scalar outgoing balls at \(r=45\).  The next
gate is validated inward complex transport and assembly of compatible
endpoint \(\tau\)-jets for the mixed four-state partial-jet system.

The result does not establish an Evans boundary, a QNM count, a QNM, or an
EP2.
