# Residual-orbit obstruction freeze v2 closeout

This work item is complete by adoption of the already materialized exact
package at commit
`0a936fb08c2f2a07edbfc97b70f5f4c1d525ddc5`.  No mathematical source,
certificate, schema, verifier, test, atlas entry or Phase 1 conclusion was
changed.

## Frozen scoped result

The proposed repair consisting of one real filtered field direction and its
cyclic dual cancels the rank-one page defect at a chosen null fibre, but it
does not close under the real Berger rotational stabilizer.  In the
plus/cross frame, the exact defect is
`J_W beta_plus = -2 beta_cross`; the normalized cross functional evaluates it
to one and annihilates the complete two-row ansatz.

Reality therefore forces the full two-dimensional helicity plane and its
cyclic dual: at least four BV rows at a real null fibre.  In the declared
finite-free support-local tensor-row category, the `SO(3)` orbit is the
five-dimensional spatial STF2 representation, so cyclicity forces ten global
rows.  The existing rank-46 STF2 graph is contractible and is only a negative
control; it does not supply the required obstruction-quotient image.

The noncontractible STF2 enlargement, later filtered pages, repaired-carrier
`q2/q3`, retained-`ell3` projection and all branch-resolved interaction claims
remain unconstructed or `NOT_ACTIVATED`.

## Exact adoption ledger

- Adopted commit:
  `0a936fb08c2f2a07edbfc97b70f5f4c1d525ddc5`.
- Result certificate SHA-256:
  `ada801a3f53e56ef32ae4772631fcc3d63ff428124d7748bd52a591a3b59848f`.
- Filtered cyclic branch obstruction SHA-256:
  `66fff0566ceccb887c1e5138984fe079203deedd5a6538ef1b55976918545a0e`.
- Phase 1 interaction disposition SHA-256:
  `27eb51544b1db4b6a33d551076f65ac857122f3d0037812fe4e0848c484f73d4`.
- Parked-item BLOCKED chronology SHA-256:
  `9dba85e125e5b82e618784434b63675b3eb6249eda46d7709abe74d3e634461a`.
- Atlas fragment SHA-256:
  `2cff62902c3e20ff66cfb3ceb8880e7a6bcdbc0f779d822dfb0eab9aabd8c898`.

## Verification

The producer check, independently implemented verifier, three focused tests,
five mutation rejections, strict Draft 2020-12 schema validation and residual
atlas validation all pass.  The independent verifier recomputes the
representation and orbit identities rather than importing producer matrices.

The existing Tier-3 receipt records a fail-closed repository-wide failure:
pytest collection stopped on four pre-existing `quantum-weyl` import
collisions.  Because this lifecycle reconciliation changes no mathematical
input, that exact failure is adopted rather than rerun and mislabeled as an
independent rail.  Consequently this closeout freezes the scoped
`LOCAL-ALGEBRAIC` obstruction only; it does not assert a repository-release
pass.

There were no deviations from the work package.  In particular, no forced
orbit enlargement, new architecture, later-page solve or branch-mixing table
was attempted.

EVIDENCE: `d_quotient_classical/certificates/BERGER_MINIMAL_HYPERBOLIC_BRANCH_REPAIR_RESIDUAL_ORBIT_OBSTRUCTION_V1.json`,
`d_quotient_classical/receipts/NONLINEAR_BRANCH_REPAIR_RESIDUAL_ORBIT_OBSTRUCTION_FREEZE_V2_TIER_RECEIPT.json`,
and
`residual_atlas/berger-minimal-hyperbolic-branch-repair-residual-orbit-obstruction-fragment-v1.json`.

CLOSE-OUT: DONE — the existing exact two-row residual-orbit obstruction is
adopted and scoped; the forced noncontractible enlargement remains a separate
post-Phase-1 theorem.
