# Minimal hyperbolic branch repair closeout

The rank-one first-page branch repair does not globalize as a two-row real
Berger-residual carrier.  One field direction and its cyclic dual pass the
page incidence, typed pairing and scalar `K_Berger` weight gates, but fail the
next residual-equivariance equation.

In the certified plus/cross frame the little-group generator is
`[[0,2],[-2,0]]`.  The proposed page boundary sends the new real field to the
plus class, while a one-dimensional real representation of connected
`SO(2)` is trivial.  The exact defect is `-2 beta_cross`; the normalized cross
functional evaluates it to one.  The independent verifier reconstructs this
from the tensor representation rather than importing the producer matrices.

The minimal enlargement is now classified.  Reality forces both helicities,
so a null fibre needs two configuration directions and their two cyclic duals.
In the declared finite-free support-local tensor-row category, the full
`SO(3)` orbit has rank five, Casimir `-6 I_5`, and scalar commutant.  The
global completion therefore needs an STF2 configuration bundle and its dual,
ten added BV rows.  The existing rank-46 STF2 graph is an exact negative
control: it has the right representation count but is contractible and adds
no image to the obstruction quotient.  The next candidate must be a distinct
noncontractible STF2 or equivalent mixed-bundle rank-46 carrier.

Later filtered pages, repaired-carrier `q2/q3`, and retained-`ell3`
compatibility are `NOT_ACTIVATED`, not failed calculations.  The current
retained `ell3` representative remains certified only on its pinned unsplit
carrier; its complete bounded cyclic class is open and branch projection is
`NO_CERTIFIED_MAP`.

The scoped producer, independent replay, schema, atlas validator, five
claim-inflating mutations, six imported-source verifiers and three package
tests pass.  The repository-wide Tier-3 pytest rail fails during collection on
four pre-existing `quantum-weyl` import collisions.  A broader
`d_quotient_classical` diagnostic was terminated after 486.16 seconds with
204 passes and two unrelated failures (the 26-row Green endpoint consumer
contract and persisted portable-coupled-64 certificate drift).  Neither
broad run is counted as a pass, and no release-wide freeze is claimed.

EVIDENCE: `d_quotient_classical/certificates/BERGER_MINIMAL_HYPERBOLIC_BRANCH_REPAIR_RESIDUAL_ORBIT_OBSTRUCTION_V1.json`, `residual_atlas/berger-minimal-hyperbolic-branch-repair-residual-orbit-obstruction-fragment-v1.json`, and `d_quotient_classical/receipts/BERGER_MINIMAL_HYPERBOLIC_BRANCH_REPAIR_RESIDUAL_ORBIT_OBSTRUCTION_V1_TIER_RECEIPT.json`.
CLOSE-OUT: OBSTRUCTED — the two-row page repair fails real residual equivariance and the smallest declared global free carrier is a noncontractible ten-row STF2 hyperbolic enlargement
