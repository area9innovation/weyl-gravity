# Compact-product symplectic extension classification

Date: 2026-07-21
Team: Einstein/nonlinear bridge

The pre-residual solution sequence has two distinct split structures. Its
coprime generic and exceptional primary modules split algebraically, and the
nondegenerate Einstein image has a unique orthogonal complement inside the
Weyl--Maxwell Lee--Wald space. In the certified primary representatives this
orthogonal quotient form is the direct extra Gram matrix.

This target-internal statement is invariantly expressed by the lift-shear
Schur complement. For `s_A(x)=s(x)+iota(Ax)`,

```text
C_A   = C + G_E A,
G_X,A = G_X + A^dagger G_E A + A^dagger C + C^dagger A,
S_X   = G_X - C^dagger G_E^{-1} C.
```

`S_X` is unchanged by every shear and changes only by congruence under a
quotient-basis change. Exact axial and polar `lambda=6,k=0` mutations make a
raw lifted-extra diagonal negative while returning the original positive
Schur complement. Therefore a chosen lift sign is not an extension invariant;
the Schur-complement rank, radical and inertia are.

No admissible parity-complete cyclic direct sum exists. In both generic
parities the Einstein--Maxwell positive-frequency coefficient form has
inertia `(2,0)`, while the Weyl--Maxwell restriction to the Einstein image has
inertia `(1,1)`. A complex-linear correction preserving time translation and
the two shells cannot change inertia. This cohomology obstruction rules out
every strict cyclic chain split under the declared locality, harmonic, gauge,
charge and time-translation conditions. It is independent of the extra lift.

The endpoint ledger remains typed. Exceptional `ell=1` admits the
solution-level source-to-target rescaling `B=I/2`; the homogeneous block admits
the inverse of its certified form normalizer. The twist block is obstructed
under time-translation equivariance because its Jordan centralizer has
nonnegative determinant whereas form matching requires a negative one.
Nonzero-Fourier polar `ell=0` is empty, and large `U(1)` winding does not remove
the Wilson-line tangent. No final residual split is asserted: the common
moment-map-zero quotient functor is still absent. Non-strict cyclic homotopy,
causal, particle and quantum statements remain open.

EVIDENCE: bridge/certificates/EINSTEIN_WEYL_SYMPLECTIC_EXTENSION_CLASSIFICATION_V1.json
CLOSE-OUT: OBSTRUCTED — the target has a canonical orthogonal primary split, but every declared parity-complete strict cyclic split is obstructed by invariant cohomology-form data.
