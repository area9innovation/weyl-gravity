# Observer quartic completion moduli and observable invariance close-out

The terminal common-action repair, its exact action family, the complete
source-labelled 108-row \(q_2\), and all twelve minimal quartic completions
were imported by content hash.

The twelve quartic actions define twelve independent cyclic \(q_3\)
directions.  Their \([q_1,q_{3,i}]\) columns have exact rank twelve; their
Maxwell, Berger-\(U(1)\), and odd-cyclicity defect polynomials vanish
identically.  This removes sampling ambiguity: the whole declared completion
space was tested.

The full-action arity-three identity fails for every point of that space.  The
complete old-repair \(q_2\circ q_2\) cross composition has 51,984 operator
keys and 56,544 exact coefficient terms.  Its decisive coefficient is
\[
 c^{*}_{1}\leftarrow(e_0e_0e_2A_0,\chi,K_{0,12})
 \;=\;-4g_0h_0 .
\]
It is the unique composition of the emitter Diff--BV term
\(c^{*}_{1}\leftarrow(e_0K^+_{0,02},K_{0,12})\), with coefficient \(-2\),
and the repair term
\(K^+_{0,02}\leftarrow(e_0e_2A_0,\chi)\), with coefficient \(2g_0h_0\).
Every quartic \([q_1,q_{3,i}]\) column is zero at this coordinate, so the
master-defect polynomial is
\(-4g_0h_0+\sum_i\lambda_i0\).

The arity-three admissible subvariety and hence its admissible equivalence
quotient are empty.  Total derivatives were already removed, terms vanishing
to fourth order cannot change \(q_3\), and invertible cyclic canonical
transformations or field redefinitions transport the master defect rather
than erase it.

The first missing representation is now exact: the auxiliary
\((\chi,\chi^+)\) pair needs its Diff--BV scalar orbit and common-action
covariance completion.  Since no quartic completion survives the earlier
master-equation gate, no \(K_{\rm Berger}\)-equivariant 110-row observer
morphism, same-action detector/memory response, nonlinear rank,
frequency-shift observable, tangent-cone restriction, branch result, or
quantum claim is promoted.

The next gate is
`ADJOIN_AUXILIARY_DIFF_BV_SCALAR_ORBIT_AND_REPLAY_ARITY_TWO_THREE`.

CLOSE-OUT: OBSTRUCTED — every classified quartic completion retains the exact \(-4g_0h_0\) emitter-Diff--BV cross defect

EVIDENCE: `closed_universe_observers/certificates/BERGER_QUARTIC_COMPLETION_MODULI_OBSERVER_INVARIANCE.json`
