# Phase 4 black-hole report — RW/Maxwell simplicity and endomorphisms

**Dependency tags:** `LOCAL-ALGEBRAIC`, `REDUCED-MODE`

The exact successor certificate
`black_hole_programme/phase4/rw_maxwell_simplicity_endomorphisms_v1`
closes the scalar simplicity gate left open by the parent-resolvent package.

It proves, for every integer `ell>=2` and positive real frequency, simplicity
and scalar rational endomorphism rings for the spin-two Regge--Wheeler and
spin-one Maxwell modules. It supplies exact algebraically special refusal
controls and separately excludes `i/4`, `i/2`, and `i` as reducibility points
of the `ell=2` module.

It also replaces the old specialization-sensitive axial cocycle witness by
fixed monomial minors. This closes the apparent `omega^2=3` gap and certifies
the axial `ell=2` repeated-spin-two extension as nonsplit on the whole positive
real axis. The categorical involution lemma therefore applies without a
Schur-type proviso: only `±I` occur, and no rational local dynamically
compatible involution can make the axial spin-two Weyl form positive.

The result does not classify the complete complex-frequency reducibility
locus or address nonlocal `C` operators, all-`ell` Bach nonsplitting, QNM
defectiveness, Green-resolvent poles, generalized ringdown, or quantum
positivity.
