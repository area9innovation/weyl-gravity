# Cylinder derived BFV/Koszul carrier close-out

The intrinsic 15-generator carrier is now assembled and certified: the BFV
ghost momenta are the Koszul generators, the ghost-free differential is
\(d_K\eta_A=\mu_A\), the full selected cubic BFV charge is nilpotent, the
endpoint suspension has normalization one in all components, and raw \(D\)
preserves the derived fibre.

The requested full local anomaly-to-time-slice chain map remains obstructed.
The pinned local lift lacks arbitrary-support \(q_2\), Diff/Weyl ghost and
antifield completion, portable local contraction maps, and the arity-two
chain identity.  Thus the local anomaly representatives have kinematic metric
orders \((2,1,2)\), but no derived-carrier images or cohomology verdicts.

Delivered:

- versioned strict certificate and schema;
- explicit 15-generator BFV/Koszul carrier and chain-map ledger;
- exact quantum receiver contract;
- independent all-Jacobi verifier and decisive coefficient mutation;
- nine focused tests and fail-closed atlas fragment.

The next item must construct the arbitrary-support full classical \(q_2\)
with ghost/antifield completion and portable \((q_1,\pi_{\rm cl},\iota_{\rm cl},s_{\rm cl})\), then verify support and the arity-two chain square.

CLOSE-OUT: OBSTRUCTED — the intrinsic fifteen-generator carrier exists, but the arbitrary-support full-BV local-to-time-slice chain map required to restrict anomaly cocycles is absent
EVIDENCE: bridge/certificates/CYLINDER_DERIVED_BFV_KOSZUL_TIME_SLICE_CARRIER_V1.json
