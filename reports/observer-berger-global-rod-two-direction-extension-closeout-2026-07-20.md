# Observer Berger global-rod two-direction extension close-out

The exact rank-six-to-rank-eight defect selects two new real rod backgrounds:
the normalized time-derivative partners of \(R_{0,1}\) and \(R_{1,1}\).
Together with their signed cyclic cotangents they are the required four-row
minimal repair.  Both new rods solve the Berger scalar wave equation and
come from the same positive massless scalar action as the existing rods.
The centered background now has a complete eight-dimensional \(K\) action.

The full eight-rod stress was recomputed.  Its zero and nonzero-frequency
blocks are Noether closed, and exact sparse primitives solve
\(H_{\rm retained}\Phi_2^{(8)}=-q_0^{(8)}\).  The primitives differ
nontrivially from the six-rod \(\Phi_2\), so this is a genuine background
repair rather than a decorative row addition.

The first later incompatibility is the demanded local chain embedding of the
old 108-row complex.  The new action has a nonzero mixed metric-to-new-rod
cotangent Hessian at both detectors.  The canonical row inclusion therefore
has two chain defects.  A local differential correction would have to solve
\(-s^2P(s)=cs\) with \(c\ne0\); the only formal solution is
\(P=-c/s\), a nonlocal order-minus-one map.  No support-controlled Green
realization of that comparison is certified.

Thus the exact rows, action, pairing, real structure, background \(K\)
closure and \(\Phi_2^{(8)}\) are certified, but no complete 112-row unary is
promoted as an extension of the old complex.  A replacement 112-row theory
or a separately certified nonlocal comparison is the next decision.

CLOSE-OUT: OBSTRUCTED — the action-derived four-row repair closes the background K orbit, but its mixed rod-gravity Hessian obstructs every local differential embedding of the certified 108-row complex

EVIDENCE: `closed_universe_observers/certificates/BERGER_GLOBAL_ROD_TWO_DIRECTION_EXTENSION_OBSTRUCTION.json`
