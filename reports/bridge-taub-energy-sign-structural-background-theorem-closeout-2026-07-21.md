# Bridge Taub-energy structural-background close-out

`CLOSE-OUT: SHORTFALL / FIRST EXACT OBSTRUCTION`

The fixed-Chern product-background chambers are now classified exactly.  The
compact Plebański--Hacyan Taub-sign fixture is the flat double-root wall of the
family, not a generic interior point.  Above the critical coupling the common
background splits into distinct positive- and negative-\(k_1\) branches; below
it no real fixed-Chern pure-magnetic common background exists.

The requested off-wall sign theorem cannot yet be stated: the imported generic
product tangent artifact certifies only a principal-symbol chain map and
explicitly lacks the curvature/flux lower-order Hessians, branch dictionary
and action-derived Lee--Wald current.  The result therefore records
`NO_CERTIFIED_MAP` rather than transporting the wall signs by analogy.

Delivered:

- exact certificate and strict schema;
- independent, non-importing symbolic verifier;
- sign-wall and false-promotion mutation controls;
- nine focused tests;
- fail-closed residual-atlas fragment;
- explicit low-curvature dS branch as the next computation.

Not delivered:

- off-wall q/p branch energies or stabilizer moment maps;
- a necessary-and-sufficient Taub sign theorem in either open chamber;
- a Paper 13 source edit.  The work item names a non-existent legacy Paper 13
  path, while the actual draft is outside the declared allowed path.  The
  certificate/report/atlas preserve the scope correction for coordinator
  integration without editing an undeclared file.

Disposition recommendation: `OBSTRUCTED` on the missing parameterized
lower-order tangent/symplectic carrier.  A successor should build that carrier
on the low-curvature dS branch before addressing the AdS branch.
