# Phase 2 polar extendible-module/current closure

The imported six sourced branches remain exact finite-depth, log-free and
compatible. The old oscillatory logarithm remains a nowhere-extendible
shallow-source artifact.

## Source-zero module correction

The asymptotic four-state homogeneous Bach master has characteristic
polynomial \(z^3(z+2i\omega)\), but its generalized polynomial directions are
not automatically solutions of all seven source-zero Ricci rows. For the
advertised direction

\[
C=B=0,\qquad K=\kappa,\qquad A=i\omega\kappa r+a_0,
\]

the angular row fixes \(a_0=(2-\Lambda)\kappa/2\), after which the \(vv\) row
retains

\[
\frac{i\kappa}{4r^2}
\left(i\Lambda^2-2i\Lambda+12\omega\right).
\]

It is nonzero on the declared generic physical domain. The four-state master
therefore cannot by itself supply the input basis for an invariant current
calculation.

Exact full-seven-row GJ prefixes instead give dimensions \(1,1,2,3\) for the
zero log-free, zero log-at-most-one, oscillatory log-free and oscillatory
log-at-most-one systems through depth eight. One zero and one oscillatory
direction restrict nontrivially to lower orders. The other two oscillatory
vectors first appear at the terminal order \(n=8\); they remain typed
`TERMINAL_ONLY_PREFIX` until their \(q=9\) compatibility is tested.

The literal current parser is independently checked: its expanded numerator
has 272 terms in 79 oriented field/derivative signatures. The theorem below
uses only the restriction-stable oscillatory direction and the three sourced
non-Einstein reconstructions.

## Convention mutation

Reconstruction jets use \(e^{+i\omega v}\), whereas the literal current's
left slot uses \(e^{-i\omega v}\). Raw insertion gives a false divergent
Einstein control at \((\Lambda,\omega)=(6,3/5)\). Conjugating the complete
reconstruction profile once at this boundary restores cancellation of every
dangerous Einstein self-layer \(p\ge-1\), matching the certified \(\ell=2\)
control. This failed raw insertion is retained as a mutation test.

## Exact dangerous-layer current filtration

Let

\[
(E,X_0,X_1,X_2)
\]

denote the restriction-stable oscillatory Einstein direction and the three
sourced non-Einstein reconstructions. All sixteen ordered current entries are
evaluated over \(\mathbb Q(i)[\Lambda,\omega]\) through the two nonintegrable
radial layers \(p=0,-1\). The apparent powers \(p=6,\ldots,1\) cancel
identically. Both coefficient matrices are anti-Hermitian.

The entry denominators factor by slots:

\[
G^{(p)}_{ij}=\frac{N^{(p)}_{ij}}{D_i\overline{D_j}}.
\]

Thus the numerator matrix has the same rank away from reconstruction poles.
Write

\[
N^{(0)}=\begin{pmatrix}0&a\\ b&K\end{pmatrix}.
\]

Exact polynomial arithmetic gives

\[
\det K\not\equiv0,
\qquad
a\,\operatorname{adj}(K)b\equiv0.
\]

Consequently, wherever \(\det K\ne0\), the full leading matrix has rank three
and

\[
y=\bigl(\det K,-\operatorname{adj}(K)b\bigr)
\]

spans its one-dimensional radical. Direct multiplication verifies
\(N^{(0)}y=0\). More strongly, the induced next-layer form cancels identically:

\[
y^\dagger N^{(-1)}y\equiv0.
\]

Therefore a one-dimensional **mixed Einstein/additional** line survives the
complete dangerous filtration \(p\ge-1\). The chosen-complement determinant
has no zeros on the declared physical domain. The Einstein line is finite
against itself, but has nonzero
leading cross-current with sourced additional directions. Finite radial
current therefore does not canonically isolate the Einstein representative
on this formal infinity module.

The invariant extra-block determinant factors as

\[
\det K=-133590662774784\,i\,
\Lambda\omega^{19}(\Lambda-2)^2
P_6(\Lambda,\omega^2)P_{20}(\Lambda,\omega^2),
\]

with exact sparse factors stored in the matrix artifact. Both remaining
factors are nonzero on the complete physical set. First,
\(P_6=\Delta\overline\Delta\); on the zero locus of \(\operatorname{Im}\Delta\)
its real part is a strictly positive polynomial in \(\omega^2\). Second,
\(P_{20}=A^2+\omega^2B^2\). The exact resultant of \(A\) and \(B\) is
\(C\Lambda^3(\Lambda-2)^3R_{87}(\Lambda)\), and
\(R_{87}(n(n+1))\) is nonzero modulo 23 for every residue class of \(n\).
Thus the canonical pivot is nonzero for every integer \(\ell\ge2\) and real
\(\omega\ne0\).

The first finite layer is also explicit. On the filtered line its coefficient
factors as

\[
C\,\omega^{51}\Lambda^3(\Lambda-2)^5
P_6^2P_{20}Q_{21}(\Lambda,\omega^2).
\]

It is generically nonzero, so the surviving mixed line is generically
**nonradical**, not merely a formal cancellation direction. The exact
exceptional set is
\(Q_{21}(\ell(\ell+1),\omega^2)=0\). At those isolated algebraic frequencies
the filtration must be continued below \(p=-2\); no deeper claim is made.
The serialized \(Q_{21}\) has nonzero leading \(\omega^2\)-coefficient for
every \(\Lambda\ge6\), so this exceptional set is finite for each fixed
physical \(\ell\). A uniform count of its positive roots as \(\ell\) varies is
not claimed here.

This conclusion is invariant under admissible changes of the extra lift. For
\(X'=X+Ec\), the full matrix transforms by congruence
\(G'=S^\dagger GS\). Its rank, radical dimension, filtered-line dimension and
the nonvanishing Einstein cross covector are invariant. Raw \(XX\) entries,
the chosen-complement \(\det K\), radical coordinates and any sign assigned to
one selected lift are not invariant; an exact shear mutation records this
distinction.

The zero-rate block is not folded into the radiation theorem. It is
stationary/generalized-zero data, while the physical conjugate-frequency
current here assumes real \(\omega\ne0\). Its dangerous-layer cancellations
are therefore not promoted as either a gauge or a radical theorem.

## Scope

This is a formal radial-infinity theorem for real \(\omega\ne0\). It does not
show that the mixed radical matches a future-horizon-regular solution, belongs
to a completed asymptotically flat phase space, or defines scattering data.
The \(q=9\) compatibility of terminal-only prefix vectors also remains open.
They vanish under restriction to every lower certified order, so excluding
them makes the retained homogeneous input maximal among restriction-stable
directions at this depth. The retained \(E\) direction has zero Ricci carrier
and is Einstein-image; the \(X_i\) have sourced nonzero Ricci carriers. No
independent residual diffeomorphism or Weyl-gauge vector occurs in the generic
\(\ell\ge2\), \(\omega\ne0\) gauge-fixed restriction image.

CLOSE-OUT: DONE — the maximal restriction-stable 4x4 current filtration,
canonical-pivot wall, lift congruence and generic nonradical finite line are
closed. Terminal-only q9 extension, the isolated \(Q_{21}\) frequencies and
horizon-to-infinity matching remain outside this scoped theorem.

EVIDENCE: `black_hole_programme/phase2/general_l_polar_extendible_current_closure/receipt.json`
