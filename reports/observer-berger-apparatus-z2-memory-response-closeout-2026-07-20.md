# Observer Berger apparatus Z2 memory-response close-out

The action-derived apparatus parent, its full payload, both localized emitter
preparations, the same-background component/unary/q2 candidates, the solved
global-rod source sector, the reduction preflight and the abstract
finite-harmonic tangent-cone theorem were imported by exact hashes.

The current parent does not define the receiver needed to instantiate that
abstract criterion on the two detector-selected Berger preparation
directions.  In particular it has no row-level crosswalk of \(u_0,u_1\) into
one combined complex; no quadratic source map on
\((u_0,u_0),(u_0,u_1),(u_1,u_1)\); no complete output-block closure; no
stabilizer moment-map/Taub projections; and no reduced nonzero-shell
adjoint-cokernel bases or resonant pairings.  The independently certified
global-rod source is a different quadratic sector and cannot fill this gap.

`BERGER_APPARATUS_SAME_BACKGROUND_Z2_RECEIVER_CONTRACT` now specifies the
complete exact interface.  It keeps bounded/quasiperiodic, smooth secular and
causal/retarded correction spaces as separate result kinds, and requires
class-specific right inverses before a zero obstruction pairing is accepted.
It also requires an exact amplitude ideal, componentwise detector
rank/kernel, and gauge-, real- and \(K_{\rm Berger}\)-covariant memory
transport.  The corresponding typed classical request is
`sf:program/request/observer-berger-apparatus-z2-memory-response-to-classical-29bed3bc4a756983`.

The leading triangular response remains rank two in the certified parent
scope.  It is not promoted to nonlinear rank two.  Individual preparation
membership, balanced combinations, persistent memory, gauge survival on
\(\mathcal Z_2\), recoil and relational redshift are all
`NO_CERTIFIED_MAP`.  No compact-product carrier is imported.

Verification receipts: `py_compile` passed in 0.03 s; generation in 0.04 s;
the method-distinct verifier in 0.11 s; atlas generation in 0.83 s and
independent atlas verification in 1.65 s.  The five package tests plus the
complete observer-atlas test module passed 60 tests in 27.34 s.  Two
`pdflatex` passes completed in 0.56 s and 0.57 s, and scoped
`git diff --check` passed.  Tier 2 used exact hashes for every imported
operator/certificate because no shared mathematical input changed.  Tier 3
was not run because this shortfall promotes no theorem, freeze, tag or
release.

CLOSE-OUT: SHORTFALL — the same-background Berger quadratic source/cokernel receiver is absent, so the detector response and memory cannot be restricted to Z2

EVIDENCE: `closed_universe_observers/certificates/BERGER_APPARATUS_Z2_MEMORY_RESPONSE_CROSSWALK.json`

MISSING-DEP: sf:program/request/observer-berger-apparatus-z2-memory-response-to-classical-29bed3bc4a756983
