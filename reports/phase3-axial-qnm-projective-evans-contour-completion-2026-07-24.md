# Phase 3 axial projective Evans contour completion

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

## Checkpoint: exact prefix 127/512

The first bounded continuation from the imported `123/512` handoff evaluates
the eight panels

```text
246/1024, ..., 253/1024
```

with the unchanged projective transport, stable horizon-remainder root and
zero-exclusion threshold. All eight panels pass. The exact contiguous
zero-free boundary prefix is now

\[
[0,254/1024]=[0,127/512],
\]

and the next honest gap begins at `254/1024`.

The producer completed in 27.94 seconds, below the scoped approximately
60-second loop target. Its replayless verifier, JSON schema, compilation rail
and three focused mutation tests pass.

## Claim boundary

This is a checkpoint, not a terminal contour theorem. It does not establish:

- nonvanishing on the complete closed contour;
- an argument-principle winding or root count;
- a QNM location or simple-root certificate;
- nonvanishing of the intrinsic projective tangent;
- a Smith branch, exceptional point or Green-resolvent pole.

The continuation must resume at `254/1024`. Only after the full boundary
excludes zero may the argument-principle and local selector gates be opened.

## Checkpoint: exact prefix 135/512 and lifted phase

Two further bounded producers evaluated panels `254` through `269` at
denominator `1024`. All sixteen panels pass the unchanged typed
zero-exclusion and two-sided interface gates. The exact contiguous prefix is
now

\[
[0,270/1024]=[0,135/512],
\]

and the next honest gap begins at `270/1024`.

A replayless phase-ledger certificate reissues all `172` accepted affine
segments as rational separating-half-plane certificates. Every associated
argument sector has width below \(\pi\), and every adjacent pair overlaps.
Consequently the entire open prefix has a continuous lifted argument branch.
The partial increment is enclosed by

\[
-1.2739656266
<
\Delta\arg\Delta\big|_{[0,135/512]}
<
-0.2264558028.
\]

The contour remains open. The phase enclosure is not a winding number, and
argument-principle, root-count, QNM, Smith and EP2 claims remain false.
