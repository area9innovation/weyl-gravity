# Paper 15 Phase-2 interest upgrade v2

## Outcome

Paper 15 now presents the externally decisive Phase-2 results while retaining
the frozen Phase-1 claim cards as an additive historical layer.  The subtitle
is lifecycle-neutral; the date line identifies the document as a Phase-1
synthesis with a scoped Phase-2 update.  The author block is unchanged.

The upgraded scientific narrative is:

- the generic-angular Schwarzschild calculation refutes Einstein-only
  selection in the declared formal radial class through an axial
  non-Einstein counterexample;
- the polar oscillatory filtration retains a one-dimensional mixed line, with
  an exact (Q_{21}) exceptional wall and exact counts for zeros of its
  induced (p=-2) coefficient, not scattering thresholds;
- selected compact reduced blocks admit structured positive
  pseudo-Hermitian metrics, but the data do not construct independent
  (P,T,C), and the cylinder candidate fails BRST chain compatibility;
- the counterflow Hamiltonian--Hopf quartet is in the broken-PT phase and
  spectrally forbids a positive pseudo-Hermitian metric;
- Nariai supplies opposite Lee--Wald factor residues without an absolute
  energy sign or identification with the compact-product branches;
- the dyonic flat-family preflight is obstructed before any stationary sign
  phase diagram exists;
- the complete Berger BV Hadamard stretch stops at a named missing
  104-dimensional carrier.  This is not a covariance nonexistence theorem;
- the strict anomaly coordinates remain stable under similarity transforms
  only within the declared local operator/regulator class.  A new
  contour/measure is a changed theory requiring its own regulator and QAP.

The Pais--Uhlenbeck equal-frequency Jordan structure is presented only as an
analogy.  No `eta` is called a Mannheim (C) operator, no reduced-mode result
is promoted to a Lorentzian state, and no formal radial result is called a
phase space, scattering theorem, or stability theorem.

## Evidence integration

The v3 claim map imports five Phase-2 result certificates by exact SHA-256 and
also pins the repaired generic-angular terminal receipt, report, and
correction request.  Its `theorem_cards` object is byte-semantically equal to
the frozen v2 object generated before this update.

The append-only reverse-coverage overlay retains all Phase-1
`OVERVIEW_SYNTHESIS` edges and adds typed Phase-2 result, paper-claim, and
result-paper-edge nodes.  The new edge kinds distinguish a theorem,
supporting evidence, limitations, and negative results.

## Verification

Tier 0 passed:

- Python compilation of the generator, verifier, and mutation tests;
- `git diff --check` on every scoped text, JSON, and Python path;
- exact inspection of the author block and generated hashes;
- two warning-free `pdflatex -interaction=nonstopmode -halt-on-error`
  passes, producing a 21-page PDF with no undefined citations or references.

Scoped Tier 1 passed:

- deterministic claim-map and reverse-coverage regeneration;
- independent Paper 15 verifier;
- four Paper 15 tests, including mutations of a frozen lifecycle, a Phase-2
  hash pin, and the author block.

The affected Tier-2 evidence chain passed independent replay:

- generic-angular Schwarzschild synthesis;
- CPT feasibility classification, including eight rejected mutations;
- Berger full-BV Hadamard obstruction;
- Nariai sign mechanism;
- dyonic flat-family preflight.

Tier 3 was triggered because this work promotes scoped results into a paper.
`python3 -m unittest discover -s . -p 'test_*.py' -f` passed all 149
discovered subtree tests.  The imported producers were not regenerated:
their terminal certificates are immutable inputs checked by hash and
independent verifiers.

## Boundaries

This paper update does not establish an asymptotically flat Bach phase space,
horizon-to-infinity matching, wave-packet flux topology, all polar
filtrations, a genuine BRST-compatible (C) operator, a full Hadamard
covariance, a positive physical Hilbert space, particles, scattering, the
optical theorem, or unitarity.

EVIDENCE:
`paper/15-four-level-ghost-classification-phase1-synthesis-claim-map.json`;
`planning/paper-coverage/paper15-phase1-synthesis-overlay-2026-07-22.json`;
`reports/PAPER15_PHASE2_INTEREST_UPGRADE_V2_TIER_RECEIPT.json`.

CLOSE-OUT: DONE — Paper 15 is upgraded by exact terminal Phase-2 evidence
without widening the source theorems or changing the frozen Phase-1 record.
