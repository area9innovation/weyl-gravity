# Paper 13 third-order Kuranishi disposition

Paper 13 reaches `THEOREM_FROZEN` for external specialist review.  The
publication theorem now distinguishes the two third-order statements that
must not be merged:

- the intrinsic compact-stabilizer class is correction independent and
  vanishes exactly in the two-dimensional quotient
  `O / im(l2)`, with `im(l2)=span{H,J1,J2}` and quotient basis `{P_x,J3}`;
- the four-shell bounded obstruction and finite exponential-polynomial
  secular preimage are certified for the declared
  no-homogeneous-addition second-order representative.  Their quotient by
  all admissible homogeneous second-order additions remains `OPEN`.

The manuscript now types the Taub pairing through the spatial constraint
map, states the blockwise cokernel table and generalized-mode convention,
uses finite exponential-polynomial rather than “smooth” as the correction
module, exposes the third-order result in the abstract and theorem hierarchy,
and places the bibliography after all scientific sections.  The claim map
pins 60 exact source blobs at the action-normalized baseline and the atlas
keeps bounded, finite exponential-polynomial and causal dispositions
separate.

The first full-package run failed closed after the manuscript hash exposed a
transitive provenance migration.  No failure was waived.  Every affected
producer and independent verifier was re-locked, the unified graph reached
1,986 exact edges with zero stale references, and the final package run
passed 1,391 tests with one declared skip.

This does not establish a bounded third-order correction for another choice
of second-order representative, a general cubic finite-harmonic theorem,
retarded solvability, all-orders integration, particles, positivity,
unitarity or quantum consistency.

EVIDENCE: `bridge/einstein_sector/receipts/PAPER13_THIRD_ORDER_KURANISHI_DISPOSITION_V1_TIER_RECEIPT.json`; `paper/13-compact-weyl-maxwell-second-order-tangent-cone-claim-map.json`; `bridge/certificates/EINSTEIN_WEYL_COMPACT_CAUCHY_THIRD_ORDER_KURANISHI_EVALUATION_V1.json`.
CLOSE-OUT: DONE — Paper 13 is theorem-frozen at the strongest correction-independent scope supported by the complete certificate chain.
