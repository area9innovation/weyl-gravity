# Observer auxiliary Diff--BV scalar-orbit repair close-out

The predecessor action, complete \(q_1\), 636-key repaired \(q_2\), and
twelve quartic \(q_3\) directions were imported by content hash.  The
smallest complete scalar Diff--BV orbit is the one-parameter action
\[
 \alpha\int \chi^+c^ae_a\chi .
\]
Exact cotangent differentiation produces 32 graded-symmetric \(q_2\) keys.
Its 30-key contractible-pair covariance defect cancels the inherited defect
if and only if \(\alpha=1\).

The forced normalization does not repair the complete action.  The earliest
source-isolated arity-two coefficient is
\[
 \tau^*\leftarrow(e_0e_1A_0,K_{0,01})=g_0h_0 .
\]
It comes from the emitter \(q_1\) and emitter Diff--BV \(q_2\), is independent
of \(\alpha\) and all twelve quartic parameters, and cannot be reached by the
scalar orbit because that action has no \(A\)--\(K\) Hessian.  The arity-two
admissible locus is therefore empty before \(q_3\) can contribute.

As a diagnostic, the scalar-repair \(q_2\circ q_2\) cross composition has
10,776 keys and 11,112 exact coefficient terms, but vanishes at the previous
arity-three witness.  The inherited coefficient
\[
 c^*_{1}\leftarrow(e_0e_0e_2A_0,\chi,K_{0,12})=-4g_0h_0
\]
therefore survives, and every imported quartic \([q_1,q_3]\) column remains
zero at that coordinate.

The next common-action enlargement must supply both an action-derived
temporal \(A\)--\(K\) Hessian with
`chi_plus <- (e0 e1 A_0,K0_01)` and a quartic Diff-covariance descendant
whose \(q_1\) image reaches the spatial `c_plus` witness.  Until those
representations exist, no \(K_{\rm Berger}\)-equivariant observer morphism,
detector or memory response, redshift, recoil, tangent-cone, branch, or
quantum claim is promoted.

CLOSE-OUT: OBSTRUCTED — the complete auxiliary scalar Diff--BV orbit is uniquely normalized but retains exact arity-two and arity-three master defects

EVIDENCE: `closed_universe_observers/certificates/BERGER_AUXILIARY_DIFF_BV_SCALAR_ORBIT_REPAIR.json`
