# Observer common-action nonlinear promotion gate close-out

The committed order-two result was imported by exact content hash.  The
complete target-grade integration-by-parts normal form retains differentiated
`K`, removes differentiated `chi` only modulo exact total derivatives, keeps
all lower PBW counterterms, and separates positive coefficient jets from the
undecorated `g_b h_b` target monomial.

The filtered target space has dimension 3,960 per emitter.  Exact
Berger-`U(1)` reduction gives kernel dimension 932 per emitter, with 466
leading reflection-even and 466 leading reflection-odd lines.  All 1,864
actions, their Euler-Hessian `q2` columns and Ward vectors are serialized.
The extended coefficient-jet Ward carrier closes at dimension 8,152.

The complete image grows from rank 230 through order two to rank 1,922
through order three.  The typed source does not increase that rank.  On the
decisive 64-coordinate exact `g_b h_b`, input-order-two grade, the projected
rank is 592 with or without the source.  A sparse exact solve exports a
36-module repair using one fixed constant auxiliary unary.  Its 636-key
`q2` is derived from one summed cubic action, has zero Maxwell gauge defect,
and equals the complete typed-source Ward vector exactly.

The conditional nonlinear gate does not pass.  The cubic repair has zero
fourth derivative and structurally zero self-composition, but it does not
select a unique quartic completion.  The zero completion and
`lambda chi^2 g0 h0 <K0,dA>` have identical certified `q1,q2` at the zero
auxiliary background and different `q3`; an explicit nonzero fourth-
derivative entry is stored.  Complete `q2q2+q1q3`, odd cyclicity through a
selected `q3`, `K_Berger` equivariance, apparatus/memory closure, causal
support and backreacted detector rank therefore remain `NO_CERTIFIED_MAP`.

No raw-`D`, redshift, recoil, tangent-cone, branch, particle, positivity,
scattering, phenomenology or quantum claim is promoted.  The next exact
gate is classification of admissible quartic completions of the repaired
common action.

CLOSE-OUT: OBSTRUCTED — the order-three common action repairs the typed-source quotient, but two exact quartic completions with the same q1/q2 give different q3
EVIDENCE: closed_universe_observers/certificates/BERGER_ORDER_THREE_COMMON_ACTION_PROMOTION_GATE.json
