# Phase 3 axial QNM: horizon moving-phase projective preflight

The selected horizon phase has been removed exactly, and
`dot(lambda_H)=0`; the tau jet therefore introduces no horizon logarithm.
The regular scalar Frobenius germ, tau sensitivity, and omega sensitivity
are enclosed on all 16 complex-frequency panels at `rho=2^-22`.

The complementary outward projective rail nevertheless refuses before the
common `r=32` match point.  Its first obstruction is not the Frobenius germ
or affine remainder: the absolute Cauchy majorant for the midpoint reference
trajectory loses a positive Riccati discriminant.  The exact per-panel
refusal radii are materialized in `horizon-run.json`.

No projective mismatch, root count, QNM, Smith selector, or EP2 is claimed.
