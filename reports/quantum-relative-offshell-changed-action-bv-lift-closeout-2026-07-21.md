# Quantum relative off-shell changed-action BV-lift close-out

## Deliverables

- Exact quantum-side producer and content-addressed certificate for the
  selected quadratic-action repair orbit.
- Strict Draft 2020-12 schema.
- Independent quantum integration verifier and nine mutation/regression
  tests.
- Imported, hash-pinned upstream independent six-density action-variation
  rail.
- Human-readable scientific report and fail-closed residual-atlas fragment.
- Test-tier receipt with commands, elapsed times, memory and skipped-tier
  criteria.

## Gate result

The complete real parity-even local Diff x U(1) Einstein--Maxwell action
quotient through four derivatives has no preimage for the requested reduced
rank-one source-action repair.  The first invariant obstruction is the axial
q-primary (2,2) coefficient of lambda: it is zero on the full action-response
image and -9 on the requested shift.  The polar lambda-squared functional is
independently 0 versus -9/4.  The 17-by-6 p-shell cross-response matrix has
exact rank six and zero kernel.

This closes the work item by its obstruction branch.  The requested changed
local action is `OBSTRUCTED`; the master action, BV differential, odd pairing,
nonminimal sector, gauge-fixed operator, 40-to-38 cyclic lift and common
regulator are `NOT_ACTIVATED`.  Relative anomaly coefficients are
`NOT_COMPUTED` and the relative QME remains `UNDEFINED`.

## Scope and deviations

Exactly one repair orbit was selected.  The axial and polar shifts are not
different orbits; one parity-even covariant action must realize both.  No
pairing-only or physical-auxiliary repair was mixed into the solve.

The expensive upstream independent action-variation verifier was not rerun:
its certificate and PASS receipt are unchanged and pinned by exact hashes,
while the new quantum verifier independently replays the finite relation
rank, both cokernel witnesses and the p-shell rank.  Under the repository test
tier policy this avoids normalizing a 198-second unchanged-input calculation
as a commit-loop requirement.

Paper 12's lifecycle is explicitly disposed in the certificate and report as
`SCOPED_FOUR_DERIVATIVE_CHANGED_ACTION_ROUTE_OBSTRUCTED_RELATIVE_QME_REMAINS_UNDEFINED`.
The manuscript source was not changed because this scoped work item does not
authorize the generated Paper 12 claim ledger and PDFs needed for a coherent
paper-artifact update.  No paper theorem or lifecycle was silently promoted.

Tier 2 was not run because no shared mathematical input changed; all affected
inputs are imported by exact hashes.  Tier 3 was not run because this is not a
freeze, release or lifecycle promotion to coefficients, QME restoration or
Lorentzian causality.

## Verification

All scoped checks pass:

- exact producer replay: 0.42 s;
- independent quantum verifier: 0.42 s;
- nine tests: 0.54 s;
- strict AJV Draft 2020-12 validation: 1.86 s;
- residual-atlas validation: 0.11 s;
- Python compilation, JSON parsing and scoped `git diff --check`.

EVIDENCE: quantum-weyl/relative/certificates/RELATIVE_OFFSHELL_CHANGED_ACTION_BV_LIFT_OBSTRUCTION_V1.json
CLOSE-OUT: DONE — the complete declared four-derivative changed-action ansatz has an exact first cokernel obstruction, so the requested off-shell BV lift does not activate
