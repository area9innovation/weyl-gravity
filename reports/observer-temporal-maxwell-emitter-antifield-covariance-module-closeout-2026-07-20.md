# Observer temporal Maxwell/emitter antifield covariance close-out

The direct temporal covariance certificate and payload from commit
`0e1e26d4844241cb70280f714cba5bc22306c60d` were imported by exact content
hash.  Their 934-column action image and 42-coordinate Maxwell-cotangent
quotient are unchanged.

The declared bounded existing-row completion contains 2,048 exact local
actions per emitter.  It includes both forced cotangent sectors,
\(A^+/\tau/K\) and \(K^+/\tau/A\), every Berger-\(U(1)\)-invariant action at
orders zero, one and two, and both cotangent transforms of all 932 inherited
IBP-closed order-three actions.  Both leading-symbol reflection parities and
all differentiated placements are present.  The unary differential is
unchanged and nilpotent; the odd pairing and real structure are unchanged;
each \(q_2\) column is the exact Euler Hessian of one serialized local action.

For each source-isolated emitter, the new module has exact rank 1,679 modulo
the predecessor image.  Hence the full action image has rank 2,613, while
adjoining the covariance source raises the rank to 2,614.  The final quotient
still has 42 coordinates and begins at
\[
 A^+_0\leftarrow(\tau,e_0e_1K_{b,01})=-3g_bh_b .
\]
Omitting either cotangent sector is independently rank-detectable.  A
separate verifier reconstructs every action and Hessian manifest before
recomputing both exact cokernels.

This is a completeness obstruction for the declared bounded existing-row
module, not for all possible field extensions.  The first unexcluded object
is a genuinely new degree-compatible \(q_1\)-preimage of \(A^+\), completed
through the Maxwell ghost-antifield differential and the signed cotangent
descendants needed for nilpotency and cyclicity.

Because the arity-two action locus remains empty, no quartic \(q_3\),
\(K_{\rm Berger}\), detector, redshift, memory, recoil-corrected rank,
tangent-cone restriction, branch identification or quantum result is
promoted.  In particular, no compact-product mode is identified with a
Berger carrier.

CLOSE-OUT: OBSTRUCTED — all 2,048 bounded existing-row antifield actions per emitter retain the exact rank-one Maxwell-cotangent covariance obstruction

EVIDENCE: `closed_universe_observers/certificates/BERGER_TEMPORAL_MAXWELL_EMITTER_ANTIFIELD_COVARIANCE_MODULE.json`
