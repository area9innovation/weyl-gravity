# Observer dynamical apparatus parent close-out

The blocked temporal all-jet source class and its finite second-jet
predecessor were imported by exact hashes and kept explicitly unresolved.

One minimal real first-order action now supplies two transported
rod-orientation doublets, two transported detector-polarization doublets, two
persistent memory pairs and two transported emitter-phase doublets.  The 28
even physical rows have 28 odd cotangent antifields.  The ghost ledger is
empty for a structural reason: Berger-\(U(1)\) is used as a rigid
simultaneous covariance, not a gauged redundancy, and the nonzero-frequency
transport symbols have no gauge kernel.

The quadratic action derives \(q_1\).  Per detector the cubic
\(-\lambda_a p_a\cdot F_a\) derives twelve ordered \(q_2\) records.  Exact
action differentiation certifies nilpotency through arity two, unary and
binary cyclicity, the real structure, simultaneous \(K_{\rm Berger}\)
covariance and first-order causal clock-transport blocks.

On the \(\lambda_a=0\) probe branch, the background polarization matrix is
the identity.  The induced detector matrix is therefore the imported
\[
\begin{pmatrix}\kappa_0&0\\ \mu&\kappa_1\end{pmatrix},
\qquad \det=\kappa_0\kappa_1\ne0,
\]
so the certified leading rank two survives.  A cloned detector polarization
drops the selection rank to one and is rejected.

Every new cubic Hessian coordinate contains at least one new apparatus input.
The imported 42-coordinate finite second-jet source support is purely old.
Their exact support intersection consequently has rank zero: the new rows do
not alter that source class and do not solve its all-jet membership problem.

Verification receipts: `py_compile` passed in 0.05 s; the method-distinct
verifier passed in 0.39 s; six scoped tests plus 55 atlas tests passed in
25.49 s; atlas schema validation passed in 0.69 s; independent atlas replay
passed in 1.62 s; and two direct `pdflatex` passes completed in 0.49 s and
0.50 s.  Scoped `git diff --check` passed.  Tier 2 is limited to the exact
hash checks of the directly imported certificates because no shared
mathematical input changed.  Tier 3 was not run because no freeze, tag,
release or cross-programme theorem is promoted.

CLOSE-OUT: DONE — the minimal action-derived dynamical apparatus parent closes through arity two and preserves the certified leading rank-two response

EVIDENCE: `closed_universe_observers/certificates/BERGER_DYNAMICAL_APPARATUS_PARENT.json`
