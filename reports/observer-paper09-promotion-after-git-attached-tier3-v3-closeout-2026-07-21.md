# Paper 9 promotion after Git-attached Observer Tier 3 v3

Decision: `NO_PROMOTION`; Paper 9 remains `DRAFT_ALLOWED`.

The conditional promotion gate requires a complete green Observer Tier-3
receipt. The Git-attached successor repaired the materialization envelope and
verified all 11,191 scoped blobs, but its exactly one authoritative fail-fast
run passed 826 tests and then stopped at the stale
`BERGER_108_ROW_EMITTER_PHYSICAL_Q2_PBW` comparison-ledger entry. That entry
has both an obsolete artifact hash and a materially obsolete claim boundary.
The full suite is therefore not green.

The non-green branch does not authorize publication regeneration. The
22-claim map, coverage ledger, article and supplement sources, and both PDFs
remain byte-identical and are independently hash-checked. Neither PDF was
rebuilt. The seven-row receiver census remains complete; every legacy receiver
row remains `NO_CERTIFIED_MAP`; no operational frequency ratio exists at the
tested point; no coordinate ratio is promoted as redshift; every certified
repaired-q70 physical block remains unstable; and higher isotypes remain
`NO_CERTIFIED_MAP`.

The exact first boundary is semantic synchronization of the comparison-ledger
bridge-artifact entry, followed by a newly owned single fresh fail-fast
Observer Tier-3 run. A blind hash-only repin is insufficient. Even after that
suite becomes green, the previously certified recursive source-binding cycle
and three stale health-nonactivation dependency pins remain separate Paper 9
freeze boundaries.

Independent verification of terminal imports and preserved publication and
observer dispositions passed, together with 16 fail-closed mutations (17
tests total). No observer science was reopened and no scientific claim was
changed.

EVIDENCE:
`closed_universe_observers/receipts/PAPER09_PROMOTION_AFTER_GIT_ATTACHED_TIER3_V3_NO_PROMOTION.json`;
`closed_universe_observers/receipts/PAPER09_PROMOTION_AFTER_GIT_ATTACHED_TIER3_V3_TIER_RECEIPT.json`;
`closed_universe_observers/receipts/OBSERVER_TIER3_GIT_ATTACHED_EXACT_MATERIALIZATION_RERUN_V1_OBSTRUCTION.json`.

CLOSE-OUT: DONE — terminal `NO_PROMOTION` disposition; Paper 9 remains
`DRAFT_ALLOWED`.
