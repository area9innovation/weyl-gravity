# Observer Berger apparatus physical reduction close-out

The typed 160-row unary carrier is valid, but its content-addressed payload
does not export executable (q_1) entries by support and zero-mode sector.
The changed positive-mixed (Phi_2) also prevents importing the older 108-row
operator as replacement cohomology.  Exact kernels, images, contraction data,
descended pairing and detector-record classes are therefore undefined from
the certified inputs.

CLOSE-OUT: OBSTRUCTED — physical reduction is `NO_CERTIFIED_MAP` until an executable sectorwise 160-row q1 is exported

EVIDENCE: `closed_universe_observers/certificates/BERGER_APPARATUS_PHYSICAL_REDUCTION_NONDEFINITION_AFTER_REPLACEMENT_112.json`

## Verification receipt

- Tier 0: Python and structured-data parsing plus scoped `git diff --check`
  — PASS.
- Tier 1 producer and schema validation — PASS.
- Tier 1 independent structural verifier — PASS.
- Tier 1 tests — PASS, 5 tests (0.09 s).
- Tier 2 atlas generator and independent verifier — PASS.
- Tier 2 direct consumers — PASS, 60 tests (24.59 s).
- Paper 09 compiled twice with `pdflatex` into a temporary output directory —
  PASS; no out-of-scope PDF was written to the worktree.
- Tier 3 is not run: this is a fail-closed interface result, not a freeze,
  shared-core change or release.
