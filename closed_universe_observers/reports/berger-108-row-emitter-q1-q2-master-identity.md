# Covariant 108-row emitter `q1-q2` master identity

Dependency tag: `LOCAL-ALGEBRAIC`.

The 84-row apparatus identity through its certified `r`-first jet and the
selected massive-two-form action now stitch into one covariant 108-row
arity-two theorem over that coefficient ring.  Expanding the local BV master
action about the zero-emitter Berger apparatus background gives

```text
{S_2,S_3}=0
```

and, after raising one slot with the certified odd pairing,

```text
q1 q2(x,y) + q2(q1 x,y) + (-1)^|x| q2(x,q1 y) = 0.
```

The replay checks the independent mechanisms needed for this coefficient:

- `delta E_A=0` for the coexact switched emitter current;
- the reciprocal `h_b'(Theta)` clock source in the energy identity;
- metric, clock, Maxwell, and emitter outputs from one symmetric action jet;
- Weyl compensation through `gHat=rho^2 g`;
- the Diff action `L_c K_b` and its cotangent transpose.

Rows 0--63 import the authoritative gravity-clock-Maxwell identity and receive
the shared-field emitter Noether outputs.  Rows 64--83 import the apparatus
identity and have no new emitter term because the emitter action is
independent of rods and memories.  Rows 84--107 carry the two massive
two-forms and their cotangent partners.  The machine ledger covers every
output row exactly once.  The cubic master-equation audit, rather than row
coverage by itself, certifies the corresponding 108 zero arity-two defects.

This theorem is covariant and action-derivative exact.  It is deliberately
not called a support-local PBW coefficient replay: the repository does not
yet contain a canonical sparse 108-by-108-by-108 PBW payload or a PBW
realization of the 84-row apparatus jets.  The localized recoil profiles,
absolute-`g^3` detector coefficient, emitter `q3/q4`, solved backreacted
branch, finite-parameter Green theory, full Dirac algebra, and quantum theory
also remain open.
