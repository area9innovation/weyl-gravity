# Correlated central clock-power rail

The exact central-even Legendre evaluator now uses every certified joint clock
moment `p=0,2,...,28`.  Each clock power retains the same stable spatial
series and its own normalized external-clock expectation.

All 1,050 central-even comparisons against the fifteen published scalar
shards through `two_j=138` overlap.  The content-addressed successor covers
15,375 intervals through `two_j=2048`; every selected `two_j=256` width is
below `0.001`, and every `two_j=2048` width is below `0.1`.

This closes the clock-power axis of the central channel.  It does not cover
noncentral diagonals or odd representations and therefore cannot yet drive
the full polarization recurrence or establish an infinite spectral tail.
No Maxwell Green image, detector-response rank, recoil coefficient,
second-order-cone restriction, or physical-branch crosswalk follows from
this scalar input rail.
