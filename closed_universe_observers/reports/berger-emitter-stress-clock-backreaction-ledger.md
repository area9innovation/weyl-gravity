# Emitter stress and clock-switch backreaction ledger

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

The selected massive polarization-two-form action now has its reciprocal
metric and clock outputs.  With `H_b=dK_b`, `F=dA`, and
`s_b=<K_b,F>`, variation with respect to the invariant clock metric gives

```text
T(K_b)_mn = (1/2) H_b,mab H_b,n^ab + m_b^2 K_b,ma K_b,n^a
            - gHat_mn[(1/12)H_b^2+(m_b^2/4)K_b^2],

T(int,b)_mn = 2 g_b h_b K_b,(m|a| F_n)^a - gHat_mn g_b h_b s_b.
```

Variation of the relational switch gives the clock-phase equation source

```text
E_Theta(int) = -sum_b g_b h_b'(Theta) s_b.
```

These are not optional external forces.  They occupy the existing
metric-antifield rows 27--36 and clock-antifield row 38 of the 108-row
carrier.  Their common third-action-derivative orbits also supply the cyclic
partners in the Maxwell and emitter cotangent rows.  The emitter-added `q2`
jet therefore includes free emitter stress, interaction stress, and clock
switch exchange without adding a hidden matter store or a competing gravity
complex.

An exact left-invariant coefficient fixture uses
`K=x e0^e1+y e2^e3`, `A=a e1`, and `de1=-beta e2^e3`.  It verifies off shell

```text
dE/dt + E_A a_dot + E_x x_dot + E_y y_dot
      + E_Theta Theta_dot = 0.
```

Deleting the clock equation row leaves a nonzero defect proportional to
`g h'(Theta) Theta_dot (x a_dot+beta a y)`.  Deleting either the metric or
clock reciprocal output also breaks a representative common-action cyclic
orbit.

This is a first-interaction-jet result.  It does not replace the localized
preparations by the homogeneous fixture, evaluate the absolute-`g^3`
detector recoil coefficient, replay every component of the 108-row
`q1 q2` identity, export emitter `q3/q4`, solve the backreacted clock and
gravity equations, establish finite-parameter Green hyperbolicity or the full
Dirac algebra, or make a quantum claim.
