# Berger 108-row scalar-q1 background-ideal obstruction

The corrected component contract supplies the right noncommuting PBW
coefficient language, and the pinned 64-row scalar unary remains exact.  It
does not yet bind its abstract background generators to the six certified
global rod functions or the physical `Phi2`, nor does it declare the
differential ideal of their shifted equations.

This omission is already decisive on one rod/diffeomorphism length-two path.
Scalar naturality gives

`K_RR(Gamma_R(e1))+K_Rh(Lie_e1 gHat)=e1(Box_gHat R0_1)`.

The covariant theorem sets this to zero on the certified rod background.  In
the receiving free jet algebra its PBW normal form instead has four
independent third-jet terms.  Assigning the `[0,3,0,0]` jet value one and the
other displayed jets zero evaluates the residual to exactly one.  Thus the
free and on-shell realizations are distinct while both remain compatible with
the current interface declarations.

The result is `NO_CERTIFIED_MAP`, not a failure of the covariant unary.  The
minimal successor must content-address the specialization of all six rods and
`Phi2`, generate the shifted gravity-clock-rod background equation ideal, and
close it under the exact Berger-frame brackets.  Only then can a scalar
108-row `q1` nilpotency replay be meaningful.  Scalar `q2`, backreaction,
tangent-cone, Bridge-3, finite-parameter and quantum claims remain open.
