# Berger clock-uniform profile Sobolev N=1 bound

Status: `CERTIFIED`, with small-tail and full-image promotion still `OPEN`.

The physical-space exterior derivative and Hodge star reproduce the certified
Berger spectral identities `Delta0 x_mu=(29/18)x_mu` and
`Delta1 dR=d Delta0 R`.  Applying this operator once to
`F_a(t)=rho_a J_a dR_aI`, with the repaired `J_a=a(t)^3 y0`, gives exact
finite expressions for both detector polarizations.

Rational support bounds, `3<pi<4`, `3<sqrt(10)<4`, `e>8/3`, and the validated
radial normalization integral give clock-uniform `L2` enclosures.  After the
certified first-omitted `two_j=1025` spectral factor, the resulting tail upper
bounds are approximately `4.98e4` for the axial record and `5.05e4` for the
transverse record.  The nonnegative unit clock bump and contractive exact-time
Maxwell multipliers preserve these bounds after clock integration.

These are rigorous but coarse triangle/Darboux bounds.  They do not certify a
small tail, fill the incomplete retained-mode projection, construct a full
Maxwell or massive-two-form image, evaluate detector response or recoil, or
activate the same-background physical-branch bridge.  The next analytic step
is a correlated squared-norm quadrature or a wider complete retained rail.
