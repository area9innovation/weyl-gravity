# Complete Berger 108-row q2 PBW payload

This artifact normalizes and zero-extends the nine certified base,
apparatus and emitter `q2` tensors into one source-labelled 108-row payload.
The base is the additive composition of the gravity-clock payload and its
separate typed Maxwell overlay required by the certified mixed `q3`; the
overlay is not a materialized replacement for gravity.  The typed presentation
has the same lowered cubic action as the legacy output-normalized tensor, but
the two are not interchangeable in coderivation composition.
The action-derived dressed-rod clock correction transports the full raw
temporal scalar orbit through the background-dependent rod clock dressing.
The resulting 171,759 operator keys contain 186,935 exact coefficient
monomials.  There are 160 explicitly retained additive overlaps between the
gravity-clock and rod-metric sources; source labels prevent overwrite and make
their summation auditable.  Deletion of any source is detected.

This closes typed scalar `q2` assembly only.  Complete `q3` is exported by a
separate certificate.  The rod-clock arity orbit is repaired, but a distinct
typed Maxwell--emitter orbit keeps arity two obstructed.  The arity-three replay, `K_Berger`
equivariance, observer-morphism stability and the detector restriction to the
second-order cone remain fail-closed.
