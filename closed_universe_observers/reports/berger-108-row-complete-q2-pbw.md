# Complete Berger 108-row q2 PBW payload

This artifact normalizes and zero-extends the seven certified base,
apparatus and emitter `q2` tensors into one source-labelled 108-row payload.
The base is the typed gravity-clock-Maxwell coderivation presentation required
by its certified mixed `q3`; it has the same lowered cubic action as the legacy
output-normalized tensor, but the two are not interchangeable in coderivation
composition.
The resulting 21,422 operator keys are cross-source disjoint and contain
36,438 exact coefficient monomials; deletion of any source is detected.

This closes typed scalar `q2` assembly only.  Complete `q3` is exported by a
separate certificate.  The two component arity replays, `K_Berger`
equivariance, observer-morphism stability and the detector restriction to the
second-order cone remain fail-closed.
