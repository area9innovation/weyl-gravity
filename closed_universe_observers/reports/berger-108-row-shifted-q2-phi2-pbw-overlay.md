# Berger 108-row shifted q2(Phi2,-) PBW overlay

Status: `CERTIFIED_SCALAR_SHIFTED_Q2_PHI2_UNARY_OVERLAY`.

The shifted base unary block is now a scalar PBW payload rather than a
covariant formula reference.  The generator zero-extends the pinned 54-row
gravity `q2` tensor, adds the pinned 64-row Maxwell overlay, and fixes the
first ordered metric-input slot with the ten certified `Phi2` components.
The contracted metric derivative becomes the ordered coefficient jet of
`Phi2`; the other argument remains the unary input.

The source payload already stores the complete symmetric ordered-slot map.
Adding the second slot would compute `q2(Phi2,-)+q2(-,Phi2)` and double the
unary; that historical mutation is now rejected explicitly.

The exact contraction starts from 92,965 first-slot placements and combines
them into 92,965 nonzero canonical terms on 310 matrix positions.  Its compact
payload retains the source suspended-graded-symmetric factorial convention.
The independent verifier reconstructs every term directly from both source
payloads and re-evaluates the physical fourth-order coefficient `623/324`.

This closes only the shifted base portion of the rod/gravity first jet.  The
six rod gauge/wave blocks, local gravity--rod Hessian, full 108-row
composition, nilpotency/cyclicity replay, tangent cone, Bridge 3, finite-r
causal and quantum gates remain open.
