# Berger adaptive diagonal-fraction scale rail

Status: `CERTIFIED` for six declared scalar (p=0) rows at `two_j=1024,1025`; complete scalar/form rails and the infinite tail remain `OPEN`.

The seed fractions are widened from `two_j=512,513` to the adjacent even/odd pair `two_j=1024,1025`. The (1/8) and (1/4) basis-index fractions remain below width `0.1` on a `64 x 64` directed Darboux grid. The (3/8) row does not: at `two_j=1024,r=384`, `64 x 64` has width about `0.12083`.

The adaptive audit separates the quadrature axes. Refining only angle (`64 x 128`) still gives width about `0.10952` and is rejected. Refining only radius (`128 x 64`) gives width about `0.07208`; the same declared refinement is applied to the adjacent odd companion. Thus the first scale loss is localized to the radial enclosure at this sentinel without paying for a full `128 x 128` grid.

This is a selected two-scale scalar rail, not a complete diagonal, representation, external-clock-power or polarized form stream. It does not establish a Sobolev/infinite-mode tail, Green image, detector response, recoil coefficient, second-order-cone restriction or active Bridge 3. The mixed (epsilon_R^2kappa) unary dependency remains coefficientwise satisfied before apparatus (q_2,q_3) and the affine-(K_{m Berger}) observer morphism; finite-(r) and all-orders stability remain open.
