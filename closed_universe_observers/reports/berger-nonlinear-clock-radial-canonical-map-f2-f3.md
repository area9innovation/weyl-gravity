# Berger nonlinear clock radial canonical map through F3

The radial part of the missing nonlinear clock chart is fixed by geometry. In the linearly dressed carrier the raw conformal metric is `g=eta+H-2 R eta`, while the invariant physical metric is `gHat=(1+R)^2 g`. Therefore

```text
H_true = H + 2 R H - 3 R^2 eta + R^2 H - 2 R^3 eta.
```

The quadratic truncation exactly reproduces the map used in the completed unary certificate. Polarization in the factorial convention produces 38 `F2` and 38 `F3` component entries across metric, radial and cotangent rows.

The BV cotangent lift is not optional:

```text
H_true_star = (1+R)^(-2) H_linear_star
R_true_star = R_linear_star - 2(H-3 R eta).H_linear_star/(1+R).
```

These formulas preserve the canonical one-form coefficientwise. Deleting the radial-cotangent term is detected, as is deleting the cubic `-2 R^3 eta` metric term.

This closes only the radial subgate. The temporal relational-time retraction and its cotangent lift remain open, so complete scalar apparatus `q2/q3` transport and every downstream observer activation remain fail-closed.
