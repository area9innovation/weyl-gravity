# Adaptive external-clock scalar streams through `p=28`

Nine typed shards for even `p=12,...,28` extend the existing six temporal
rails without changing their content-addressed certificates.  Each shard
reconstructs 9,870 diagonal coefficients through `two_j=139` from 4,970
validated intervals and retains the exact external detector clock factor.
Polarization and the order-14 charge-block Green calculation remain open.
