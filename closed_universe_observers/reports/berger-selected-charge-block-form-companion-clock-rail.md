# Berger selected charge-block form companion clock rail

Status: `CERTIFIED` for all 33 on-support real-form companions and all 18 selected Maxwell charge-block input vectors through `p=28`; temporal Green application remains `OPEN`.

The complete 18-row scalar input union is combined with exact detector prefactors and Clebsch–Gordan recurrence coefficients. This produces 33 real-form `p=0` companions using 84 scalar-term applications. Each row is then propagated through all 15 normalized even clock moments, for 495 complex intervals. Every real/imaginary width remains below `0.1`, and no clock/profile independence assumption is used.

Together with the original 18 selected entries and 27 exact structural zeros, these companions close 18 distinct `q=m+s` charge-block inputs. The certificate serializes 270 complete clock-power helicity vectors. It does not yet apply the exact temporal functional calculus, certify a spatial tail, construct full Green images, evaluate response or recoil, or activate Bridge 3.
