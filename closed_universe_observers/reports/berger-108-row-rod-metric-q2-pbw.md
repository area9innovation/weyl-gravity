# Berger 108-row rod--metric q2 PBW block

The invariant six-rod action has three cubic fluctuation orbits:

```text
(metric, rod, rod), (metric, metric, rod), (metric, metric, metric).
```

They are the first three Fréchet derivatives of the densitized inverse metric in `-epsilon_R_squared sqrt(-gHat) gHat^{-1}(dR,dR)/2`. The generated payload raises the action derivative with the canonical signed pairing and exports every metric-cotangent and rod-cotangent output in the noncommuting Berger PBW basis.

This is one exact `q2` subblock. Memory transport, normalized readout, emitter sectors, all `q3`, arity replay and observer-cone response remain open.
