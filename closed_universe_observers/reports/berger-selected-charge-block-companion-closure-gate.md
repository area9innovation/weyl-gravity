# Berger selected charge-block companion closure gate

Status: `OBSTRUCTED` for direct temporal functional-calculus promotion of the 18 selected form entries; the exact companion completion is finite and `OPEN`.

The 18 selected real form-`two_j=1024` entries decompose into 18 distinct helicity blocks for the conserved Maxwell charge `q=m+s`. Their real-entry union contains:

- 18 already selected entries;
- 27 entries that vanish by the exact detector support rules; and
- 33 additional on-support entries that cannot be set to zero.

The 33 on-support companions depend on 18 scalar recurrence rows. Twelve are already in `BERGER_POLARIZATION_RECURRENCE_SCALAR_CLOSURE`; the precise missing set is `(1023;129,257,385)` and `(1025;130,258,386)`. Treating every unselected companion as a structural zero is rejected.

This is a closure ledger, not a Green image. The next gate is to evaluate the six missing scalar rows, construct the 33 real-form companions for all even clock powers through `p=28`, and only then apply the exact temporal charge-block functional calculus. Spatial-tail, response, recoil, cone, Bridge 3 and quantum claims remain open.
