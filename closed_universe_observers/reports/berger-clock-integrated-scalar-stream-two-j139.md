# Clock-integrated Berger scalar stream through `two_j=139`

Dependency tags: `LOCAL-ALGEBRAIC`, `LORENTZIAN-CAUSAL`.

The specialized diagonal symmetric-power formula replaces construction and
expansion of the full representation matrix.  Isotropic angular reduction
then expresses every term using the validated radial and clock-secant moments
through `k=50`.  A directed 256-bit internal fixed-point calculation is
rounded outward to 160-bit rational intervals, and every truncated
`y0=sqrt(1-|y|^2)` series carries a uniform Taylor remainder.

All 140 scalar modes from `two_j=0` through `139` are evaluated.  Reflection
of the diagonal index reconstructs 9,870 values from 4,970 serialized unique
intervals.  The specialized stream overlaps every previously certified
`two_j<=4` interval.  At the top rail the intervals remain nontrivial and
narrow; this is coefficient evaluation rather than a capacity estimate.

The scalar stream closes the value input required by the pointwise detector
polarization recurrence through form `two_j=138`.  Applying that recurrence
inside the temporal Green chain, bounding the infinite tail beyond the
cutoff, composing full Maxwell and massive images, and evaluating recoil are
the next gates.  Bridge 3 and the second-order observer restriction remain
inactive/open respectively.
