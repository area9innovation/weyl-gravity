# Berger recoil-scalar stream activation gate

Dependency tags: `LOCAL-ALGEBRAIC`, `LORENTZIAN-CAUSAL`.

The symbolic word and internal executable stream are now ready. The generic
reality-folded adapter composes the certified direct-shell carrier, causal
feedback evaluator, exact SU(2) partner map, four detector/source aggregates,
and tail-aware stop gate for every nonempty contiguous declared shell
sequence. An actual carrier-building `two_j=6` replay reproduces the prior 32
direct plus 24 reality-derived blocks exactly.

Activation still fails closed at the external boundary. The exact numerical
input schema contains no declared physical masses, nonzero couplings, Berger
inverse-volume enclosure, shell schedule, tail schedule, or stopping goal.
Thus `INTERNAL_MODEWISE_RECOIL_STREAM_READY=true`, while
`FOUR_RECOIL_SCALAR_STREAM_ACTIVE=false` and no physical recoil interval or
recoil-corrected rank is exported.

The next gate is to audit and activate the deferred exact input contract.
Apparatus quotient descent, restriction to the second-order tangent cone,
same-background Bridge 3 activation, finite-parameter observer stability and
quantum claims remain open.
