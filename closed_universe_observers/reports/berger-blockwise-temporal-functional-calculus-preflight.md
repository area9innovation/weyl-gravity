# Berger blockwise temporal functional-calculus preflight

The global order-14 Taylor rail failed because it expanded the full kernel
time `tau=T+s/48`, whose D1 phase reaches about `sqrt(945)`.  The replacement
uses the exact angle-addition identities inside each finite Hermitian Maxwell
charge block.  The large `T` dependence stays in exact spectral functions
`cos(T sqrt(B))` and `sin(T sqrt(B))/sqrt(B)`; only the normalized internal
clock phase `s sqrt(B)/48`, with `|s|<=1`, is Taylor expanded.

At `two_j=138`, all resulting geometric ratios are below `1/100`.  The
order-14 `Delta1` cosine microphase remainder is below `1.64e-18`; the scalar
cosine and sine-over-root remainders are smaller.  The detector integrand is
even in `s`, so odd transforms vanish and the existing fifteen even rails
through `p=28` are sufficient.

This certifies the route and its operator error budget.  It does not yet
stream the microphase-dressed block inputs, export the exact-`T` temporal
image, close the spatial tail, compose the massive image, evaluate recoil,
restrict records to the second-order cone, activate Bridge 3, or make a
quantum claim.
