# Berger direct-shell and tail-stop gate

Dependency tags: `LOCAL-ALGEBRAIC`, `LORENTZIAN-CAUSAL`.

The direct recoil carrier is no longer extended by a shell-specific adapter.
A content-addressed callable now applies the certified generic profile-moment,
Peter--Weyl de Rham and exact sine-kernel engines at any declared finite
`two_j`.  The carrier validator accepts an extension only when both detector
rows, the D1-on-h0 remainder and all five Maxwell/massive blocks are present
contiguously.  A `two_j=6` sentinel exercises both coefficient and interval
kernel consumers.  It remains explicitly unrelated to the hashed exact-`T`
rail (`NO_CERTIFIED_MAP`).

The same gate exports the four-stream tail formula

`rho_ab(N)=|g_b| D_a E_b(N) sum_c |g_c|^2 C_c(m_c)`

and fail-closed tolerance, nonzero, sign and determinant stop rules.  The
certificate-derived validation fixture remains `OPEN`; a synthetic exact
fixture exercises the certified stop branch.  This is a controller theorem,
not a physical recoil result: `two_j=6` feedback blocks, the complex-to-real
shell map, physical masses and couplings, four recoil intervals and the
recoil-corrected rank remain open.
