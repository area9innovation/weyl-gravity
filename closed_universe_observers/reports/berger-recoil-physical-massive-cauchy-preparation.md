# Berger recoil physical massive Cauchy preparation

The certificate derives cosine-kernel enclosures from the corrected odd-power
sine payload and evaluates the diagonal image and its first time derivative.
At the compact support-left boundary, flatness gives zero source derivatives,
so the wave equation supplies `F_tt=-A F` and `F_ttt=-A F_t` without assuming
derivative control from a value-only remainder.

Exact Berger spacetime blocks then apply
`I+mu^-2 Dhat_1 Deltahat_2` to both the value and time derivative.  The result
is a finite physical two-form Cauchy pair for each detector mode and passive
column through `two_j=4`.  The positive-energy dual, full spatial tail and
recoil contractions remain open.
