# Berger selected charge-block scalar companion completion

Status: `CERTIFIED` for the exact six scalar rows missing from the selected charge-block companion closure; form companions and Green images remain `OPEN`.

The correlated Darboux evaluator now encloses shell `1023` indices `129,257,385` and shell `1025` indices `130,258,386`. Rows near indices `385,386` use `128` radial by `64` angular cells; the other four use `64 x 64`. Every width is below `0.1`, with the maximum below `0.099`.

Together with the 12 rows already present in `BERGER_POLARIZATION_RECURRENCE_SCALAR_CLOSURE`, this supplies all 18 scalar recurrence inputs needed to build the 33 on-support real-form companions. It does not build those form intervals, propagate their clock powers, close the selected charge blocks, apply Green kernels, evaluate response or recoil, or activate Bridge 3.
