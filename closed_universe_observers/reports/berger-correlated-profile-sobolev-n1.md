# Correlated Berger profile Sobolev N=1 bound

Status: `CERTIFIED`; small-tail and full-image promotion remain `OPEN`.

The calculation retains the full squared coframe components of
`Delta1(rho J dR_I)`.  Exact parity removes angularly odd monomials, and the
standard even moments on `S2` reduce each detector polarization to 21 radial
interval terms.  A 4096-cell directed Darboux enclosure keeps the shared rod
amplitude and repaired `y0` Gram factor correlated; only the final cell
touching the flat boundary uses global bounds on `B`, `B'`, and `B''`.

Both clock-uniform `Delta1` norms are below approximately `5.11e8`.  After the
certified first-omitted spectral factor above `two_j=1024`, both Maxwell tail
upper bounds are below approximately `1.95e3`.  This strictly improves the
previous `4.98e4`/`5.05e4` triangle bounds, but is still not a small-tail
certificate.

An upper bound larger than one does not obstruct the true tail.  The result
therefore leaves the complete retained projection, full Maxwell and massive
images, detector response and rank, recoil, and tangent-cone restriction
open.  The next route is a direct correlated Green-tail estimator or a wider
complete retained harmonic rail.
