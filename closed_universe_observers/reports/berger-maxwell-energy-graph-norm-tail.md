# Berger Maxwell energy/graph-norm tail

The exact recoil switch commutator needs `dA`, not only the Maxwell-potential
`L2` tail.  Two boundary-flat clock integrations by parts and the certified
Berger spectral lower bound give, for each of the three field-strength pieces,

`2304 S_a Lambda_N^(-3/2)`.

The component-sum bound is three times this value.  It is about `3.01e5` above
retained `two_j=1024` and first falls below one for both profiles at retained
`two_j=68743`.  A dense rail there would require 14,177,143,864 supported
entries and is not selected.  The response-specific shell stream remains the
route to a scalar recoil interval.

The Maxwell graph-norm input is now certified.  The finite-time massive
energy constant and numerical recoil coefficient remain open.
