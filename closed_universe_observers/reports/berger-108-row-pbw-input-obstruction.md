# Berger 108-row PBW input obstruction

## Result

The covariant 108-row `q1 q2` identity remains certified, but its current
dependency closure does not determine a canonical scalar support-local PBW
payload.  The correct atlas state is `NO_CERTIFIED_MAP`, not `CERTIFIED`.

The pinned 64-row gravity-clock-Maxwell payload is a genuine sparse scalar PBW
tensor over `Q(sqrt(10))`.  The apparatus successor instead exports its rod,
memory, readout and scalar-BV interactions as covariant Fréchet-derivative
formula strings.  It exports neither scalar component rows nor a differential
coefficient grammar for the nonconstant detector profiles.  Likewise, the
108-row unary artifact lists covariant source/target block ranges but not a
complete scalar 108-by-108 PBW matrix.  Those objects are insufficient for an
independent coefficientwise replay.

## Exact non-uniqueness witnesses

A normalized three-coordinate radial bump of width `epsilon` has centre value
proportional to `epsilon^-3`.  Widths `epsilon` and `epsilon/2` both satisfy the
frozen smooth, compact, nonnegative, unit-integral apparatus profile contract,
but their centre coefficients differ by exactly eight.  The readout `q2`
coefficient therefore differs.

Similarly, a unit-integral one-dimensional flat switch of radius `r` has centre
value proportional to `r^-1`.  Radii `r` and `r/2` both satisfy the frozen
emitter handoff, but their centre interaction coefficients differ by exactly
two.  Identifying the two widths or the two radii collapses the corresponding
witness; both mutations are tested.

Later exact detector-smearing and emitter-switch certificates exist.  They are
not dependencies of the covariant `q1 q2` theorem and cannot be imported
retroactively.  A successor may import them, but must still define a canonical
108-row component/cotangent crosswalk, a closed differential coefficient-jet
normal form, a complete scalar PBW `q1`, and an action-derived apparatus/emitter
`q2` expander including every cyclic reciprocal output.

## Boundary

No component coefficient is inferred from output-row coverage or prose action
formulas.  This result does not export the 108-row PBW payload, certify a
component replay, choose physical masses or couplings, solve a backreacted
branch, extend to `q3/q4`, activate the tangent-cone or physical-branch bridge,
or make a quantum claim.
