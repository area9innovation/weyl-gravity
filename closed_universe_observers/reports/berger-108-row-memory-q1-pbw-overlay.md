# Berger 108-row memory q1 PBW overlay

Status: `CERTIFIED_SCALAR_MEMORY_Q1_FIRST_JET_OVERLAY`.

The two memory pairs are now scalarized through the complete certified
coefficientwise first jet
`Q00+epsilon_R_squared Q10+kappa Q01+epsilon_R_squared*kappa Q11`.
The payload contains clock transport and its frozen-density adjoint, both
normalized detector profile maps and their adjoints, the `Phi2` transport
variation, and the normalized metric-profile variation.  Every derivative of
`f_a rho_a J_a`, the selected rod, and `Phi2` created by formal adjunction is
explicit in the noncommuting Berger-frame coefficient grammar.

The 618 serialized terms are stored in a compact schema-validated payload.
The claim certificate records the exact 16-block/20-position support,
canonical term digest and payload byte digest, while the independent verifier
loads that payload and reconstructs every formal-transpose and sign check.

This is the complete memory contribution, not the complete apparatus unary.
The six rod gauge/wave sectors, gravity--rod Hessian, shifted
`q2(Phi2,-)` block, composed 108-row replay, scalar `q2`, cone, Bridge 3,
causal and quantum gates remain open.
