# Berger selected clock-power polarized form rail

Status: `CERTIFIED` for all even external powers `p=0,2,...,28` on 18 selected form-`two_j=1024` detector entries; complete form and infinite rails remain `OPEN`.

The corrected `p=0` source intervals already include the common pointwise factor `a(t)` and uniformly enclose the full normalized clock support. For every nonnegative even power, multiplying that uniform interval by the certified positive normalized moment of `s^p` encloses the joint clock/spatial integral. No independence assumption is used.

All 270 complex intervals are serialized and content-addressed. The `p=0` rows reproduce their source exactly, and every maximum real/imaginary width remains below `0.1`. Dropping the `p=28` layer is rejected by exact power coverage.

This remains a selected rail at one form representation and three anchors. It does not supply complete form coverage, a Sobolev/infinite-mode spatial tail, temporal or spatial Green images, detector response, recoil, tangent-cone restriction or active Bridge 3. The coefficientwise mixed `epsilon_R^2 kappa` apparatus sequencing remains unchanged.
