# Correlated extreme-axial oscillatory evaluator

The extreme diagonal `r=0` is integrated as the bounded oscillatory factor

```text
(1-X)^(two_j/2)
cos(two_j atan2(sqrt(Z),sqrt(1-X-Z)))
```

rather than expanded into independent moments.  A directed-rounding tensor
Darboux enclosure uses the exact radial bump, isotropic angular measure and a
single enclosure of the full normalized clock support.

The `32 x 32` low audit overlaps all published `r=0` rows through
`two_j=4`.  On the `256 x 256` rail, the selected `two_j=975` and
`two_j=2047` intervals are both narrower than `0.1`.  Halving each axis at
`two_j=2047` leaves width above `0.1`, so the resolution mutation is rejected.

This is a selected extreme-axial `p=0` seed.  The complete axial rail,
intermediate Jacobi diagonals, other clock powers, polarized tail, Green
images, detector response, recoil and second-order-cone restriction remain
open.
