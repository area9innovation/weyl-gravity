# Berger recoil stream executable-readiness audit

Dependency tags: `LOCAL-ALGEBRAIC`, `LORENTZIAN-CAUSAL`.

The complete per-shell symbolic word now has a first executable consumer.
Exact rational interval aggregation of supplied `I_abc[two_j,k]` values is
certified, including every passive column, `g_b g_c^2`, and the Peter--Weyl
weight.  The detector coefficient intervals, nested advanced/retarded time
convolutions, and tail-aware four-stream stop loop remain absent.  Supplying
numerical masses and couplings now would therefore still not produce a
physical recoil interval.

The audit adopts an explicit four-callable backend protocol and records the
shell evaluator `CERTIFIED` while the other three rows remain `OBSTRUCTED`.
It also exports a strict exact-rational numerical
input schema in the certified `gHat` operator units, but leaves every value
undeclared.  The next gate is a validated callable finite-shell interval
detector-coefficient and nested-convolution backends; external specialization
follows only after the full protocol closes.

The symbolic operator theorem remains `CERTIFIED`.  No recoil coefficient,
second-order-cone restriction, physical-branch bridge, finite-parameter
observer theorem, or quantum claim follows from this audit.
