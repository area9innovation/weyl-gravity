# Berger recoil stream executable-readiness audit

Dependency tags: `LOCAL-ALGEBRAIC`, `LORENTZIAN-CAUSAL`.

The internal interval stream is now executable. The certified stack includes
the complete symbolic recoil word, finite causal convolution and mode-kernel
engines, physical massive preparation, conserved retarded Maxwell channel,
all finite feedback channels through `two_j=5`, the complete reality-folded
`two_j=6` shell, a content-addressed direct-shell provider, exact
complex-to-real shell extraction, four-entry aggregation, and the tail-aware
stopping rules.

`BERGER_RECOIL_REALITY_FOLDED_SHELL_STREAM_ADAPTER` closes the last internal
composition gap. For every caller-declared contiguous shell it advances all
three direct carriers, evaluates only the independent SU(2) columns, derives
their partners exactly, aggregates all four detector/source entries, and
applies the stop rule before advancing. Its actual `two_j=6` replay exactly
matches the prior 56-block certificate.

The strict v2 numerical schema and callable translator are now certified for
every declaration-controlled runtime argument and all four stopping goals.
The incompatible v1 schema is retained as
`OBSTRUCTED_SCHEMA_RUNTIME_MISMATCH`. The validation replay remains explicitly
nonphysical, so the four physical streams are not active and no physical
recoil coefficient, sign, recoil-corrected determinant, second-order-cone
restriction, same-background Bridge 3 map, finite-parameter observer theorem,
or quantum claim follows. The next gate awaits a provenance-complete
`EXPLICIT_EXTERNAL_VALUES` declaration; no values are inferred here.
