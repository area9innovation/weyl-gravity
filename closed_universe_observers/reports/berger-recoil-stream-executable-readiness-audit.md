# Berger recoil stream executable-readiness audit

Dependency tags: `LOCAL-ALGEBRAIC`, `LORENTZIAN-CAUSAL`.

The complete per-shell certificate is a typed symbolic operator word, not an
interval evaluator.  The current package has no callable implementation for
the detector coefficient intervals, nested advanced/retarded time
convolutions, one-shell recoil interval, or the tail-aware four-stream stop
loop.  Supplying numerical masses and couplings now would therefore not
produce a recoil interval.

The audit adopts an explicit four-callable backend protocol and records all
four rows as `OBSTRUCTED`.  It also exports a strict exact-rational numerical
input schema in the certified `gHat` operator units, but leaves every value
undeclared.  The next gate is a validated callable finite-shell interval
backend; external specialization follows only after that gate closes.

The symbolic operator theorem remains `CERTIFIED`.  No recoil coefficient,
second-order-cone restriction, physical-branch bridge, finite-parameter
observer theorem, or quantum claim follows from this audit.
