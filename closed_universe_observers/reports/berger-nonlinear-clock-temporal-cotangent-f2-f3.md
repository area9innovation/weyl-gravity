# Berger nonlinear clock temporal BV cotangent lift through F3

Let the certified temporal field map have homogeneous corrections `C2+C3`. Its differential Jacobian is

```text
DF = I + A1 + A2,  A1=D C2,  A2=D C3.
```

The cotangent equation is differential, so transposition alone is wrong. Coefficientwise integration by parts in the noncommuting Berger PBW algebra gives `B1=A1^dagger` and `B2=A2^dagger`. Solving the canonical one-form identity yields

```text
P = p - B1 p + (B1^2-B2) p + O(4).
```

Both formal adjoints are involutive, and the complete degree-two and degree-three inverse defects vanish. The generated payload contains 93 `F2` and 135 `F3` cotangent entries. Nonzero `sqrt(10)` coefficients retain the Berger spatial commutators. Pointwise transposition, omission of `B1^2`, and deletion of the nonholonomic terms are all detected.

This closes the temporal canonical submap only. Mixed radial-temporal terms must still be generated by composing the two canonical charts before scalar apparatus interactions can be transported.
