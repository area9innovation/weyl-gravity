# Berger correlated diagonal-fraction stream

Status: `CERTIFIED` for the declared scalar (p=0) fraction rows; the complete scalar/form rail and infinite tail remain `OPEN`.

The correlated Jacobi evaluator was run at `two_j=512` for basis-index fractions (r/512=1/8,1/4,3/8), together with the same indices in the adjacent odd representation `two_j=513`. All six `64 x 64` directed Darboux enclosures retain the terminating Jacobi factor, axial amplitude/phase and normalized clock envelope inside one interval integrand, and all six widths are below `0.1`.

This is deliberately not called a complete diagonal stream. It samples three declared fractions in one even representation and their adjacent odd companions; it does not cover every diagonal, all odd representations, or clock powers above (p=0).

The certificate also records the evaluated-Sobolev alternative fail-closed. The scalar eigenvalue formula, left-invariant frame, fixed rod chart and bump moments are available. The pushed-forward detector density relative to Berger Haar volume, a clock-uniform repeated-Laplacian norm, the polarized form/coderivative Sobolev norm, and the Green-weighted omitted-shell conversion are not certified inputs. Consequently no Sobolev or infinite-mode tail is promoted.

The mixed (epsilon_R^2kappa) unary dependency remains coefficientwise satisfied before the already-certified apparatus (q_2,q_3) and affine-(K_{m Berger}) observer morphism. Nothing here promotes finite-(r), fixed-background, full-(q_4), or all-orders observer-morphism stability.
