# High clock-power moment rail through `p=28`

Dependency tags: `LOCAL-ALGEBRAIC`, `LORENTZIAN-CAUSAL`.

The fixed normalized one-dimensional flat clock bump now has validated even
moments through `p=28` on the same 32768-cell exact-unimodality Darboux rail.
All foundational `p<=12` intervals overlap.  These are inputs for the
adaptive order-14 Green calculation, not joint secant moments, profile
coefficients, Green images, tail bounds or recoil.
