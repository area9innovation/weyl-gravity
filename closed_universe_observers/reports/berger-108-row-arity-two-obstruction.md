# Complete Berger 108-row arity-two obstruction

The exact differential-coefficient PBW replay stops at its lowest bidegree.
The typed 64-row gravity-clock-Maxwell control has zero defect, but the
the action-derived dressed-rod clock correction removes the former rod
witness and all six rod/rod-cotangent defect rows.  The physical-emitter
action exporter now also restores the missing multiplicity-two Hessian entry
when its two remaining slots coincide.  The remaining formal extension has
3,984 keys and 4,272 monomials.  Applying the exact switch chain rule
`e0 Theta_bar=3/4`, `ei Theta_bar=0` leaves 2,772 keys and 2,820 monomials on
24 output rows.

The first remaining lexicographic witness has output `c_spatial_1_star`,
inputs `e1 A_0` and `e1 K0_01`, and coefficient `-3 g0 h0`.  Two-sided
source isolation assigns it to the shared typed Maxwell--emitter orbit: the
typed Maxwell and physical emitter `q2` terms cross the base and emitter
unary sources with a nonzero exact remainder.

The fail-closed repair gate is to rederive that shared orbit from the common
BV action in the typed suspended graded-symmetric factorial convention and
replay all first-bidegree coefficients.  Arity three,
`K_Berger`, observer-morphism stability, `O_detector|Z2`, nonlinear rank and
physical Bridge 3 are not authorized.
