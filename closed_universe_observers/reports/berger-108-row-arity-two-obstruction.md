# Complete Berger 108-row arity-two obstruction

The exact differential-coefficient PBW replay stops at its lowest bidegree.
The typed 64-row gravity-clock-Maxwell control has zero defect, but the
complete 108-row extension has 4,408 nonzero operator keys and 4,732 exact
coefficient monomials on 30 output rows.

The first lexicographic witness has output `c_spatial_1_star`, inputs `tau`
and `R0_1_plus`, no input derivatives, and coefficient `+e0 e1 R0_1`.  Its
time modes remain nonzero after the certified Berger background differential
quotient.  Two-sided source isolation assigns the binary side to gravity `q2`
and the unary side to the local-rod overlay.  The calculation does not infer
that this is the only missing mixed orbit.

The fail-closed repair gate is to derive the background-compatible mixed
gravity-rod binary/chart contribution—or revise the unary/binary crosswalk
from the common action—and replay all first-bidegree coefficients.  Arity three,
`K_Berger`, observer-morphism stability, `O_detector|Z2`, nonlinear rank and
physical Bridge 3 are not authorized.
