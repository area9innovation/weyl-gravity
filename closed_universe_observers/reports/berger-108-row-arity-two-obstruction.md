# Complete Berger 108-row arity-two obstruction

The exact differential-coefficient PBW replay stops at its lowest bidegree.
The typed 64-row gravity-clock-Maxwell control has zero defect, but the
complete 108-row extension has 4,768 nonzero operator keys and 5,128 exact
coefficient monomials on 30 output rows.

The first lexicographic witness has output `c_spatial_1_star`, inputs
`c_spatial_1` and `R0_1_plus`, no input derivatives, and coefficient
`-e1^2 R0_1`.  Its two time modes remain nonzero after the certified Berger
background differential quotient.  Source isolation attributes this witness
to the apparatus scalar-BV block.  The calculation does not infer that this
is the only missing orbit.

The fail-closed repair gate is to derive the background-compatible apparatus
BV/chart contribution—or revise the unary/binary crosswalk—from the common
action and replay all first-bidegree coefficients.  Arity three,
`K_Berger`, observer-morphism stability, `O_detector|Z2`, nonlinear rank and
physical Bridge 3 are not authorized.
