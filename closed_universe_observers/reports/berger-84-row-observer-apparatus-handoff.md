# Berger 84-row observer-apparatus handoff

## Forward interface

This certificate resolves the incompatible prospective apparatus layouts.
The historical 78-row gate remains an exact obstruction result for its
three-shared-rod input, but every new construction must use the ordered
84-row carrier:

```text
64 certified gravity-clock-Maxwell rows
+ 6 detector-indexed rods
+ 2 memories and 2 multipliers
+ 10 paired antifield-density rows
= 84 rows
```

The new rows use the canonical field/antifield sign convention and change the
degree ranks from `[6,26,26,6]` to `[6,36,36,6]`.

The complete base 64-row gravity-clock-Maxwell cyclic causal
`K_Berger`-Cartan contraction is imported through arity three.  The retained
observer descent gate independently confirms that the probe record cannot be
typed as a retained `K`-cocycle until precisely these apparatus rows and their
cyclic partners are adjoined.

## Frozen modeling choices

The memories and multipliers are four-dimensional bulk scalar fields, not
worldline defect variables.  Their transport is the normalized clock vector
`T=n_Theta^a nabla_a`, their pairing uses `dvol_gHat`, and their retarded
inverse is a clock-line Volterra integral on the declared support domain.

Detector `D0` depends only on `R0_1,R0_2,R0_3` and uses
`dTheta wedge dR0_1`.  Detector `D1` depends only on
`R1_1,R1_2,R1_3` and uses `dTheta wedge dR1_2`.  Cross-detector rod leakage is
forbidden.

The rod action carries `epsilon_R^2`; the readout carries the independent
coupling `kappa`.  The profile operator is frozen only through its exact
two-jet, which is sufficient for `q1,q2,q3`; the cubic remainder and `q4+`
remain open.

## Physical background and boundary

The certificate combines the normalized phase-basis primitives into the
actual real two-detector `Phi2`, including both Hopf phases and time shifts.
Both independent Fourier-block residuals vanish exactly.

The emitter currents remain external q-closed inputs.  Consequently the
84-row gate targets receiver rod/memory backreaction but must not be called a
full recoil theorem.  Dynamical emitters and spatially localized emitter
worldtubes require a separate carrier extension.

The next gate is the shifted-background 84-row unary, pairing, and Green
complex.  Nilpotency, cyclicity, retarded propagation, higher operations,
the observer morphism, and deformed rank two all remain open.
