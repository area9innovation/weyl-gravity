# Berger reality-folded shell-stream adapter

The reusable direct-shell pipeline is now exported. For each nonempty,
contiguous declared shell sequence it builds and binds the detector,
cross-window and exact-kernel carriers; evaluates only
`k=0,...,floor(two_j/2)`; derives the remaining passive columns by the
certified SU(2) reality relation; aggregates all four detector/source shell
intervals; and applies the tail-aware stopping rule after every shell.

The certificate performs an actual carrier-building replay at `two_j=6`.
The direct numerical inputs are the 32 representative blocks already
certified by `BERGER_RECOIL_TWO_J6_REALITY_FOLDED_BINDING`; the generic
adapter reproduces all 56 direct/reality-derived blocks exactly. Independent
verification reconstructs the partner rectangles and all four aggregate
intervals from rational endpoints. Missing or duplicate shells, incomplete
representatives, absent tail data and altered reality partners fail closed.

The aggregate replay uses unit couplings and a unit inverse-volume interval
only to certify interface composition. It is not a physical Berger-volume or
coupling specialization. Correct tail radii are recomputed after retained
`two_j=6`; the validation rank-two stop remains `OPEN`. Physical masses,
couplings and a stopping goal remain deferred, as do nonzero recoil,
recoil-corrected rank, apparatus quotient descent, second-order-cone
restriction, Bridge 3, nonlinear observer stability and quantum claims.
