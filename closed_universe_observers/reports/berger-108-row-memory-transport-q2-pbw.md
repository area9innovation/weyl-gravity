# Berger 108-row memory-transport q2 PBW block

The profile-free memory action is `sum_a integral p_a V_gTheta^mu e_mu m_a`. Its cubic vertex uses the exact first variation

```text
delta V0 = (2/3) tr_eta(h) - (16/9) e0(theta),
delta Vi = -(4/3) h0i - (16/9) ei(theta).
```

The payload contains both channels and all p-, m-, metric- and Theta-cotangent cyclic outputs in the noncommuting Berger PBW basis. Normalized readout, emitter q2, every q3 and downstream observer-cone claims remain open.
