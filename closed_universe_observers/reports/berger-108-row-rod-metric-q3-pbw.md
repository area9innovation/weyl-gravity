# Berger 108-row rod-metric q3 PBW certificate

`BERGER_108_ROW_ROD_METRIC_Q3_PBW` certifies the complete six-rod physical
metric contribution to the ternary operation on the canonical Berger
carrier.  It is a `LOCAL-ALGEBRAIC` subblock result, not a complete nonlinear
observer theorem.

The producer differentiates

\[
-\frac{\epsilon_R^2}{2}\sum_{a,I}\int
\sqrt{-\det\widehat g}\,\widehat g^{-1}(dR_{aI},dR_{aI})
\]

through quartic action order.  An exact determinant-log partition expansion
and noncommutative inverse-metric series supply the fourth densitized inverse
metric jet.  The lower three orders agree componentwise with the certified
rod `q2` producer, four direct symbolic fourth variations pass, and all
30,000 adjacent-transposition checks vanish.

The row chunks contain the `h-h-r-r`, `h-h-h-r`, and `h-h-h-h`
metric-output orbits and the `h-h-r` and `h-h-h` rod-output cyclic mates.
An independent PBW formal-adjoint replay reproduces both rod-output families,
including derivatives of background rod jets.  Graded symmetry and a
single-key deletion mutation are exact.

This leaves the base gravity-clock-Maxwell, memory-transport, normalized
readout, and physical-emitter `q3` sources to assemble.  The scalar-BV and
emitter Diff-BV sectors are structural `q3` zeros, but the complete zero
ledger has not yet been emitted.  Arity identities, Berger-isometry
equivariance, observer-morphism stability, detector response on the
second-order cone, nonlinear rank, Bridge 3, finite-parameter causal, and
quantum claims remain fail-closed.
