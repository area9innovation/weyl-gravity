# Berger 108-row apparatus scalar-BV q2 PBW block

The ten apparatus scalars use the universal master-action term

```text
integral phi_star L_c phi.
```

The certified gravity-clock `q2` already contains the complete 24-term PBW tensor for `Theta,Theta_star`. Each rod, memory and readout scalar block is the exact row relabeling of this tensor. The 108-row pairing proves that all ten relabelings preserve the signed field/cotangent pairing, so graded symmetry and lowered cyclicity transfer termwise.

The payload contains ten blocks and 240 terms. This is the first executable apparatus `q2` component payload, but it is only the scalar-BV semidirect sector. Rod metric couplings, memory transport, normalized readout, emitter blocks and all `q3` tensors remain to be exported before arity replay.
