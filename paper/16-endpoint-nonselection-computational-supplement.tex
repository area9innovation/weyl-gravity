\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,booktabs,tabularx}
\usepackage{microtype,xurl}
\usepackage[colorlinks=true,linkcolor=blue!55!black,
            citecolor=blue!55!black,urlcolor=blue!55!black]{hyperref}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}

\title{Computational Supplement to\\
``Lorentzian Endpoint Conditions Do Not Select the Einstein Sector
of Pure Weyl Gravity''}
\author{GPT-5.6.sol
\and Claude Fable 5}
\date{Working supplement, 24 July 2026}

\begin{document}
\maketitle

\section{Purpose}

This supplement is the destination for verification details removed from the
main narrative: recurrence bounds, interval cells, projective chart changes,
contour panels, mutation tests, content hashes, and exact commands.  The
theorem statements and proofs in the main paper are the mathematical
authority.  The machine-readable certificates provide pinned reproducibility
records, exact witnesses, validation margins, and independent implementation
checks.  A timeout, skipped rail, or reproduced producer output is never
recorded as an independent verification pass.

\section{Evidence taxonomy}

\begin{center}
\begin{tabularx}{0.96\textwidth}{@{}Y Y@{}}
\toprule
Evidence kind & Required supplement record\\
\midrule
Exact symbolic & input hashes, exact arithmetic kernel, canonical witness,
independent verifier\\
Validated numerical & declared domain, interval backend, nonzero margins,
subdivision policy, independent verifier\\
Numerical fixture & precision, residuals, purpose, and explicit
non-theorem status\\
Open computation & last valid checkpoint, failure mode, and
\texttt{does\_not\_establish} ledger\\
\bottomrule
\end{tabularx}
\end{center}

\subsection{Levels of independence}

``Independent'' is recorded at one of four levels:
\begin{center}
\begin{tabularx}{0.96\textwidth}{@{}c Y@{}}
\toprule
Level & Meaning\\
\midrule
I & Separate executable using independently written formulas\\
II & Different elimination, variable choice, or mathematical representation\\
III & Different exact-arithmetic, interval, or CAS backend\\
IV & Independently derived mathematical formulation\\
\bottomrule
\end{tabularx}
\end{center}
Rerunning a producer is reproduction, not verification.  The universal
Hessian/intertwiner, Schouten--Einstein/Maxwell carrier, and Euler
transgression chains have Level~II/IV checks.  The projective Evans selector
has a different projective representation and local-unit rail (Level~II).
The outgoing partial-jet checkpoint presently has a separately expanded
direct boundary system (Level~II) but not a second arithmetic backend.

Every publication record pins
\[
(\text{repository commit},\ \text{certificate SHA-256},\
\text{producer SHA-256},\ \text{verifier SHA-256})
\]
together with dependency versions, arithmetic or interval backend,
platform metadata where relevant, the canonical command, expected exit
status, and deterministic generator definition.

\section{Principal certificate chains}

The final supplement will tabulate the independent chains for:
\begin{enumerate}
\item the established conformal detour context, the universal
Ricci-factorized Hessian, the Schouten--Einstein/Maxwell carrier, and its
Euler-current boundary;
\item the second-order auxiliary-tensor parent action, its factorized
Einstein Green current, canonical null lift, and QNM divisor count;
\item the parent-Hessian inverse, conditional rank-one QNM Laurent algebra,
simple-self-extension involution lemma, and endpoint Krein graph/cotangent
obstructions;
\item axial operator extraction and Ricci reconstruction;
\item RW/RW/spin-one factorization, partial-jet identities, and the exact
no-rational-intertwiner test;
\item incoming and horizon Lee--Wald Grams;
\item global incoming population and the common scattering cell;
\item boundary d\'evissage and the no-growing-mode theorem;
\item projective Evans winding, local selector, and spin-one local-unit test.
\end{enumerate}

The primary local-structure reproducibility record is
\path{black_hole_programme/phase4/
axial_universal_hessian_intertwiner_v1/certificate.json}.
Its independent verifier re-derives the curvature-invariant coefficients,
the \(2\times2\) positivity determinant, the fourth-order intertwiner
compatibility equation in both spin directions, the indicial factors, and
the terminal \(A+B/r\) substitution.  Five mutation tests fail closed on
the Hessian trace coefficient, horizon exponents, terminal residual, and a
forbidden nonlocal promotion.

The parent-current reproducibility record is
\path{black_hole_programme/phase4/
second_order_parent_flux_v1/certificate.json}.  Its Level~II verifier
independently repeats the four-dimensional trace elimination and null-lift
algebra, checks the imported Euler and physical connection-EP2 boundaries,
and fails closed on generic radial-to-time-Jordan promotion,
Green-resolvent promotion, all-frequency reflection-zero exclusion, and a
complete polar Gram.  The principal future current audit is
\[
G_{\rm raw\ Weyl}-G_{\rm parent}=G_{\rm Euler\ cut}
\]
at the horizon, a finite matching section, and null infinity.  Until those
endpoint comparisons are separately certified, the universal parent
identity does not substitute for the existing raw-Weyl endpoint
certificates.

\subsection{Parent-resolvent and Krein-obstruction chain}

The exact record
\path{black_hole_programme/phase4/
parent_resolvent_krein_obstructions_v1/certificate.json}
checks the auxiliary-tensor Hessian inverse by two-sided block
multiplication and independently verifies the rank-one identity
\[
P\mathcal AP
=\frac{\langle\widetilde u,\mathcal Au\rangle}{\alpha^2}
u\otimes\widetilde u .
\]
It also certifies the characteristic-zero involution lemma for a nonsplit
self-extension of a simple object, the endpoint positive-graph
obstruction, and the quotient duality \(B_2/E\simeq E^*\).

The record is deliberately fail-closed at three points.  It does not certify
a physical Fredholm QNM pole, generic simplicity of the Regge--Wheeler
differential module, or a Schwarzschild retarded propagator.  Consequently
the stronger conclusion that every rational involution of the Bach
spin-two block is \(\pm I\) remains conditional; only the earlier
branch-resolving no-go is unconditional.

\subsection{Parent-overlap and retarded-convolution experiments}

The first experiment imports the certified simple axial QNM disk and
computes the regularized parent overlap
\[
\operatorname{FP}\langle\widetilde f_n,\mathcal Af_n\rangle
\]
including endpoint terms.  A separate scalar-projection rail then compares
it with the normalized radial extension overlap and must verify that the
induced commutators vanish in the same regularized adjoint pairing.
Numerical agreement without this projection audit is not a promotion gate.

The second experiment implements the formal convolution
\[
G_{hh}^{\rm ret}
=\frac1{4\alpha_{\rm W}}\,
G_E^{\rm ret}*\mathcal A G_E^{\rm ret}.
\]
Its first control is the flat-space biwave kernel
\(\theta(t-r)/(8\pi)\), up to the declared operator-sign convention.  Only
after that control passes will the Schwarzschild rail compare direct
six-state evolution with two sequential Einstein evolutions plus the
homogeneous source-Einstein solution.  These are experiment specifications,
not current time-domain theorems.

\section{New-experiment rail}

The explicit outgoing experiment uses the four-state dual-number family
\[
\begin{pmatrix}
A+\varepsilon E&D+\varepsilon C\\
0&A_1
\end{pmatrix},
\qquad \varepsilon^2=0,
\]
with phase-factored endpoint amplitudes.  It transports correlated base and
tangent data so that the combination \(bd-ac\) is formed inside the
dual-number algebra rather than reconstructed from independent interval
boxes.  The paper will not promote an explicit \(T_+\) band until this rail
has an independent certificate and a Stokes-identity audit.

The first Phase-4 successor resumes the last certified common-moving
checkpoint at \(r=979/32\) and reaches \(r=487/16\).  On that panel the
validated exponential tail is
\[
4.4421679558528834\times10^{-5},
\]
the joint hull width is \(5.12490045532877\), the partial-jet and independent
direct sixteen-state Taylor coefficients agree exactly, and their interval
difference contains zero.  These statements have different force: the
independently generated symbolic Taylor recurrences agree coefficientwise
through the declared order, whereas the separately enclosed numerical
transports merely overlap on the panel.  The overlap is a consistency check,
not a proof of equality.

The physical partial-jet equation has six complex components per selected
column: two upper spin-two tangent components, two base spin-two components,
and two spin-one components.  The direct boundary gate deliberately embeds
this into the unreduced derivative system.  For the two selected outgoing
columns it transports an \(8\times2\) array,
\[
16
=
8\ \hbox{base entries}
+8\ \hbox{full tangent entries},
\]
including the spin-one tangent rows that the exact partial jet proves to be
zero.  ``Six-state'' therefore names the reduced physical system per column;
``sixteen-state'' names the two-column unreduced computational audit.

Generator \(7315\) is the deterministic affine-noise identifier for the
single shared frequency generator; preserving it verifies that the base and
tangent rails retain the same frequency dependency rather than receiving
independent interval noise.  The hull width \(5.12490045532877\) is below
the programmed fail-closed checkpoint threshold \(10\).  It is useful only
as evidence that this short correlated successor remains bounded and
restartable; it is too coarse to certify physical amplitudes or conditioning.
This is a transport checkpoint only: the common frame has not reached
\(r=4\), so no \(T_+\), reflection, or Stokes claim follows.

\end{document}
