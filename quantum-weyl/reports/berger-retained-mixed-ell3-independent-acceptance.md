# Independent acceptance of the retained Berger mixed ell3

The quantum-side consumer independently accepts the retained typed
gravity--Maxwell `ell3` at classical hardening commit `e99d0c1d`. It reconstructs the
explicit cyclic 64-to-36 contraction without importing the classical
producer and matches all 25,950 retained contact coefficients exactly.

The second inclusion contains 96 gravity coefficients supported on rows
37--38 and 12 mixed coefficients supported on row 38. The
gravity-outer/mixed-inner channel has 144 outer/inner coefficient pairs.
After direct-leg inclusion and the three graded unshuffles these give 324
signed contributions and 342 canonical full-complex PBW coefficients, all in
full output row 38. None has an output row supported by the retained
projection. The other two
relative exchange channels have no outer/inner coefficient pairs. Consequently all
three retained `q2 S q2` exchange sectors vanish exactly.

This independently agrees with the hardened producer's exchange-vanishing
ledger: the first channel has full-complex carriers, while its retained
projection is zero.

As a projection-sensitivity control, the consumer adds one mutant entry
mapping full row 38 to retained row zero. All 342 exchange coefficients then
survive. The mutation is rejected, proving that the certified zero is caused
by the actual projection and is not a hard-coded empty exchange table.

The retained relative arity-three identity closes on all 36 rows. Mutating
one retained coefficient produces two exact defects. The nonzero bracket
contains 7,614 gravity-output coefficients with two Maxwell inputs and
18,336 Maxwell-output coefficients with one Maxwell input, establishing a
genuine retained gravity--light interaction.

This is `LOCAL-ALGEBRAIC` classical-input acceptance. The retained 36-row
complex has not yet been projected onto the Einstein-like and extra-Weyl
dynamical residual branches, so it does not yet give their mixing table. The
even dynamical and odd topological deformation/vertex classes are a separate
basis; the topological class is not a third particle branch.
The bracket does not modify the unary kinetic operator and therefore does not
introduce a negative physical direction, but this is not a unitarity theorem.
The companion `BERGER_RETAINED_MIXED_ELL3_PHYSICAL_CYCLICITY` certificate now
independently replays all 25,662 physical quartic coefficients. The 288-term
ghost/antifield completion still imports cyclicity from the typed transfer
theorem. No QME or quantum claim is made.
