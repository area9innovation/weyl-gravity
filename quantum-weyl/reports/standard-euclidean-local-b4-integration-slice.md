# Standard Euclidean local-b4 integration slice

Dependency tags: `EUCLIDEAN-SPECTRAL`, `LOCAL-ALGEBRAIC`.

The standard round-\(S^4\) conformal-spin-two calculation is now consolidated
into one integration-slice ledger:

| factor | \(\Gamma_1\) exponent | \(Z\) exponent | zero modes |
|---|---:|---:|---:|
| `Delta_2_perp(4)` | `+1/2` | `-1/2` | 0 |
| `Delta_0(-4)` | `-1/2` | `+1/2` | 5 |
| `Delta_2_perp(2)` | `+1/2` | `-1/2` | 0 |
| `Delta_1_perp(-3)` | `-1/2` | `+1/2` | 10 |

It binds the nonzero-mode York/Hodge cancellation, unit nonminimal-quartet
superdeterminant, priming of the fifteen conformal ghost zero modes, normalized
`+iR` algebraic TT auxiliary thimble, and parity-even local heat-kernel
prescription. The unwanted scalar `Delta_0` exponent is exactly zero.

Consequently, the standard local anomaly vector

\[
(C^2,E_4,C\widetilde C)
=\left(\frac{199}{30},-\frac{87}{20},0\right)
\]

has a single machine-readable factor/measure/zero-mode/contour provenance
chain. Removing finite-dimensional kernels and normalizing the algebraic
identity block do not change the local \(b_4\) coefficient.

This does not promote the vector to a repository result. The round-\(S^4\)
repository TT Hessian dictionary and auxiliary-row identification remain
absent, the full-BV multiplicity receiver has not accepted a physical export,
and the global determinant phase of the negative scalar level-zero eigenvalue
is not fixed.
