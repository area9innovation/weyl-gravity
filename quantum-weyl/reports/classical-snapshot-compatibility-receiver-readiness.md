# Classical snapshot compatibility receiver readiness

Status: `LOCAL-ALGEBRAIC` receiver ready; no physical cross-commit bridge supplied.

The regulated Slavnov-breaking input may be produced at a different repository commit from the frozen local-BV calculation. Commit equality is therefore not required, but content equality is. The receiver accepts a distinct-commit bridge only when all five canonical hashes agree exactly:

- minimal generator dictionary;
- local atom dictionary;
- classical BV differential;
- action and identity dependencies;
- grading and locality scope.

The physical path also replays two content-addressed, role-specific proof objects: the frozen quantum import and the analytic producer's classical export. A declaration containing equal strings without those proofs is rejected. A synthetic equal-hash fixture and four localized mutations exercise the mechanics; that fixture is not a scientific compatibility bridge.

This closes an API ambiguity, not an analytic gate. The round-S4 TT dictionary, complete Euclidean elliptic formulation, regulator and measure policy, and regulated BV Slavnov insertion remain required. No multiplicity ledger, anomaly coefficient, QME disposition, residual transfer, or Lorentzian quantum theory follows from this readiness result.
