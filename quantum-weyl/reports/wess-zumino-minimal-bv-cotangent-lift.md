# Wess–Zumino minimal-BV cotangent lift

Status: `EXACT_MINIMAL_BV_COTANGENT_LIFT_CERTIFIED_EXTENDED_COHOMOLOGY_OPEN`

The compensator field row is generated by the single minimal master term

\[
S_{\tau,\min}=\int \tau^*(\mathcal L_\xi\tau+\omega).
\]

Its four Euler derivatives force the complete new rows:

\[
Q\tau=\mathcal L_\xi\tau+\omega,
\qquad
\delta\omega^*=N_\omega+\tau^*,
\]

\[
\delta\xi^*_{\mu}=N_{\xi,\mu}+\tau^*\nabla_\mu\tau,
\qquad
\gamma\tau^*=\mathcal L_\xi\tau^*.
\]

The corresponding Lie-prolonged cotangent rows are derived with the opposite
formal-adjoint signs. On all 24 strict-plus-compensator atoms the exact replay
finds

```text
delta^2 = 0
delta gamma + gamma delta = 0
Q^2 = 0
all component gradings and antifield-number shifts exact
```

The useful canonical change of variables is

\[
\widehat g=e^{-2\tau}g,
\qquad
\widehat g^*=e^{2\tau}g^*,
\qquad
\widehat\tau^*=\tau^*+2g_{\mu\nu}g^{*\mu\nu}.
\]

It preserves the canonical one-form and converts the Weyl Noether row to

\[
\delta\omega^*=\widehat\tau^*.
\]

Thus `(tau,omega,omega_star,tau_hat_star)` is an exact contractible quartet;
the stored matrices independently verify `Q_W^2=0` and
`Q_W h+h Q_W=I_4`.

The exponential change is interpreted in the formal `tau`-adic local analytic
completion. This receipt certifies the cotangent lift only. Its successor,
[`wess-zumino-extended-local-bv-cohomology.md`](wess-zumino-extended-local-bv-cohomology.md),
enumerates the pure-Diff dimension-four `H04` quotient in `g_hat`, binds the
existing pure-Diff `H14=0` result to that algebra, and restores the local
Euclidean QME at one loop. Residual transfer nevertheless remains forbidden
until the compensator is included in a certified classical contraction.

Machine receipt:
`anomalies/certificates/WESS_ZUMINO_MINIMAL_BV_COTANGENT_LIFT.json`.
