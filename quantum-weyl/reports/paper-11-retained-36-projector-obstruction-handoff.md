# Paper 11 retained-36 projector-obstruction handoff

Paper 11 is under a separate live review edit, so this receipt records the
non-conflicting change required at that review's next claim-map regeneration.

Import
`BERGER_RETAINED_36_BRANCH_PROJECTOR_OBSTRUCTION_IMPORT` and replace the open
request for `BERGER_RETAINED_36_RESIDUAL_BRANCH_BASIS_V2` with the following
scoped disposition:

- the paper's retained full-BV `ell3` cyclicity theorem is unaffected;
- a canonical finite-order support-local same-bundle Einstein-like/extra-Weyl
  projector is obstructed on the retained 36-row carrier when the Einstein
  image is the certified rough tensor-wave branch;
- no 36-row branch-space `ell3` mixing table is authorized;
- the next support-local construction target is the candidate retained
  46-row STF2-prolongation-plus-cyclic-dual carrier;
- the rank-46 target is not yet a projector theorem;
- filtered/mapping-cylinder alternatives remain open, and a nonlocal split
  must carry the `REDUCED-MODE` tag.

The claim map should retain explicit false values for residual branch
projection, residual mixing, QME restoration, particle interpretation, and
all quantum claims. The obstruction certificate should be added to the input
hash ledger. This handoff does not edit or certify the manuscript while its
review changes remain uncommitted.
