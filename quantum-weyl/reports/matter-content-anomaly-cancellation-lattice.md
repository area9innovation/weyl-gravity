# Matter-content anomaly-cancellation lattice

The strict pure-Weyl one-loop vector is

\[
(c,-a,p,b)=\left(\frac{199}{30},-\frac{87}{20},0,0\right).
\]

For standard free conformal spin \(0,\frac12,1\) fields, two exact
coefficient routes agree: a minimal-operator Seeley--DeWitt ledger (including
the vector Faddeev--Popov complex) and an independent central-charge/index
reconstruction from the integer spin weights. The strict breaking,
antifield-completion audit, and Weyl-graviton \(\Box R\) scheme conversion
are imported by content hash. In the common reference scheme the rows are

| field | \(c\) | \(-a\) | \(p\) | \(b_{\Box R}\) |
|---|---:|---:|---:|---:|
| real conformal scalar | \(1/120\) | \(-1/360\) | \(0\) | \(1/180\) |
| Weyl absolute determinant | \(1/40\) | \(-11/720\) | \(0\) | \(1/60\) |
| Dirac fermion | \(1/20\) | \(-11/360\) | \(0\) | \(1/30\) |
| gauge vector with ghosts | \(1/10\) | \(-31/180\) | \(0\) | \(-1/10\) |

A Yang--Mills adjoint contributes \(\dim(\operatorname{ad}G)\) complete
gauge-vector rows, including its ghosts and nonminimal cancellation.

The \(C^2\) coordinate is strictly positive for gravity and every
standard-sign matter generator. Therefore no nonnegative real combination,
and hence no nonnegative integer content in any finite box or without a
bound, cancels the strict anomaly. This argument is independent of the
scheme-dependent \(\Box R\) coordinate.

## Complete signed lattice

Let \(N_W\) count Weyl absolute determinants and let

\[
N_V=N_{U(1)}+\sum_i\dim(\operatorname{ad}G_i)N_{{\rm YM},i}.
\]

After multiplying the two even equations by \(720\), their matter matrix is

\[
\begin{pmatrix}
6&18&36&72\\
-2&-11&-22&-124
\end{pmatrix},
\]

with Smith invariant factors \((1,30)\). Every formal signed solution is

\[
\boxed{
N_s=128+48t,\quad
N_W=-308-2u-20t,\quad
N_D=u,\quad
N_V=t
}
\]

for \(u,t\in\mathbb Z\). Its homogeneous kernel is generated by
\((0,-2,1,0)\) and \((48,-20,0,1)\).

Negative multiplicity means an inverse determinant or wrong-statistics
power. An overall wrong-sign bosonic kinetic contour does not automatically
implement that sign, so this lattice is not advertised as healthy matter.

## Chiral Ward policy and compensators

For the declared common diffeomorphism-, Lorentz-, and gauge-Ward-preserving
regulator, one left or right Weyl determinant has \(p=0\) and exactly half the
Dirac even anomaly. This is independently reproduced by Pauli--Villars
Fujikawa and Weyl-determinant path-integral calculations. Regulator
prescriptions producing a Pontryagin trace term are not admitted into the
common BV complex and are not silently treated as extra lattice columns.

The full chiral refinement therefore has one additional integer parameter
\(r=N_L\):

\[
N_L=r,\qquad N_R=-308-2u-20t-r.
\]

No parity-balance assumption is needed for the declared \(p=0\) policy, and
the even cone no-go is unchanged.

An ordinary homogeneous conformal scalar is one scalar column. The shifting
Wess--Zumino compensator is not: it changes the BV complex and local
counterterm algebra and makes the anomaly exact in the formal tau-adic
theory. Higher-derivative conformal fields are not silently added; each needs
a full BV complex and two independently certified coefficient methods.

The displayed affine formula is complete both over \(\mathbb Z\), with
\(u,t\in\mathbb Z\), and over \(\mathbb Q\), with \(u,t\in\mathbb Q\).

Dependency tags: `LOCAL-ALGEBRAIC`, `EUCLIDEAN-SPECTRAL`.

This result does not establish a Lorentzian QME, state, particle,
phenomenology, GUT selection, scattering, positivity, or unitarity theorem.

CLOSE-OUT: DONE — the healthy standard matter cone is empty and the full formal signed determinant lattice is classified
EVIDENCE: quantum-weyl/anomalies/certificates/MATTER_CONTENT_ANOMALY_CANCELLATION_LATTICE.json
