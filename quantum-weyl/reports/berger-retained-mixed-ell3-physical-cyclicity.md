# Retained mixed ell3 physical-quartic cyclicity

The quantum-side exact consumer independently lowers the retained mixed
`ell3` with the typed 36-row pairing and transposes the first physical input
through the invariant volume form. All derivative terms are formally
adjointed and reduced in the exact PBW algebra over
`Q(sqrt(10))`.

All 25,662 degree-zero physical quartic coefficients reproduce with zero
defects. Of these, 7,506 have gravity output and 18,156 have Maxwell output.
Changing the Maxwell pairing weight from two to one produces 17,108 defects
on 14 rows, so the successful equality is sensitive to the typed pairing
normalization.

The convention ledger retains the signed odd-pairing orientations: ten
gravity equation/field pairs carry `-1`, while four Maxwell pairs carry `+2`.
The physical field-equation transpose uses their absolute component
multiplicities one and two. Recording both prevents the physical check from
silently converting the odd BV pairing into a positive pairing theorem.

The theorem is intentionally narrower than full retained BV cyclicity. The
remaining 288 `ell3` coefficients form the ghost/antifield completion and
have not yet received an independent cyclic transpose replay. Their
cyclicity remains imported from the classical typed-transfer theorem.

This is a `LOCAL-ALGEBRAIC` classical-input acceptance. It does not project
onto Einstein-like or extra-Weyl residual modes, does not treat the
topological deformation class as a particle direction, and makes no QME,
Lorentzian, unitarity or quantum claim.
