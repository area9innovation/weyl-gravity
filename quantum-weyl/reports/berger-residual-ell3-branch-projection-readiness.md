# Residual mixed-ell3 branch-projection readiness

The consumer contract for the next classical interaction gate is frozen. It
requires a content-addressed residual basis on the retained 36-row complex,
including exact inclusion and projection maps, the pulled-back pairing,
parity, real structure, and the `K_Berger` weight operator.

The mixed tensor requires both gravity and Maxwell residual carriers. A
gravity-only basis cannot decide the accepted sectors with two Maxwell inputs
and gravity output or one Maxwell input and Maxwell output. The gravity ledger
must distinguish the dynamical `Einstein_like` and `extra_Weyl` branches;
the Maxwell branch list must be explicit and exhaustive for the declared
projection sector.

The topological direction is kept in a separate local deformation/vertex
carrier. Its exact basis is
`e=(W_+^2+W_-^2)/sqrt(2)` and
`o=(W_+^2-W_-^2)/sqrt(2)`, with Euler--Lagrange ranks one and zero,
respectively. The contract therefore cannot misidentify the Pontryagin/theta
direction `o` as a third particle or dynamical gravity mode.

The eventual mixing table must be computed by exact precomposition and
postcomposition of the accepted retained `ell3`. Every zero needs a support or
cancellation witness, and graded symmetry, cyclicity, parity, and `K_Berger`
selection must be independently replayed in branch coordinates. The contract
forbids inferring kinetic or pairing health from `ell3` alone.

No branch-basis manifest is currently supplied. Consequently no residual
branch projection, Einstein/extra-Weyl mixing table, deformation-vertex action,
residual quantum transfer, QME, Hadamard, particle, or quantum claim is made.
