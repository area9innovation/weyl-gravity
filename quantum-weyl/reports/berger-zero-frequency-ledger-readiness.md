# Berger retained zero-frequency ledger readiness

The complete classical causal chain and local Hadamard singularity are known,
but they do not determine the smooth stationary zero-frequency covariance.
The global stationary export currently fixes 10,528 of 10,816 entries of the
104-row Cauchy generator. The remaining 288 coordinates are exactly the ghost
and identity diagonal `12 x 12` blocks.

This incompleteness is spectrally material. Setting both unknown blocks to
zero or to the identity gives two exact formal completions that agree on every
exported coordinate but differ by 24 endpoint zero eigenvectors. These are
mask-compatible witnesses, not proposed physical operators. They prove that
the present artifacts do not determine even the ordinary kernel, hence cannot
determine the generalized zero space or Jordan structure.

The receiving contract therefore requires one complete exact `A104`, the
104-row Cauchy BRST prolongation, nondegenerate graded Cauchy/Krein form, and
real involution in the frozen ordering. Only after their algebraic identities
and a closed-realization/isolation theorem pass may the zero-mode CCR, Ward,
reality, stationarity, and physical positivity problems be solved.

This is a `LOCAL-ALGEBRAIC` and `LORENTZIAN-CAUSAL` readiness theorem. It is
not a zero-frequency ledger, Hadamard state, positivity theorem, QME, or
quantum claim.

Tier-1 receipts on 2026-07-17: certificate reproduction `PASS` in 0.04 s,
independent verification `PASS` in 0.11 s, and six mutation-sensitive tests
`PASS` in 0.15 s. The two new Draft 2020-12 schemas passed together in
0.12 s. Tier 2 was unnecessary because the audit replays the complete
content-addressed known-entry mask and does not alter its upstream operators.
Tier 3 was not run because the result is fail-closed and makes no Hadamard,
QME, freeze, release, or quantum promotion.
