# Berger Schur spectral receiver: exact nondefinition

The corrected Berger Schur inputs do not yet determine any of the five complete parity-even third-curvature functions. This is a certified receiver result, not an absence-of-work statement.

The integration imports the normalized Schur variations through cubic order, the exact low blocks, the first high-mode insertion obstruction, the generic finite-kernel nonuniqueness theorem, and the frozen ten-coordinate affine receiver. The scalar surrogate is excluded.

## What survives

- All ten universal partial-BV coordinate rows remain computed and content-addressed.
- The ambiguity matrix has exact rank ten and transpose-kernel dimension zero.
- On the selected scalar-flat Berger background, the local rows are
  \(\operatorname{Wres}K=8/3\), \(\operatorname{Wres}K^2=4/9\), and
  \(\partial_{\log\mu}\log\det_3=22/9\), before the common \((4\pi)^{-2}\) factor.
- Exact priming and matched zero-pole cancellation remain certified on the low-block scope \(2j\leq2\), \(|n|\leq1\).

## What is nondefined

Every coordinate in the \(I10,I24,I25,I28,I29\) carrier family has a nondefined finite Schur row. The smallest joint missing object is a regulated \(B_1\), finite-part \(B_2\), and complete trace-class \(B_3\) evaluation on the fully primed Berger vector-Schur spectrum, with common domain, exceptional-block continuation, phase, contour, and subtraction policy.

The ordinary absolute trace route is unavailable: the exact central-weight family gives a nonvanishing absolute shell lower bound for \(B_1\). The accepted M21 and M23 requests have not supplied a replacement global payload. Accordingly the complete-function count is zero, not five and not a partial vector.

This result is tagged `LOCAL-ALGEBRAIC` and `EUCLIDEAN-SPECTRAL`. It does not compute an anomaly coefficient or determinant, alter Paper 12, restore a QME, or establish a Lorentzian or Hadamard result.

Machine certificate: `quantum-weyl/spectral/euclidean/certificates/BERGER_SCHUR_SPECTRAL_RECEIVER_NONDEFINITION_V1.json`.
