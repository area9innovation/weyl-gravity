# Repository regulator/zero-mode/measure readiness

Dependency tags: `LOCAL-ALGEBRAIC`, `EUCLIDEAN-SPECTRAL`.

The remaining combined integration carrier now has an executable receiver.
It requires one content-addressed repository ledger to bind every full-BV
factor to its exact determinant exponent, zero-mode priming, nonzero-mode
measure and Jacobians, covariant local regulator, indefinite-direction
contour, finite symmetry-volume policy, and global phase disposition.
For physical inputs it recursively invokes the semantic elliptic-complex,
multiplicity, and cross-snapshot validators rather than trusting result IDs.

The standard round-`S4` integration slice supplies local regulator, measure,
priming, and auxiliary-contour evidence. The unique negative scalar phase is
now explicit and proved irrelevant to the local `b4` density and local Slavnov
variation on its fixed-sign chamber; no global determinant branch is selected.
Repository binding remains open. The accepted full-BV multiplicity ledger binds
repository rows and standard factors, but is not itself a regulator or phase
ledger. The elliptic receiver remains ready without a physical full symbol
complex. These complementary artifacts are not silently composed.

The synthetic fixture and four mutations test exact weighted-rank arithmetic,
zero-mode totals, priming consistency, and the proof digest. They do not
certify the physical functional measure or a Slavnov breaking.

## Verification

```bash
PYTHONPATH=quantum-weyl python3 -m spectral.euclidean.regulator_measure_readiness --check
PYTHONPATH=quantum-weyl python3 -m spectral.euclidean.verify_regulator_measure_readiness
PYTHONPATH=quantum-weyl python3 -m unittest spectral.euclidean.tests.test_regulator_measure_readiness
```
