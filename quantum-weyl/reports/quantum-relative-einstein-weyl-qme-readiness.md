# Quantum relative Einstein--Weyl QME readiness

Generality level: `G0`.

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`, `LORENTZIAN-CAUSAL`.

## Imported evidence

The strongest current compact Einstein--Maxwell to Weyl--Maxwell evidence now
contains both generic parities. The complete standard-harmonic tangent
inclusion before any optional stabilizer reduction is exact on-shell and its classical
pullback form is nondegenerate, but it is not a global off-shell BV chain map
and is not symplectic block by block. A separate quadratic-channel preflight
supplies partial relative obstruction fixtures.

The relative linear-triangle preflight certifies the principal BV chain map
and cone and a strict polynomial off-shell chain map, solution cofiber, and
direct pairing in the generic axial block. The newly imported generic-polar
lift adds the complete eight-field ungauged ghost--field--equation--identity
chain map without inverting momentum or frequency. The quantum consumer
independently replays 14 exact contraction, Noether, chain-map, and formal-
adjoint identities. The pinned classical verifier also recomputes its exact
off-shell local Green identity with 184 temporal and 184 spatial terms.

The polar result is not a cyclic BV morphism and has no authorized stabilizer
descent. Exceptional and global relative rows and the global mapping cofiber
also remain open. The classical functor preflight therefore still rejects
`EINSTEIN_WEYL_RELATIVE_LINEAR_TRIANGLE_V1`. The polar and stabilizer
scientific artifacts are separately pinned to their commits and hashes.

The stabilizer authority also corrects the residual language. The connected
automorphism algebra of the fixed-flux Plebański--Hacyan background is
`R H direct-sum R P_x direct-sum so(3)`, not the vacuum-cylinder `SO(4,2)`.
Its action preserves the generic axial/polar `q` and `p` modules and their
Lee--Wald form, but explicit matrix elements refute universal presymplectic
nullity. Hence no absolute quotient is authorized. The lawful sequence is
local gauge quotient, stabilizer representation, common moment-map/Taub-zero
locus, and only then quotient by a certified null subalgebra.

The quantum side imports the current local anomaly-to-(D) comparison and the
partial global Berger `A104` ledger.  The former has no Cartan verdict; the
latter has no full Cauchy BRST operator, pairing, or Hadamard state.

## Shared relative row

| Setting | Map \(\iota\) | Cofiber | Relative pairing | \(\mathfrak O_2\) | Residual action | Observable map | Quantum lift |
|---|---|---|---|---|---|---|---|
| Compact Einstein--Maxwell product; complete standard harmonic tangent; fixed compact bundle; before any optional stabilizer reduction | principal, generic-axial, and generic-polar ungauged off-shell preflights imported; global V1 open | generic-axial solution cofiber and polar pre-derived-sector inclusion certified; global cofiber open | classical reduced-mode pullback only; not renormalized | partial quadratic fixtures; arity-three disposition open | five-generator generic equivariance certified; common Taub-zero locus open | relative pullback open | `ANALYTIC_FRAMEWORK_MISSING` |

## Exact remaining-sector ledger

The import closes the generic-polar equation/Noether square, not the global
triangle. The remaining classical work is now localized:

1. On generic polar modes, construct a cyclic BV enhancement or a normalized
   obstruction; compute the `H`, `P_x`, and `J_i` moment maps on the Einstein
   `q`-primary and extra `p`-primary coefficients; classify their common
   Taub-zero locus and any null subalgebra; and only then compute the derived-
   sector cofiber and pairing.
2. On the exceptional `ell=1` block, supply the relative off-shell equation
   and identity maps plus the mapping cofiber and action-derived cyclic form.
3. On `ell=0` and global twists, supply those maps together with the fixed-
   charge domain and global cofiber.
4. Assemble a degreewise-injective or explicitly derived replacement
   triangle, prove global cone nilpotency and cohomology, close magnetic-bundle
   patching/boundaries, and issue `EINSTEIN_WEYL_RELATIVE_LINEAR_TRIANGLE_V1`.

The missing classical import gate consists of exactly:

```text
EINSTEIN_WEYL_RELATIVE_LINEAR_TRIANGLE_V1
EINSTEIN_WEYL_RELATIVE_LINFINITY_THROUGH_ARITY_THREE
RELATIVE_RESIDUAL_AND_OBSERVABLE_FUNCTOR_V1
```

The quantum consumer will import these results by hash.  It will not
reconstruct their maps or cofiber independently.

## Relative anomaly contract

Only the future target is frozen:

\[
[\mathcal A_{\mathrm{rel}}]
=
[\mathcal A_{\mathrm{Weyl}}-\iota_*\mathcal A_{\mathrm{Einstein}}].
\]

This class is not yet defined.  It requires the off-shell map, separate local
QME dispositions, renormalized observable algebras and restriction, and
explicit antifield and boundary maps.  Bulk, antifield, boundary/corner,
zero-mode, measure/Jacobian, central-extension, and (D)-Cartan ledgers remain
separate.

## Verdict and claim boundary

The classical import gate remains `NOT_SATISFIED`; the axial-plus-polar
preflight is positive local evidence, not the required all-sector V1 result. The sole
quantum verdict is `ANALYTIC_FRAMEWORK_MISSING`. No relative anomaly,
restored QME, renormalized pairing, state restriction, (D)-Cartan verdict,
particle interpretation, or Lorentzian quantum theorem is claimed.  Residual
quantum transfer remains unauthorized.

## Verification

The certificate has strict Draft 2020-12 validation, exact source hashes,
pinned polar/stabilizer commit checks, an independent polynomial replay,
classical Green-current replay, and fail-closed coefficient and claim
mutations. Tier-1 receipts on 2026-07-17 are:

| Command | Elapsed | Status |
|---|---:|---|
| `PYTHONPATH=quantum-weyl python3 -m relative.einstein_weyl_qme_readiness_certificate --check` | 0.72 s | `PASS` |
| `PYTHONPATH=quantum-weyl python3 -m relative.verify_einstein_weyl_qme_readiness` | 2.49 s | `PASS` |
| `PYTHONPATH=quantum-weyl python3 -m active_frontier_certificate --check` | 0.04 s | `PASS` |
| `PYTHONPATH=quantum-weyl python3 -m verify_active_frontier` | 0.10 s | `PASS` |
| `PYTHONPATH=quantum-weyl python3 -m unittest quantum-weyl/relative/tests/test_einstein_weyl_qme_readiness.py quantum-weyl/tests/test_active_frontier.py -v` | 3.14 s | `PASS` (19 tests) |
| strict Draft 2020-12 AJV validation of the relative and frontier certificates | 1.97 s + 1.08 s | `PASS` |

Tier 2 is unnecessary because the classical polar input is unchanged and
content-addressed; Tier 3 remains unnecessary because the full triangle gate
is unsatisfied and no theorem, freeze, release, or quantum lifecycle is
promoted.
