# Quantum relative Einstein--Weyl QME readiness

Generality level: `G1_COMPLETE_LINEAR_RELATIVE_COMPLEX_ONE_BACKGROUND`.

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`, `LORENTZIAN-CAUSAL`.

## Imported evidence

The final classical linear triangle is now imported by commit and content
hash. It is an all-row, finite-order, support-local off-shell chain map with
global endpoints, an exact mapping cofiber, and exact `H_product`
equivariance. It deliberately remains noncyclic: the source, target, and
relative action-derived forms are distinct, and the standard-pairing cyclic
map is obstructed rather than silently identified.

The final observable-functor export is imported separately. It gives the
contravariant support-local pullback on the linear local BRST observable DGA
and exact reduced coefficient detectors for every certified extra cofiber.
Those detectors are `REDUCED-MODE` solution-cohomology functionals, not
support-local spacetime, Peierls, relational, or quantum observables.

The older generic-polar lift remains an independent exact regression input.
The quantum consumer replays 14 contraction, Noether, chain-map, and formal-
adjoint identities, while the pinned classical verifier recomputes the 184
temporal and 184 spatial terms of its local Green identity. The polar and
stabilizer scientific artifacts remain pinned to their commits and hashes.

The stabilizer authority also corrects the residual language. The connected
automorphism algebra of the fixed-flux Plebański--Hacyan background is
`R H direct-sum R P_x direct-sum so(3)`, not the vacuum-cylinder `SO(4,2)`.
Its action preserves the generic axial/polar `q` and `p` modules and their
Lee--Wald form, but explicit matrix elements refute universal presymplectic
nullity. Hence no absolute quotient is authorized. The lawful sequence is
local gauge quotient, stabilizer representation, common moment-map/Taub-zero
locus, and only then quotient by a certified null subalgebra.

The quantum side imports the current local anomaly-to-(D) comparison and the
partial global Berger `A104` ledger.  The former has no Cartan verdict; the
latter has no full Cauchy BRST operator, pairing, or Hadamard state.

## Shared relative row

| Setting | Map \(\iota\) | Cofiber | Relative pairing | \(\mathfrak O_2\) | Residual action | Observable map | Quantum lift |
|---|---|---|---|---|---|---|---|
| Compact Einstein--Maxwell product; complete standard harmonic tangent; fixed compact bundle; before any optional stabilizer reduction | complete all-row support-local noncyclic linear triangle imported | support-local mapping cofiber and exact extra detectors imported | three action-derived forms distinct; standard cyclic map obstructed; not renormalized | partial quadratic fixtures; arity-three disposition open | exact `H_product` equivariance imported; final residual quotient open | support-local linear BRST-DGA pullback imported | `ANALYTIC_FRAMEWORK_MISSING` |

## Exact remaining-sector ledger

The linear import gate is satisfied. Remaining work is divided without
conflating it with the completed triangle:

1. Resolve the cyclic relative structure by either constructing a replacement
   for the obstructed standard-pairing map or retaining an explicitly
   noncyclic theory; no renormalized relative pairing exists yet.
2. Construct the complete relative `f2` and verify the arity-three identity.
3. Compute matched Einstein and Weyl QME dispositions, a renormalized
   observable pullback, and a causal state restriction before defining the
   quantum relative class.

The sole remaining classical nonlinear import is:

```text
EINSTEIN_WEYL_RELATIVE_LINFINITY_THROUGH_ARITY_THREE
```

The quantum consumer will import these results by hash.  It will not
reconstruct their maps or cofiber independently.

## Relative anomaly contract

Only the future target is frozen:

\[
[\mathcal A_{\mathrm{rel}}]
=
[\mathcal A_{\mathrm{Weyl}}-\iota_*\mathcal A_{\mathrm{Einstein}}].
\]

The classical pullback now exists, but this quantum class is not yet defined.
It still requires separate local QME dispositions, renormalized observable
algebras and restriction, and explicit antifield and boundary maps. Bulk,
antifield, boundary/corner,
zero-mode, measure/Jacobian, central-extension, and (D)-Cartan ledgers remain
separate.

## Verdict and claim boundary

The linear classical import gate is
`LINEAR_GATE_SATISFIED_NONLINEAR_GATE_OPEN`. The sole quantum verdict remains
`ANALYTIC_FRAMEWORK_MISSING`. No relative anomaly,
restored QME, renormalized pairing, state restriction, (D)-Cartan verdict,
particle interpretation, or Lorentzian quantum theorem is claimed.  Residual
quantum transfer remains unauthorized.

## Verification

The certificate has strict Draft 2020-12 validation, exact source hashes,
pinned final-triangle/functor and polar/stabilizer commit checks, an independent
polynomial replay, classical Green-current replay, and fail-closed coefficient
and claim mutations. Volatile roadmap and team-brief prose are intentionally
not semantic certificate dependencies; their edits cannot invalidate the
mathematical import. Tier-1 receipts on 2026-07-19 are:

| Command | Elapsed | Status |
|---|---:|---|
| `PYTHONPATH=quantum-weyl python3 -m relative.einstein_weyl_qme_readiness_certificate --emit` | 1.51 s | `PASS` |
| `PYTHONPATH=quantum-weyl python3 -m relative.verify_einstein_weyl_qme_readiness` | 5.05 s | `PASS` |
| `PYTHONPATH=quantum-weyl python3 -m unittest quantum-weyl/relative/tests/test_einstein_weyl_qme_readiness.py -q` | 6.15 s | `PASS` (11 tests) |
| strict Draft 2020-12 AJV validation | 2.50 s | `PASS` |

Tier 2 is required because the authoritative classical map and observable
functor changed. Tier 3 remains deferred until the complete regenerated
quantum receipt chain passes its scoped rails; no QME, Hadamard, release, or
quantum lifecycle is promoted here.
