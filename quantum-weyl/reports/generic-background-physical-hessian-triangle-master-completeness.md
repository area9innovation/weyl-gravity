# Generic physical-Hessian triangle master completeness

## Result

The eleven exact generic physical three-`H1` numerator rows over
`Delta^4` lie in a rank-52 relative-simplex-IBP span generated by the 46
canonical tangent columns and six master carriers.

The exact generic rank ladder is

```text
46 tangent columns
  + J_triangle       -> 47
  + M_x1             -> 48
  + M_x2             -> 49
  + M14 singlet      -> 50
  + M15 standard-u   -> 51
  + M16 standard-v   -> 52
```

Here

```text
M14 = alpha0 alpha1 alpha2
M15 = alpha0 alpha1 alpha2 (alpha1-alpha2)
M16 = alpha0 alpha1 alpha2 (alpha2-alpha0).
```

The last two carriers transform as the two-dimensional standard
representation of `S3`; they are not additional symmetric `M14` copies.
Adjoining each raises the generic rank by one.  The eleven physical rows form
five exact `S3` orbits (`I10`, `I24`, `I25`, `I28`, `I29`).  Independently
adjoining each orbit representative to the completed span leaves the exact
generic rank at 52; the stored exact orbit crosswalk transports that result
to every labelled row.

## Verification rails

The exhaustive rail recomputes five orbit-representative ranks over the exact
fraction field `Q(x1,x2,x3)` and the exact orbit crosswalk covering all eleven
rows.  It is intentionally not a per-commit test.
The fast rail replays dependency hashes, the exact generic six-master rank
ladder, the `S3` action, the formula digest and four exact rational box
fixtures.

```bash
PYTHONPATH=quantum-weyl python3 -m \
  spectral.euclidean.generic_background_physical_hessian_triangle_master_completeness \
  --fast --check

PYTHONPATH=quantum-weyl python3 -m \
  spectral.euclidean.verify_generic_background_physical_hessian_triangle_master_completeness

# Freeze/evidence rail only:
PYTHONPATH=quantum-weyl python3 -m \
  spectral.euclidean.verify_generic_background_physical_hessian_triangle_master_completeness \
  --exhaustive --jobs 1
```

## Claim boundary

Tags: `LOCAL-ALGEBRAIC`, `EUCLIDEAN-SPECTRAL`.

This closes the physical triangle's carrier inventory.  It does not yet
evaluate the renormalized singlet or standard-`S3` master values.  Therefore
it does not integrate the physical triangle, assemble third-curvature form
factors, supply `Gamma1` or `Q1`, restore a QME, authorize residual transfer,
or certify a Lorentzian or Hadamard theory.

## Test receipt

All commands ran from `physics/symplectic-reconstruction` on 2026-07-19.

| tier | command | elapsed | result |
|---|---|---:|---|
| 1 exhaustive evidence | `PYTHONPATH=quantum-weyl python3 -m spectral.euclidean.generic_background_physical_hessian_triangle_master_completeness --emit --jobs 1` | 8m38.180s | PASS |
| 1 fast producer | same module with `--fast --check` | 38.667s | PASS |
| 1 independent fast replay | `PYTHONPATH=quantum-weyl python3 -m spectral.euclidean.verify_generic_background_physical_hessian_triangle_master_completeness` | 35.691s | PASS |
| 1 unit tests | `PYTHONPATH=quantum-weyl python3 -m unittest spectral.euclidean.tests.test_generic_background_physical_hessian_triangle_master_completeness` | 39.312s | 5 PASS |
| 2 frontier chain | active-frontier emit, independent verifier and 8 tests | 1.6s | PASS |
| 2 atlas chain | atlas emit, independent verifier, 7 tests and common fragment validator | 6.0s | PASS |
| paper claim map | generate, stale check and independent verifier | <1s | PASS; 59 hashed inputs |
| paper build | main two passes; supplement three passes | 11.3s | PASS; warning-free |

Tier 3 was not run: this result adds a scoped Euclidean/local physical
triangle carrier certificate, but changes no shared core algebra, anomaly or
QME lifecycle state, residual transfer, Lorentzian theorem, freeze or release.
