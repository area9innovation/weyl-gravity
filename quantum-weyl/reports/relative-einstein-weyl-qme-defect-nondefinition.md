# Relative Einstein--Weyl QME defect non-definition

The strongest complete classical spine currently available is
`EINSTEIN_WEYL_RELATIVE_LINEAR_TRIANGLE_V1`: the complete all-row,
finite-order, support-local **noncyclic** linear triangle on the compact
Einstein--Maxwell product. Its companion observable theorem supplies the
contravariant chain map

\[
\iota^*:\operatorname{Obs}_{\rm WM}\longrightarrow
\operatorname{Obs}_{\rm EM}
\]

and exact \(H_{\rm product}\) equivariance, including time translation.

This is enough to define the classical linear relative complex convention

\[
C_{\rm rel}=\operatorname{Cone}(\iota^*:C_{\rm WM}\to C_{\rm EM})[-1],
\]

with

\[
D_{\rm rel}(a_{\rm WM},b_{\rm EM})
=
(s_{\rm WM}a_{\rm WM},
\iota^*a_{\rm WM}-s_{\rm EM}b_{\rm EM}).
\]

It is not enough to define a coefficient-bearing one-loop relative anomaly.

## Two exact missing rows

First, the requested target-valued subtraction

\[
[\mathcal A_{\rm rel}]
=
[\mathcal A_{\rm WM}-\iota_*\mathcal A_{\rm EM}]
\]

requires an action-compatible cyclic pushforward \(\iota_*\). The three
action-derived pairings remain distinct. The fixed 238-row carrier has an
exact degree-rank obstruction to a nondegenerate odd pairing. The 316-row
cotangent completion does have a pairing, but changes the carrier and pairing
and is not the missing action-current pushforward.

Second, the repository has no matched pair of renormalized
Einstein--Maxwell and Weyl--Maxwell QME insertions on this background with
common gauge, measure, antifield, zero-mode and regularization ledgers. The
known vector

\[
\left(\frac{199}{30},-\frac{87}{20},0,0\right)
\]

belongs to strict **pure** Weyl gravity. It cannot be reused as the
Weyl--Maxwell target or relative coefficient vector.

## Disposition

The relative local class and coefficient are `NOT_DEFINED`. Determinant
factorization, residual \(H_{\rm product}\) action and Lorentzian state status
remain separate:

- no matched relative determinant/measure subtraction is computed;
- the linear classical residual action is equivariant;
- the compact periodic setting has no asymptotic boundary claim;
- the common Taub-zero derived sector and final null quotient remain open;
- no Lorentzian relative state or \(D\)-Cartan verdict is constructed.

Dependency tags: `LOCAL-ALGEBRAIC`, `EUCLIDEAN-SPECTRAL`, `REDUCED-MODE`.

This result does not compute a relative QME defect, restore a QME, renormalize
the cofiber pairing, or establish particles, positivity, scattering or
unitarity.

CLOSE-OUT: OBSTRUCTED — the action-compatible cyclic pushforward and matched Einstein--Maxwell/Weyl--Maxwell renormalized QME insertion pair are absent
EVIDENCE: quantum-weyl/transfer/certificates/RELATIVE_EINSTEIN_WEYL_QME_DEFECT_NONDEFINITION.json
