# Wess–Zumino extended local BV cohomology

Status:
`TAU_ADIC_EXTENDED_GAUGE_FIXED_H04_H14_COMPLETE_ONE_LOOP_LOCAL_EUCLIDEAN_QME_RESTORED`.

The canonical dressed variables contract the complete Weyl quartet

\[
(\tau,\omega,\omega^*,\widehat\tau^*)
\]

and reduce the compensator-extended local BV complex to the pure-Diff BV
complex of `g_hat`. The exponential change is an exact formal automorphism in
the `tau`-adic local analytic completion; inverse series are replayed through
order eight with exact rational coefficients, and the coefficient convolution
is proved for every order by the binomial identity `(1-1)^n=delta_(n0)`.

At engineering dimension four, the exhaustive curvature basis gives

\[
H^{0,4}_{\rm ext,even}
=\operatorname{span}\{C(\widehat g)^2,E_4(\widehat g),R(\widehat g)^2\},
\]

\[
H^{0,4}_{\rm ext,odd}
=\operatorname{span}\{C(\widehat g)\widetilde C(\widehat g)\}.
\]

The new `R(g_hat)^2` direction was nonclosed in the strict Weyl complex and
must not be omitted. `Box R(g_hat)` remains horizontally exact. The only
dimension-zero natural tensor that could produce an incoming positive-AFN
boundary is `g_hat`; its image is the trace of the Bach row and vanishes.
Normalized coordinate duals for all three even classes and the odd class
annihilate both the horizontal and positive-antifield boundary spaces.

For ghost number one, the explicit primitives `(B_C,B_E,B_P,B_BOX)` have full
rank on the strict four-candidate carrier, while the independently certified
four-dimensional pure-Diff anomaly sector is zero. Therefore

\[
H^{1,4}_{\rm ext}=0
\]

in both parities within the declared algebra.

The coefficient-bearing counterterm

\[
-(4\pi)^{-2}\hbar\left({199\over30}B_C-{87\over20}B_E\right)
\]

cancels the complete repository one-loop breaking. Hence the local Euclidean
QME is restored at one loop in this compensator-extended theory.

This does not prove an all-loop or Lorentzian QME. The frozen strict residual
contraction does not include the compensator, so residual transfer remains
forbidden. Bridge 4 still lacks a same-background normalized mode/state
crosswalk, and Bridge 5 still lacks Bridge 2's interaction-to-physical-branch
map.

Machine receipt:
`anomalies/certificates/WESS_ZUMINO_EXTENDED_LOCAL_BV_COHOMOLOGY.json`.
