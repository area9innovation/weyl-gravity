# Repaired retained Berger Volterra v2 import

The quantum consumer pins classical commit `eb56d5af` and independently
accepts the repaired retained-metric causal theorem. The historical v1 audit
remains immutable and rejected; v2 closes its eight defects explicitly.

The accepted source contains:

- strict Draft 2020-12 schema validation;
- time-regular slab spaces (X_s(I)) and (Y_s(I));
- distinct solution and source resolvents;
- factorial bounds on both typed spaces;
- explicit advanced (J^-) and retarded (J^+) conventions;
- both companion and graph-pullback inverse identities;
- metric/antifield adjoint reversal against (A^sharp), without assuming
  (A=A^sharp);
- five content-addressed analytic proofs, a complete source manifest, and a
  timed verification receipt.

The earlier exact PBW replay supplies (D=e_0) equivariance and the formal
adjoint companion bundle. Thus the retained ten-row metric Green operators
are now accepted quantum inputs.

This does not yet accept the 26-row or 54-row v2 causal homotopies. Those
certificates must depend on the repaired v2 hash; causal (D)-Cartan,
Hadamard, QME and quantum claims remain open.

```text
PYTHONPATH=quantum-weyl python -m lorentzian.retained_biwave_volterra_v2_import_certificate --check
PYTHONPATH=quantum-weyl python -m unittest quantum-weyl/lorentzian/tests/test_retained_biwave_volterra_v2_import.py -v
```
