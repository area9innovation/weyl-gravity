# Residual ell3 branch-projection readiness V2

The classical preflight proved that the normalized parity basis

\[
e=(W_+^2+W_-^2)/\sqrt2,
\qquad
o=(W_+^2-W_-^2)/\sqrt2
\]

cannot be represented over \(\mathbb Q(\sqrt{10})\) alone. The V2 receiving
contract repairs this exactly without rewriting V1:

- retained operator and dynamical-branch data remain over
  \(\mathbb Q(\sqrt{10})\);
- normalized deformation data use
  \(\mathbb Q(\sqrt2,\sqrt{10})\);
- the normalized Gram matrix remains \(I_2\).

The strict schema distinguishes operator artifacts from deformation
artifacts and rejects a deformation artifact declared over the smaller
field. The synthetic inclusion/projection, parity, and mutation receipt still
passes.

No dynamical Einstein-like/extra-Weyl/Maxwell branch projector has been
supplied. Consequently no branch-space \(\ell_3\), mixing table, QME,
Hadamard, particle, positivity, or quantum claim is made.
