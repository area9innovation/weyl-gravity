# Finite-harmonic Tier-3 provenance relock

## Result

The Lee--Wald component-API migration is fully re-locked.  A unified scanner
recognized all four live provenance dialects: ordinary `path`/`sha256`
pairs, path-keyed hashes, `parent`/`parent_sha256`, and paired
`*_path`/`*_sha256` fields.  Its largest replay closure contained 204
artifacts and was acyclic.

The terminal audit covered 347 JSON artifacts and 1,811 references.  It found
zero missing inputs and zero stale hashes.  The complete Einstein-package
Tier-3 rail then passed 1,255 tests with one declared skip in 495.702 seconds
and 476,816 KB maximum RSS.

The structural finite-support theorem and Paper 13 are promoted to
`THEOREM_FROZEN`.  The unrestricted bounded common zero remains `OPEN`.
A post-promotion package rail, including the three new relock tests, then
passed 1,258 tests with one declared skip in 788.975 seconds (2,620,784 KB
maximum RSS).

## Fail-closed exclusions

Two opt-in replays are not certified by this result.  The complete covariant
proof replay and the direct axial quadratic-source producer each timed out at
approximately one hour.  Neither belongs to the declared 1,255-test rail.
The axial certificate received only a provenance-hash repair, after which its
fast three-test certificate rail passed.  A timeout is retained as a non-pass.

This receipt does not establish infinite-harmonic, Sobolev, causal/retarded,
all-orders, final-residual, scattering, particle, positivity, or quantum
claims.

CLOSE-OUT: CERTIFIED — unified provenance relock and finite-support theorem lifecycle promotion
EVIDENCE: EINSTEIN_MAXWELL_WEYL_FINITE_HARMONIC_TIER3_PROVENANCE_RELOCK_V1
