# The complete k=0 oscillator-pair census misses every minus shell

For an `ell=1` oscillator times a generic `ell` oscillator, angular selection
restricts the target to `L=ell-1,ell,ell+1`.  Writing each generic frequency as
`ell+u`, the certified branch intervals reduce a collision to an integer
`D=L-ell` in `{-1,0,1}`.

Exact rational enclosures

```text
23/20 < 2/sqrt(3) < 7/6
```

show that every sum/difference interval misses those integers.  The only two
candidate intervals are exceptional-dipole plus/minus an Einstein-minus mode
at `D=+/-1`; both are excluded by the strict adjacent-minus-gap bound.  The
single reversed-order case `2-omega_minus(2)` lies below the lowest target.

Together with the equal- and distinct-`ell` generic-pair theorems and the
dipole audit, every quadratic pair in the certified `k=0` oscillator inventory
misses every generic Einstein-minus target shell.
