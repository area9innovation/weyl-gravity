# Complete k=0 bounded no-go over the exceptional ellipse

On the declared carrier, every standard generalized-zero datum, every finite
physical/extra/Einstein-plus `k=0` oscillator sum, and a smooth Wiener--Bohr
Einstein-minus completion are included.

Bounded global consistency removes `b` and twist velocity `B`.  Every surviving
nonminus block lies on the nonpositive side of the Hamiltonian moment map, so
a common zero over a nonzero ellipse point requires at least one Einstein-minus
coefficient.  The complete oscillator-pair census proves that no quadratic
oscillator pair lands on a generic minus shell; electric, Wilson,
circumference and constant-twist columns have zero relevant adjoint component.

After the triangular `a` step, each occupied minus shell therefore contains
only

```text
d C_parity(lambda) c_(ell,m,parity).
```

Both pivots are nonzero and every ellipse point has `d!=0`, forcing all minus
coefficients to vanish—the opposite of the Taub requirement.  Hence the
bounded smooth uniformly almost-periodic tangent cone has empty intersection
with this complete `k=0` carrier over the nonzero ellipse.  Nonzero momentum,
maximal Sobolev completion, infinite secular inversion and causal propagation
remain separate open problems.
