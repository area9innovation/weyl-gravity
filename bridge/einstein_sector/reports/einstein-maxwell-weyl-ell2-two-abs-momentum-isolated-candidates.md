# The first two-|k| source gate has 21 exact rows

The 198-row identity audit can be filtered completely without floating-point
sign decisions.  Exactly 21 rows have positive circumference parameter
`rho=(2*pi/L)^2`, all at distinct algebraic values.  The exact sign of the
unsquared shell equation assigns each retained row to its temporal sum or
difference channel and removes squared spurious roots.

Therefore generic circumference has no nonzero-frequency cross-fibre
resonance on the complete `ell=2`, `|n|=1,2` primary carrier.  At exceptional
circumference the remaining direct workload is precisely the 21-row ledger:
apply parity selection and compute each reduced adjoint Weyl--Maxwell source
coefficient.  This does not yet classify the two-fibre tangent cone.
