# Circumference velocity over the twist--ell=2 cone

Adjoining the circumference-velocity Jordan tangent `d` produces a stratified
bounded cone, not another spectator product.

```text
wave-free: c,d,W_x,A arbitrary, B=0;
nonzero wave: d=B=0; c,W_x,A arbitrary; H=J_i=0.
```

On the predecessor cone every old bounded adjoint pairing already vanishes.
The exact Hamiltonian equation shows that any nonzero wave has nonzero
Einstein-minus occupation.  Direct axial and polar `m=0` source pivots make the
`d`-times-minus map nonzero, and `SO(3)` multiplicity one promotes it to an
injective scalar map on every `m` in each parity.  The two parity outputs cannot
cancel.  Hence every nonzero wave forces `d=0`.

Sufficiency is immediate on the two strata: the wave-free stratum is the exact
standard static branch, while the wave stratum reduces to the certified
circumference/Wilson spectator product.  Radion and electric tangents, other
harmonics and momenta, unrestricted secular corrections, causal propagation,
all-orders solutions, residual observables and quantum states remain open.
