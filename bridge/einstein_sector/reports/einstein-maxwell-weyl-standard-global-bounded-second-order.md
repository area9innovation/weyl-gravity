# Standard generalized-zero bounded cone

The complete standard generalized-zero carrier consists of the homogeneous
coordinates `(a,b,c,d,Q_e,W_x)` and the axial twist position/velocity vectors
`(A,B)`.  Its positive-degree quadratic source ideal is triangular.  The
homogeneous `E11` coefficient `(15/2)b^2 t^2` forces `b=0`; the polar `L=2`
coefficient `STF(B tensor B)t^2` forces `B=0`; and the only remaining
positive-degree coefficient is `Q_e*a*t`.

Intersecting this polynomial zero locus with the five Taub moment maps gives

```text
a=b=Q_e=0,  B=0,
c,d,W_x arbitrary,  A in R^3 arbitrary.
```

The surviving source has a bounded correction.  The homogeneous `c,d,W_x`
source vanishes, while constant `A` is the tangent to the exact lifted
mapping-torus family and has the certified constant polar `L=2` correction.

The polynomial elimination is also universal in the complete finite-support
carrier: bounded oscillatory products cannot cancel the zero-frequency
homogeneous `b^2 t^2`, twist `STF(B tensor B)t^2`, or Maxwell `Q_e*a*t`
blocks.  Thus every point of the still-open complete bounded cone already
satisfies `b=B=0` and `Q_e*a=0`.  The remaining positive-degree gate is `a`
and `d` crossed with oscillators; the repaired zero-momentum polar `d` column
already supplies one nonzero `P_(j,1)` coefficient.
Electric-duality transport and flat Wilson data are treated separately;
Static circumference `c`, the polynomial-compatible `d` face, and constant
twist position then enter the shell-resonance ledger.

No complete finite-support bounded, causal, all-orders, residual,
observational, particle, or quantum theorem is claimed here.

The generated theorem is
`bridge/certificates/einstein_maxwell_weyl_standard_global_bounded_second_order.json`.
Hashes, exact commands, resource measurements and skipped higher tiers are in
`bridge/einstein_sector/receipts/EINSTEIN_MAXWELL_WEYL_STANDARD_GLOBAL_BOUNDED_V1_TIER_RECEIPT.json`.
