# Zero axial-twist Einstein--Weyl solution cofiber

The complete exceptional `ell=1,k=0` target decomposition supplies the exact
spectral projector

`P_twist=3*(x-4/3)*(x-4)/16`, with `x=omega^2`.

It is one on the generalized-zero twist primary and zero on the extra and
standard primaries.  The source and target twist coordinates are the same
three real position/velocity pairs `(A_m,B_m)`, hence the twist solution
cofiber is zero.

The action-derived forms still differ: on every pair the target form is
`-2` times the Einstein--Maxwell form.  The theorem remains
`ONSHELL_MAP_ONLY`; the off-shell twist complex, finite holonomy-moduli
quotient and final residual descent remain open.
