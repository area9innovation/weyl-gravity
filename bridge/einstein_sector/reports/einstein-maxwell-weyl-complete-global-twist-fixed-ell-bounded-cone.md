# Complete standard-global plus one-fixed-ell bounded cone

For every one fixed `ell>=2` at `k=0`, the exact bounded second-order cone is

```text
wave=0:  a=b=Q_e=B=0; c,d,W_x,A arbitrary,
wave!=0: a=b=d=Q_e=B=0; c,W_x,A arbitrary; mu_H=mu_J_i=0.
```

This is an append-only successor to the historical partial global theorem.
Keeping it separate avoids a certificate cycle: the fixed-ell twist
factorization already imports that historical global subcone.  The successor
retains its generic `a,b,d` pivots, universal `B=0` condition and independent
`E11=Q_e^2/2` electric witness, replacing only the obsolete `A=0` wave
restriction with the complete fixed-ell twist product theorem.

Quadratic sufficiency follows from the certified wave self-products,
`A`--wave inverses, bounded `c` transport, zero `W_x` source and the standard
generalized-zero theorem.  Finite multi-ell sums, nonzero momentum,
exceptional waves and causal/higher lifecycles remain fail-closed.
