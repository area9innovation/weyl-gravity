# Complete global/twist plus ell=2 bounded cone

The bounded second-order cone is now complete for the compact
Plebański–Hacyan carrier containing every standard homogeneous coordinate
`(a,b,c,d,Q_e,W_x)`, the full twist pair `(A,B)`, and every axial/polar
`ell=2,k=0` Einstein and extra-primary wave coefficient.

It is the stratified union

```text
wave = 0:
  a=b=Q_e=B=0; c,d,W_x,A arbitrary,

wave != 0:
  a=b=d=Q_e=B=0; c,W_x,A arbitrary; H=J_i=0.
```

The radion and electric gates close by different mechanisms. Every nonzero
total moment-map zero contains an Einstein-minus coefficient. The exact axial
and polar minus-source ideals contain `a*z`, and `SO(3)` multiplicity one
promotes the nonzero `m=0` pivot to every `m`, forcing `a=0`.

For the electric tangent, electromagnetic duality makes every
`Q_e`-times-oscillator source a bounded linear image, but that is not the
decisive zero-frequency test. Direct substitution into the homogeneous source
gives

```text
(E00,E11,sphere,Maxwell1)
  = Q_e^2*(-1/2,1/2,-1/2,0).
```

The wave scalar source has row direction `(1,0,1/2,0)` and cancels with the
Hamiltonian moment map. It therefore cannot cancel `E11=Q_e^2/2`. The bounded
homogeneous operator has zero constant-frequency image, so real bounded
solvability forces `Q_e=0`.

Sufficiency then reduces to the already certified standard static theorem on
the wave-free stratum and the circumference-velocity predecessor theorem on
the nonzero-wave stratum. Other angular blocks, nonzero compact momentum,
unrestricted secular corrections, causal/retarded propagation, all-orders
integration, residual observables and quantum transfer remain fail-closed.
