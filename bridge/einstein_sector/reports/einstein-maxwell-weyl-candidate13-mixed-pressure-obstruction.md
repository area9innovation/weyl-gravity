# Candidate-13 mixed pressure obstruction

The declared `ell=2`, axial, `m=0` mixed tangent contains one normalized
extra `p`-primary mode on the `n=1` fibre and normalized Einstein `q_minus`
modes on the `n=1` and `n=-2` fibres.  Its exact occupations make all five
compact-stabilizer moment maps vanish.  The extra occupation on `n=-2` is
zero, so all eighteen candidate-13 cross-fibre resonance coefficients vanish;
the complete same-fibre census places every nonzero-frequency output off
shell.

The bounded zero-frequency block has an additional action-derived pairing.
For a simple primary with normalized current `h`, differentiation with
respect to the constant circle-radius direction gives

```text
R_c(u) = (1/2) sum_j k_j^2 h_j.
```

On the exact witness this reduces to
`R_c=(rho/2)(1-y1-4y2)<0`: `rho>0`, `p1>q1`, hence `y1>1`, while `y2>0`.
A nonzero constant circle-pressure source is outside the bounded or finite-
quasiperiodic image of the homogeneous zero-frequency operator.  The witness
is therefore `OBSTRUCTED` in that correction class.

The smooth exponential-polynomial verdict is different.  The complete smooth
finite-support theorem says that only the five stabilizer pairings persist;
the pressure row and finite-frequency resonances admit secular inverses.
Consequently this same witness has a `CERTIFIED` smooth second-order
correction.  The certificate does not assert a componentwise normalization
for the pressure primitive.

This is an independence theorem:

```text
mu_X(u)=0 and R_13,j(u)=0 for every X and j, but R_c(u) != 0.
```

It classifies one exact mixed tangent, not the full candidate-13 bounded cone.
Completeness of the bounded zero-frequency functional ledger, all-orders
integration, causal correction, residual observables, and quantum claims
remain `OPEN` or `NO_CERTIFIED_MAP`.
