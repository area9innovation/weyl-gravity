# Twist-aligned opposite-momentum resonance gate

The nonzero-momentum twist theorem forces bounded travelling waves onto the
`m_A=0` face.  That condition does not remove the independent standing-wave
phase divisor.

For every `ell>=2`, set

```text
k^2 = sqrt(2*ell*(ell+1)) - ell/2 - 1/6 > 0.
```

Choose the circle circumference so this is an allowed compact momentum.  Put
equal `+k/-k` rank-one densities on the `m_A=0` Einstein-plus branch and the
energy-balancing Einstein-minus branch.  Then all five compact moment maps
vanish, and the already certified constant-twist-times-wave column has a
bounded correction.

Nevertheless the two positive-frequency Einstein-minus coefficients at
opposite momenta satisfy

```text
K=0,  Omega=2*omega_minus,
4*omega_minus^2 = Lambda_(2ell)-2/3.
```

They therefore populate the polar extra `p` shell at `L=2ell,M=0`; the top
Gaunt coefficient is nonzero.  This establishes that a phase-sensitive
resonant functional remains necessary after both moment-map and twist
alignment descent.

It does not establish an obstruction.  The actual Weyl--Maxwell adjoint
source coefficient could still vanish dynamically.  The next calculation is
the exact `ell=2`, `k^2=2*sqrt(3)-7/6` projection into the polar `L=4`
extra-primary adjoint.  Smooth exponential-polynomial extension is already
certified; bounded extension and causal propagation remain open.
