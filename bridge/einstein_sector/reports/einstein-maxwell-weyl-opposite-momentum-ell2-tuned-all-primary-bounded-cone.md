# Complete tuned all-primary axisymmetric bounded cone

Adjoining the full axisymmetric extra `p`-primary multiplicity creates no new
bounded shell collision at the tuned `ell=2` fibre.  An exact over-complete
140-row census across `L=1,2,3,4`, both output momenta and every zero,
sum and difference frequency still finds only

```text
L=4, K=0, Omega=2omega_-, p-primary,
```

which is generated solely by the two `q_-` inputs.  The resonance zero set
therefore remains the two mixed sheets

```text
a_+ = sigma sqrt(3) p_+,
a_- = sigma sqrt(3) p_-,
sigma = +/-1,
```

plus two one-sided planes excluded by compact-momentum balance.

The extra branch does widen the allowed imbalance.  Let
`r_e=omega_-/omega_e`, where `omega_-<omega_e<omega_+`.  Nonnegative positive-
branch occupations exist exactly for

```text
(1-r_e)/(1+r_e) <= |p_+|^2/|p_-|^2 <= (1+r_e)/(1-r_e).
```

The complete balancing fibre is the convex polytope of nonnegative extra and
Einstein-plus occupations satisfying the two Hamiltonian/momentum equations.
An extra-only balance is an explicit sufficiency witness.  Thus this is a
necessity-and-sufficiency theorem for the tuned axisymmetric all-primary
carrier, while other harmonics, momenta, all-orders and causal/quantum claims
remain open.
