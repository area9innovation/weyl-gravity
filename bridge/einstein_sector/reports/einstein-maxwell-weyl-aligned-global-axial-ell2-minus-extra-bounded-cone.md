# Aligned global plus axial ell=2 bounded cone

The first opposite-sign enlargement is nonempty but sharply stratified.  On
the axial `ell=2,m=0,k=0` carrier containing Einstein-minus and both extra
primaries, every nonzero bounded wave forces the generalized homogeneous
coordinates `a=b=d=0`.  The universal global polynomial conditions also give
`B_z=0`.

Electric charge cannot provide the remaining negative occupation in the
bounded category.  After the Hamiltonian row is cancelled, its homogeneous
zero-frequency source retains `E11=Q_e^2/2`; the constant-frequency
homogeneous operator has zero bounded image.  Thus `Q_e=0` independently.

The complete declared cone is the union of:

- the static standard branch `(c,d,W_x,A_z)`; and
- the wave branch with arbitrary `(c,W_x,A_z)`, arbitrary relative phases,
  and

  `x_minus=(972*x_e1+52*x_e2)/(27*(-6+5*sqrt(3)))`.

The wave branch is sufficient by the certified axial full-extra correction,
bounded `k=0` circumference transport, the vanishing Wilson cross source and
exact-family transport of the aligned constant twist holonomy.  Polar input,
all `m`, other harmonics and momenta remain open.
