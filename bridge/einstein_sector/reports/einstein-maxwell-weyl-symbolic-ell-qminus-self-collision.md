# Symbolic-ell tuned Einstein-minus self-collision theorem

For every integer `ell>=2`, tune the allowed compact momentum to

```text
k^2 = sqrt(2*ell*(ell+1)) - ell/2 - 1/6.
```

The complete characteristic-shell census of an Einstein-minus primary with
itself has exactly one nonzero-frequency hit:

```text
L=2*ell, K=0, Omega=2*omega_minus, polar extra p-primary.
```

The proof is symbolic.  At zero output momentum the extra-shell offset is
exactly `L=2*ell`, and strict shell ordering excludes every Einstein target.
At doubled output momentum, irrationality excludes the generic case.  When
the square roots happen to be integral, a putative Einstein hit reduces to

```text
(2*s-2-(u+epsilon))*(2*s-2+(u+epsilon)) = 3.
```

The only positive integer factorization gives `s=2`, hence `ell=1`, outside
the generic domain.  Zero-difference outputs have nonpositive reduced shell
coordinate and cannot collide with a physical `L>=1` shell.  The separate
nonzero-Fourier scalar complex is exact.

This closes the arithmetic carrier but not the bounded theorem.  The polar
`L=2*ell` dynamical adjoint source coefficient remains `OPEN`; smooth secular
extension is `CERTIFIED`, while causal/retarded transport remains
`NO_CERTIFIED_MAP`.
