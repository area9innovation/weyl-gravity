# Complete bounded zero-frequency receiver at candidate 13

For the finite generic candidate-13 carrier, equal-branch products with their
reality conjugates produce the only `K=Omega=0` wave source.  After local
gauge and Noether descent, its complete bounded receiver is

```text
span{mu_H, mu_Px, mu_J1, mu_J2, mu_J3, R_c},

R_c = (1/2) sum_j k_j^2 h_j.
```

The scalar homogeneous rows are exhausted as follows.  `E00` and `E01` are
the calibrated Hamiltonian and compact-momentum Taub pairings.  `E11` is the
action-derived circle pressure.  Weyl tracelessness fixes the sphere-trace
row.  The closed-slice Gauss identity removes `Maxwell0`, while the integrated
Maxwell-x row has the factor `omega_1+omega_2` and vanishes on a zero-frequency
conjugate pair.

The axial `L=1` quotient contributes precisely the three lifted rotations;
the polar `L=1` quotient contributes none.  Every static `L>=2` block is
invertible after local gauge reduction.  Hence a bounded zero-frequency
correction exists if and only if all six displayed functionals vanish.

This theorem is restricted to the declared finite generic candidate-13
carrier.  It does not classify its eighteen nonzero-frequency collision
coefficients, exceptional/global inputs, the real quadratic zero locus,
all-orders integration, causal propagation, residual observables, or quantum
states.
