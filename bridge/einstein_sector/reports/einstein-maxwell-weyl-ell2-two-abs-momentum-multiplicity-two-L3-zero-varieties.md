# Multiplicity-two source L3 zero varieties

Candidates 6, 10 and 18 are the remaining `L=3` fibres.  Each has one
two-dimensional `p_extra` source multiplicity.  Exact algebraic-number
reduction proves that its two coefficient rows are related by the real
factors `-24 sqrt(2)` and `-8 sqrt(2)`; rational interval witnesses fix the
negative signs independently of the squared identities.

One internal functional in each parity is active and its one-dimensional
kernel supplies a spectator binary quartic.  The active parity pencil has
`lambda^2=384` and diagonalizes into two first-transvectant equations.  Their
zero set is the product of two six-dimensional rank-one determinantal cones;
including the ten spectator coordinates gives one irreducible complex
dimension-22 variety in ambient dimension 30 for each physical fibre.

The three circumference backgrounds remain distinct.  Same-fibre sources,
Taub intersections, bounded and smooth-secular correction classes, causal
maps, residual observables and quantum interpretations remain fail-closed.
