# Tuned mixed-parity bounded second-order extension

The tuned `ell=2` opposite-momentum fixture now has one certified bounded
second-order jet.  Choose the two Einstein-minus polar coefficients as
`p_+=p_-=1` and the axial coefficients as `a_+=a_-=sqrt(3)`.  Add equal
`+k/-k` axisymmetric Einstein-plus occupation to cancel the negative
Einstein-minus Hamiltonian moment map.  Compact momentum and all three
rotation moment maps then vanish.

An exact collision census deliberately retains the over-complete angular
range `L=1,2,3,4`.  It tests 80 exceptional, `p`-primary and `q`-primary
shell residuals over

```text
K = 0, +/-2k,
Omega = 0, 2omega_-, 2omega_+, omega_+ +/- omega_-.
```

There is exactly one nonzero-frequency collision:

```text
L=4, K=0, Omega=2omega_-, p-primary.
```

The direct parity-resonance matrix has two adjoint rows on this block.  Both
vanish on the declared `a=sqrt(3)p` face.  The zero block is exhausted by the
five vanishing stabilizer moment maps, the exceptional `L=0` nonzero-Fourier
complex is exact, and the constant-twist cross column is bounded on `m_A=0`.
Every remaining finite source block therefore has a bounded harmonic inverse.

This proves one bounded finite-quasiperiodic second-order correction.  It does
not classify the full bounded cone, other `ell` or momentum fibres, all-orders
integration, causal propagation, residual particles or quantum theory.
