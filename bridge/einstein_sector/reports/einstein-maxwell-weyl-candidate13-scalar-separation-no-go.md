# Candidate-13 bounded scalar-separation no-go

On the complete generic candidate-13 two-fibre carrier, define

```text
D = -(8/(5L)) mu_H -(3/(2L sqrt(rho))) mu_Px -(2/rho) R_c.
```

Using the action-derived current normalization, this is a sum of absolute
current occupations.  Its coefficients on the `q-minus`, `p-extra`, and
`q-plus` branches at `n=1,-2` are all strictly positive.  Exact rational
frequency bounds give the four independent lower bounds

```text
379/1000, 101/250, 23/80, 38/45.
```

The `q-plus` coefficients are larger than the corresponding `p` coefficients.
The proof uses only the exact candidate-13 interval `1/2<rho<3/5`, current
orthogonality, and the parity-independent branch signs.

Consequently

```text
{mu_H=mu_Px=R_c=0} = {0}
```

on the complete declared generic carrier.  Since these three functionals are
necessary for bounded inversion, the entire candidate-13 bounded cone is the
origin; the rotations and eighteen resonance equations are automatically zero
there.  The smooth exponential-polynomial cone remains nontrivial because
`R_c` has a secular inverse in that correction class.

Exceptional/global inputs, all-orders integration, causal propagation,
residual observables, and quantum claims are excluded.
