# Exceptional arbitrary-\(k\) locked resonance

## Result

The authoritative sign--resonance join was imported at exact SHA-256
`63b605c2441e9cf9d38e8b542641f060cc1a240de0af377cee57ebb0c0b848fd`.

The exceptional additional-Weyl dipole has invariant mass squared `4/3`.
The generic polar `ell=2` extra primary has invariant mass squared `16/3`.
Consequently, for every allowed compact momentum `k=2*pi*n/L`,

```text
(omega_2,2k) = 2*(omega_e,k),
omega_e(k)^2 = k^2+4/3.
```

The `2k` mode lies on the same circle lattice; no circumference background is
changed or identified.

The certified rest-frame exceptional-by-`ell=2` difference matrix can be
transported with one proper `SO(1,1)` transformation acting simultaneously on
input polarizations, equation rows and adjoint covectors.  Naturality of the
local Weyl--Maxwell operator makes the bilinear source map intertwine these
invertible transformations.  In the transported normalization the complete
axisymmetric eight-column matrix therefore has the same six zero columns and
two nonzero rows:

```text
R_ax(k)  = -(768/5) conj(x_exceptional_axial(k))
                    y_boosted_extra_polar_e2(2k),
R_pol(k) = -(864/5) conj(x_exceptional_polar(k))
                    y_boosted_extra_polar_e2(2k).
```

The output has `(L,K,Omega)=(1,k,omega_e(k))`, because subtracting the
exceptional four-momentum from the locked generic four-momentum returns the
exceptional shell exactly.

The boost is not claimed to be a global automorphism of the compact quotient.
It is an algebraic tensor transport used to construct periodic coefficient
representatives at the already allowed momenta `k` and `2k`.

## Interpretation

This is the first exact arbitrary-\(k\), cross-absolute-momentum resonance row
in the exceptional completion.  It proves that the `k` and `2k` fibres cannot
be joined merely by adding their Taub moment maps: a separate characteristic
shell functional survives.

On the locked two-mode carrier, the displayed monomials completely describe
this difference-channel projection.  On the enlarged carrier, other
exceptional, generic and global pairs can share the same output labels.
Their simultaneous cancellation geometry remains open.

## Coverage and open rows

Already certified for every exceptional oscillator and every allowed `k`:

- the circumference `c` column;
- electric `Q_e` transport;
- the identically zero Wilson `W_x` column.

Still open:

- arbitrary-\(k\) radion/circumference-velocity `a,d` columns;
- constant twist-position and twist-velocity columns;
- all other exceptional-by-generic branch and momentum pairs;
- the all-\(m\) tensor assembly;
- the full multiple-`|k|` common zero locus.

The atlas fragment is fail-closed: the locked resonance is `CERTIFIED`, the
bounded enlarged cone is `OPEN`, and causal, observer and quantum maps are
`NO_CERTIFIED_MAP`.

CLOSE-OUT: SHORTFALL — useful work landed, but the stop condition is incomplete
EVIDENCE: EINSTEIN_MAXWELL_WEYL_EXCEPTIONAL_ARBITRARY_K_LOCKED_RESONANCE_V1
