# Multi-ell Einstein-minus global pivot fixtures

The direct four-dimensional `a,b,d` cross-source replay now accepts every
physical generic harmonic degree.  The certified fixture ledger contains
complete axial and polar triangular pivots at `ell=2,3`, plus the leading
`b` pivot at `ell=4`.

With `lambda=ell(ell+1)` and
`omega_minus^2=lambda-sqrt(2*lambda)`, the samples reconstruct

```text
C_A=3*i*omega_minus*(1-3*sqrt(2*lambda)),
C_P=lambda^2*(2*lambda-1)/6.
```

The axial `(a,b,d)` pivots are `(2*C_A,C_A,C_A)` at polynomial degrees
`(1,2,0)`.  The polar pivots are `(3*C_P,C_P,3*C_P)` at degrees `(2,3,1)`.
The exact physical values of the polar leading pivot are `66`, `552`, and
`2600` for `ell=2,3,4`.

These candidates are nonzero for every physical `lambda>=6`, but the result
is intentionally fixture-only.  A symbolic natural-operator
functional-form or degree bound is still required before the candidate laws
can be promoted to a general-`ell` bounded-cone theorem.  Nonzero momentum,
cross-harmonic sums, causal propagation, all-orders integration, residual
descent, observables and quantum theory remain outside the claim.
