# Symbolic-ell standard-branch collision census

For every integer `ell>=2` on the tuned opposite-momentum fibre, all
q-minus/q-plus self and cross products have been compared with every target
shell through `L=2*ell`.

The q-plus self sums lie above the largest target shell.  For the mixed
q-minus/q-plus sum, the `K=2k` reduced coordinate lies strictly between
`P(2*ell)` and `Q_plus(2*ell)`, while the `K=0` coordinate lies above the top
q-plus shell.  The mixed difference obeys

```text
4/3 < (omega_plus-omega_minus)^2 < 2,
```

and its `K=2k` reduction is negative.  It therefore misses both exceptional
`L=1` frequencies (`4/3`, `4`) and every generic shell.

Consequently the sole nonzero-frequency standard-branch collision is still
the q-minus self-sum into the polar p-primary `L=2*ell` shell.  The symbolic
two-parity matrix already classifies and cancels that shell on two mixed
null sheets.

This closes dispersion arithmetic only.  The next bounded gate is the
zero-frequency and compatible-source join on those sheets.  Extra-primary
inputs, multiple absolute momenta, causal transport, residual descent,
observational claims and quantum claims remain outside scope.
