# Candidate-13 mixed bounded second-order extension

The exact candidate-13 witness contains one axial `p`-primary occupation on
the `n=1` fibre and axial Einstein `q_minus` occupations on the `n=1` and
`n=-2` fibres.  Its five stabilizer moment maps vanish, and the candidate-13
cross-fibre `L=4` source vanishes because the `p` occupation on the second
fibre is zero.

The remaining bounded extension gate is complete by composition of certified
operators.  The same-fibre census excludes every nonzero-frequency target
shell.  At zero frequency, the complete generic finite-harmonic cokernel is
exactly the span of `H`, `P_x`, and the three lifted rotations; all five
pairings vanish on the witness.  Static generic outputs with `L>=2` are
invertible after local gauge reduction.  The isolated cross-fibre ledger
shows that no other collision shares the candidate-13 circumference.

Consequently every quadratic source block lies in the bounded reduced image.
A real, smooth, spatially periodic, finite-quasiperiodic second-order
correction is obtained blockwise from the certified algebraic and zero-block
right inverses and their complex conjugates.

This establishes one mixed Einstein-extra second-order jet, not the full
candidate-13 tangent cone.  Arbitrary phases and polar amplitudes, all-orders
integration, causal correction, residual observables, and quantum claims
remain open or `NO_CERTIFIED_MAP`.
