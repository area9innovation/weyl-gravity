# Factorized cross-fibre amplitude system

The complete 164-coefficient branch-basis ledger is now lifted to arbitrary
complex positive-frequency amplitudes and every magnetic component.  Exact
algebraic comparison keeps the twenty-one tuned circumference values as
twenty-one separate physical fibres: three `L=1`, six `L=3` and twelve `L=4`
collision backgrounds.

The `L=4` axisymmetric coefficients are divided by their common exact
`P_2 x P_2 -> P_4` conversion factor.  The odd-`L` coefficients retain their
declared fixed target-coordinate phases.  Exact normalized
Clebsch--Gordan maps then produce 418 complex scalar magnetic equations from
54 target-parity/adjoint equations, 128 ordered branch-basis fixtures and the
164 reduced internal coefficients (162 nonzero and two zero components inside
otherwise nonzero cokernel vectors).  Negative-frequency equations are the
complex conjugates for a real tangent.

Every fibrewise system is bilinear, so the plane where the first momentum
fibre vanishes and the plane where the second momentum fibre vanishes are
mandatory components of its zero locus.  The nontrivial mixed components are
not yet irreducibly decomposed.  Same-fibre quadratic sources and the five
Taub moment maps must also be joined before any complete two-fibre tangent
cone can be claimed.  Smooth-secular and causal correction classes remain
fail-closed.
