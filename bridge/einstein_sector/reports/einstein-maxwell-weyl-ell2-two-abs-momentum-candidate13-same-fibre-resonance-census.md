# Candidate-13 same-fibre resonance census

The mixed candidate-13 carrier may contain the Einstein `q_minus`, extra
`p_extra` and Einstein `q_plus` branches on each of the two compact momentum
fibres `|n|=1,2`.  Before computing new quadratic source matrices, the exact
shell census removes every nonzero-frequency same-fibre channel.

For each fibre, all six unordered branch pairs contribute a positive-positive
sum and the three unequal pairs also contribute a positive-negative
difference.  This gives 18 temporal channels.  The certificate checks each
against both exceptional `ell=1` shells and the `p` and `q` shell polynomials
for `ell=2,3,4`: 144 exact algebraic defects, all isolated away from zero by
rational intervals.  The `ell=0` positive-positive sums have nonzero output
Fourier momentum and use the empty nonzero-Fourier quotient.  Unequal-branch
differences instead have `K=0,Omega!=0`; these use the separate homogeneous
nonzero-frequency theorem, whose physical quotient is also empty.  Keeping
those two cases separate closes a coverage gap that a nonzero-Fourier theorem
alone would not address.

Thus no nonzero-frequency same-fibre source projection is needed for the
bounded candidate-13 gate.  The remaining calculation is sharply localized:
equal-branch products with their reality conjugates have `(K,Omega)=(0,0)`.
Their homogeneous and twist adjoint-cokernel rows must be restricted to the
mixed Einstein-extra carrier and joined to all five stabilizer moment maps.

This is shell arithmetic, not the mixed tangent-cone theorem.  Zero-frequency
source coefficients, bounded and smooth-secular verdicts, residual descent,
causal corrections, observables and quantum states remain fail-closed.
