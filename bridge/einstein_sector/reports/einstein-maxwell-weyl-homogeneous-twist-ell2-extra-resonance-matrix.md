# Complete homogeneous/twist times `ell=2` extra resonance matrix

The non-axisymmetric channel `m_twist=1,m_extra=0 -> M=1` has normalized
Clebsch--Gordan coefficient `sqrt(2)/2`.  Because
`Hom_SO3(V1 tensor V2,V2)` is one-dimensional, this fixture fixes the full
angular map.

After projection onto the two axial and two polar extra-shell adjoint rows,
the twist-position matrix has rank two.  The twist-velocity matrix has

`det M_B(t)=4129056*(72*t^2+34*sqrt(3)*I*t+3)`,

which never vanishes for real `t`; its pointwise rank is four.  The twist
columns are therefore genuine resonance data, not spectators.

Together with the imported rank-three `a,b,d` polynomial chains and the
certified removable `c,W_x,Q_e` columns, this completes the declared
homogeneous/twist bounded-resonance source matrix.  The simultaneous
stabilizer-plus-resonance zero locus, smooth-secular sufficiency, other
momentum fibres and causal/retarded corrections remain open.
