# Full-time repair of the circumference-velocity column

The direct `d` source fixtures used by the earlier adjoint-isomorphism theorem
set `t=0`.  They correctly classify the constant shell projection, but they
cannot establish that the full source is bounded.

Locality reconstructs the omitted coefficient without replaying the expensive
four-dimensional tensor calculation:

```text
S[d*t,u](t)=t*S[c,u](t)+S[d*t,u](0).
```

The coefficient of `t` is exactly the already certified static-c source.
For the two axial extra representatives it vanishes.  It also vanishes for
the first polar representative.  For the second polar representative it is
the nonzero full eight-row vector

```text
(-36, 0, 164, -8*sqrt(3)*i, 0, -100, -24, -20).
```

Hence the isolated polynomial zero locus is `d*z_polar_e2=0`.  The old
four-column adjoint matrix remains an invertible statement about the constant
resonant projection, but it is not a complete bounded-column theorem.  The
polar control direction cannot be used until its polynomial coefficient is
canceled jointly with the `a` and other same-channel columns.

The simultaneous bounded cone, other momenta and harmonics, causal transport,
all-orders integration, residual descent and quantum claims remain open.
