# Exceptional ellipse: bounded obstruction after Einstein-minus balance

The Einstein-minus enlargement passes the five Taub constraints and its
complete zero-frequency source cancels.  It nevertheless fails the bounded
second-order equation.

The exceptional resonance ellipse requires `d!=0`, while charge balance
requires a nonzero axial `ell=2` Einstein-minus coefficient `A_-`.  Their
quadratic product lies on the same axial `L=2` Einstein-minus shell and has
the exact adjoint pairing

```text
12*i*(3*sqrt(3)-1)*sqrt(6-2*sqrt(3))*d*A_- != 0.
```

No other occupied input pair shares this carrier.  The pre-existing exact
shell ideal `<b*z,a*z,d*z>` therefore applies directly: on a nonzero minus
wave, bounded compatibility forces `d=0`, contradicting the ellipse domain.

Consequently this explicit stabilizer-balanced tangent is `OBSTRUCTED` for
bounded or finite-quasiperiodic corrections.  It remains `CERTIFIED` for
smooth exponential-polynomial corrections, where the resonant inverse is
allowed to contain a secular term.  This is one endpoint obstruction, not a
classification of the full exceptional mixed zero locus.
