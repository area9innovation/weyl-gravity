# Complete fixed-ell constant-twist bounded cone

For every one fixed integer `ell>=2` at `k=0`, constant twist position is a
free bounded spectator over the complete axial/polar `q/p` wave moment cone:

```text
Z2_bounded(A,wave)
  = R_A^3 x {wave : mu_H=mu_J1=mu_J2=mu_J3=0}.
```

The same-shell map vanishes by the flat-connection Feynman--Hellmann theorem.
The only other mixed outputs lie at `L=ell-1,ell+1`.  On each physical axial
and polar target fibre the reduced determinant is a nonzero scalar times
`p_L^2 q_L`.  Exact substitution of the input `p_ell` and `q_ell,+/-`
frequencies gives nonzero factors in both neighboring blocks.  The radical
signs reduce to four positive polynomial margins, including

```text
2*(9*ell^2+3*ell+2)/9,
2*(9*ell^2+15*ell+8)/9,
(ell+1)*(ell-1),
ell*(ell+2).
```

The `ell=2 -> L=1` exceptional lower channel is retained through the direct
corrected bounded theorem rather than inferred from a generic physical-ring
specialization.  Twist self-products lie on the exact static holonomy family,
and wave self-products are solved exactly on the common compact stabilizer
zero cone.

Finite sums containing distinct input `ell`, nonzero momentum, causal
propagation, all-orders integration, residual observables and quantum transfer
remain open.
