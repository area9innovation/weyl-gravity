# Complete electric and Wilson oscillator transport

Electromagnetic duality removes the electric-charge cross column from the
bounded oscillator ledger on the complete certified nonzero-frequency
carrier.  For every standard `q`-primary or extra `p`-primary Jacobi field
`(h,f)`, differentiation of

```text
F_theta=cos(theta)F+sin(theta) star_g F
```

gives the mixed correction

```text
h_cross=0,
f_cross=star_bar f+(D_g star)[h]F_bar.
```

It has the same finite quasiperiodic time dependence as the input mode.  Its
two-sphere period vanishes for every `ell>=1`, so it lifts to a global
fixed-bundle connection-difference correction.  Therefore every
`Q_e`-times-oscillator source lies in the bounded image of the linearized
operator.  The direct `ell=2,k=0` extra calculation remains an independent
coefficient-level fixture for this natural identity.

The Wilson coordinate is simpler: `delta A=W_x dx` has zero field strength,
and the local action depends on the connection only through `F=dA`.
Consequently every `W_x` mixed source vanishes.

This does not remove the separate zero-frequency condition `Q_e*a=0` and it
does not solve the complete bounded cone.  After the already universal
`b=B=0` and `Q_e*a=0` conditions, the positive-degree gate is reduced to
`a,c,d` crossed with oscillators.  Constant twist position and bounded wave
products can still contribute shell-resonance functionals.

Causal transport, all-orders fixed-bundle duality, residual descent,
observables and quantum claims remain fail-closed.
