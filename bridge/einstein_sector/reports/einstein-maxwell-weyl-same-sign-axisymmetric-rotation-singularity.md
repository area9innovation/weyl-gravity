# Axisymmetric rotation critical locus

Every nonzero point of the certified scalar-cone amplitude section is a
critical point of the three-component lifted-rotation moment map.  In the
spin-two basis, `T3 e0=0`, while the real `T1` and `T2` covectors at `e0` are
independent.  Because every nonzero section point has at least one occupied
block with nonzero current norm, the full rotation Jacobian has exact rank
two.  It has rank zero at the cone origin.

Thus the axisymmetric section proves real nonemptiness but cannot seed a
codimension-three implicit-function classification.  The missing third
rotation equation begins quadratically in nonaxisymmetric directions.  The
next local gate is its quadratic normal form on the kernel of the two
transverse linear rotation equations.

This is a Jacobian-rank theorem, not a classification of the local real zero
set, tangent cone, connected components, or singular strata.

Verification on 2026-07-19 passed the producer, independent verifier and
three focused tests.  The regenerated Einstein atlas passed its independent
verifier and all 93 atlas tests.  Paper 13 compiled in three `pdflatex` passes
with no undefined references, layout warnings or box warnings.
