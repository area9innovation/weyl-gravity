# Global plus complete ell=2 bounded cone

The bounded classification now includes every `m`, both wave parities, both
Einstein branches and both extra-primary multiplicities at `ell=2,k=0`.

The direct axial and polar `m=0` global-times-Einstein-minus sources promote
to all `m` because the homogeneous inputs are `SO(3)` scalars and
`Hom_SO3(V_2,V_2)` is one-dimensional.  Axial and polar outputs remain in
distinct parity blocks.  Every nonzero wave common zero contains an
Einstein-minus component, so the promoted source pivots force `a=b=d=0`.
Universal boundedness also forces twist velocity `B=0`.

Electric charge is removed by the full homogeneous source rather than by the
Hamiltonian moment map alone: after the wave common-zero equations hold, the
remaining source has `E11=Q_e^2/2`, while the bounded zero-frequency
homogeneous operator has zero image.

Two bounded strata are certified:

- the static standard branch `(c,d,W_x,A)`; and
- `(c,W_x)` times the complete nonzero wave cone
  `mu_H=mu_J1=mu_J2=mu_J3=0` at `A=B=0`.

They cannot be promoted to a product with arbitrary `A`: a perpendicular
constant twist times axial extra `e1` has nonzero adjoint pairing
`24*sqrt(3)`.  The complete nonzero-`A` wave zero locus is open.

The combined axial--polar solve is genuinely bounded.  After the total
rotation moment maps vanish, the only former zero-frequency `L=1`
polynomial/Jordan caveat has compatible source `(S0,S1,S0,S1)` and the
constant correction `(S0/2,-S1/2,0,0)`.  Thus no secular term is needed.

The next gate is the constant-twist position resonance zero locus, followed
by nonzero momentum where the circumference direction has its own bounded
resonance.
