# Global bounded-cone real-locus gate

The complete finite-harmonic bounded obstruction ledger is frozen, but its
unrestricted real common zero is not classified.  The global finite-support
space is a direct union of finite carriers, so a single complex primary
decomposition cannot close the physical problem.

After the certified fixed-occupation closures for candidate 16, candidates
17/20, and the componentwise linear sheets of candidates 19/21, the first
unresolved nonlinear carrier in the declared candidate order is candidate
18.  At fixed positive active occupations its exact carrier is

\[
 \mathbb C^{10}\times
 \operatorname{Rank}_{\leq1}(5\times2;\mathbb C)_+
 \times
 \operatorname{Rank}_{\leq1}(5\times2;\mathbb C)_- .
\]

This is an irreducible complex variety of dimension \(22\) in a
\(30\)-complex-dimensional ambient space.  That complex theorem is already
certified.  The remaining physical gate is different: impose the two
Hermitian norm levels and the three lifted \(SO(3)\) moment maps, retain
conjugate reality, and classify the resulting real semialgebraic fibre modulo
\(U(1)^2\times SO(3)\).

One point in each known singular component is connected through a smooth
central bridge at every positive occupation.  No certificate yet proves that
every component meets that bridge, computes the complete real radical, or
classifies the global orbit quotient.  The atlas therefore remains `OPEN`.

The next exact task is the real-radical/component decomposition of this
60-real-coordinate Hermitian determinantal moment ideal, with all ten
spectator directions retained.
