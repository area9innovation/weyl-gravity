# Twist-aligned opposite-momentum bounded obstruction

The first phase-sensitive resonant functional on the nonzero-momentum
twist-aligned face is nonzero.  At

```text
ell=2,
k^2=2*sqrt(3)-7/6,
omega_-^2=29/6,
(L,M,K,Omega)=(4,0,0,2*omega_-),
```

the direct four-dimensional Weyl--Maxwell quadratic source from the two axial
Einstein-minus `+k/-k` coefficients has action rows

```text
-64*(163+261*sqrt(3))/7,
0,
32*(-21293+9450*sqrt(3))/105,
384*(-137+55*sqrt(3))/7.
```

The polar `L=4` target is on its extra `p` shell.  Its two left-adjoint
vectors are

```text
(0,1,0,0),
(-4/87,0,-40/29,1).
```

Their source pairings are respectively

```text
0,
-1152*(-265+149*sqrt(3))/203.
```

The second value is nonzero because the algebraic norm
`265^2-3*149^2=3622` is nonzero.  No Einstein-plus or twist cross term shares
this output carrier.  Consequently the explicit tangent constructed by the
preceding intersection gate has vanishing five stabilizer moment maps and
solves the complete twist-wave bounded column, but it has no bounded or
finite-quasiperiodic second-order correction.

This does not conflict with the smooth theorem: a finite secular correction
is certified in the smooth exponential-polynomial class.  The result is one
tuned `ell=2` obstruction fixture, not a classification of the full bounded
zero locus, and it makes no causal or quantum claim.
