# Constant-twist position kernel on the ell=2 extra primary

The nonzero constant-twist interaction is not uniformly obstructed.  After
rotating the twist axis to `z`, the unique resonant `V_1 tensor V_2 -> V_2`
intertwiner has Clebsch--Gordan coefficient `-m/sqrt(6)`.  The complete
resonance operator therefore factors as `(A_hat dot J_2) tensor P`, where the
direct four-dimensional source certificate gives `rank(P)=2`.

The multiplicity kernel is

```text
span_C{polar_e1, -4*sqrt(3)*axial_e1+15*polar_e2}.
```

Consequently the positive-frequency kernel has complex dimension twelve:
all four multiplicities survive at `m_A=0`, while precisely the two displayed
multiplicity combinations survive at each `m_A=+/-1,+/-2`.  The perpendicular
`axial_e1` counterexample lies outside this kernel.

This classifies only the bounded resonant projection of constant twist times
the `ell=2,k=0` extra p-primary.  The twist-position matrices on the Einstein
q-primary shells and the simultaneous full tangent cone remain open.
