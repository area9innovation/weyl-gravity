# Exceptional `ell=1` positive-sum resonance census

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

The obstructing channel has `L=2`, `K=0`, and
`Omega=2omega_e`, with `Omega^2=16/3`.

The direct homogeneous operator theorem proves that the `ell=0,k=0` target
has no nonzero-frequency quotient.  Every nonzero `k=0` oscillator therefore
has `omega>=omega_e`.  Hence two nonzero
positive-frequency inputs sum to `2omega_e` only when both are exceptional
dipoles; that case is already closed by the all-`m` STF theorem.

A generalized zero-frequency direction can instead multiply a mode at
`omega^2=16/3`.  The generic extra shell gives

```text
lambda-2/3=16/3  iff  lambda=6  iff  ell=2.
```

No Einstein primary shares this value.  The plus branch is already larger at
`lambda=6`; the minus-branch equation has candidate roots
`(19+-sqrt(105))/3`, one below `6` and the other strictly between the
consecutive physical values `6` and `12`.

Thus, over the complete `k=0` harmonic source spectrum, the unique new
positive-sum gate is the bilinear source between a
standard homogeneous/twist global direction and an axial or polar `ell=2`
extra-primary mode.  That coefficient must be computed next.  Frequency
differences and opposite nonzero momenta remain open.
