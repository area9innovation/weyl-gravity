# Collinear homogeneous/twist common-zero face

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

The complete four-dimensional quadratic source was recomputed for homogeneous
data `(a,c,d)` and one real standard axial `ell=1` twist

```text
A(t)=A+B*t,
3*a^2=4*B^2.
```

It has only homogeneous `L=0`, polar `L=2`, and axial `L=1` components. All
of them are removable. In the axis frame, one correction is

```text
K2 = -(4/9) B^2 t^4,
A_t2 = -(5/6) B^2,
C_t2 = (5/6) B^2 -(2/3)(A+B t)^2,
U2 = -(7/36) B^2,
h_x2 = A a + B a t +(B d/4)t^2 +(B a/6)t^3,
q_x2 = -(B d/4)t^2 -(B a/6)t^3.
```

The circumference-position coordinate `c` is absent from the complete source;
the independent circumference velocity `d` is absorbed by the displayed axial
correction. Hence this family is strictly larger than the time-translation
orbit of the earlier `A=0,d=0` fixture.

The standard twist moment maps require `A cross B=0`. If `B` is nonzero,
`A` and `B` have a common axis; if `B=0`, the balance forces `a=0` and the
remaining `A` can be rotated to the same axis fixture. `SO(3)` covariance and
the residual axial `SO(2)` invariance therefore promote the direct calculation
to the full standard collinear vector face on the declared
`b=Q_e=W_x=0` homogeneous slice.

Physical and exceptional fourth-order `ell=1` inputs, the enlarged homogeneous
slice, all-orders integration, final residual descent, causal scattering, and
quantum claims remain open.
