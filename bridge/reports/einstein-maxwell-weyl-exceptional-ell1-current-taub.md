# Exceptional `ell=1` current and Taub obstruction

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

The axial and polar exceptional `ell=1,k=0` quotients both contain a
fourth-order shell

```text
omega_e^2=4/3.
```

The certified four-dimensional natural Lee--Wald current is polynomial in the
spherical eigenvalue and uses no inverse `lambda-2` factor. Specializing its
exact generic identity to `lambda=2`, after taking the exceptional quotient,
gives the normalized extra Gram matrix

```text
G_X^(ell=1)=diag(16,3)
```

in the axial/polar representative order. A separate direct physical-`ell=1`
fixture fixes the exceptional normalization: its axial and polar weights are
respectively `64=4*16` and `16=16*1`, exactly as dictated by the coordinate
maps from the physical representatives to the gauge-slice representatives.
The standard and extra shells are orthogonal within each parity, and
axial--polar mixing vanishes by parity.

Hence the constant-lapse Taub pairing on the exceptional block is

```text
mu_H=-(L/4)*(4/3)*(16 ||c_ax||^2_W1 + 3 ||c_pol||^2_W1).
```

It is strictly negative away from the origin. Therefore every nonzero real
pure exceptional `ell=1` tangent is obstructed at second order on the closed
fixed-bundle slice. The standard physical `ell=1` block has the same current
sign, so adding physical dipole oscillators cannot cancel this obstruction;
the isolated physical-plus-extra common-zero locus is still only the origin.

This does not remove the certified linear modes. It says that a nonlinear
extension must include an opposite-sign sector, such as an Einstein-minus
branch, already at first order. Mixed opposite-sign fixtures, all-orders
integration, final residual descent, causal radiation, and quantum claims
remain open.
