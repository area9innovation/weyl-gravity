# Axial ell=2 neutral face at second order

Dependency tags: `LOCAL-ALGEBRAIC`, `REDUCED-MODE`.

Paper 91's balanced Einstein-minus/extra ray is a boundary of a larger
second-order-extendible face.  Add the axisymmetric axial Einstein-plus
branch and denote the three raw squared amplitudes by `x_plus`, `x_extra`,
and `x_minus`.

The direct four-dimensional quadratic source gives three homogeneous
zero-frequency vectors.  They are exactly collinear:

```text
S_s(0) = tau_s (1,0,1/2,0),
```

where `tau_s` is also the branch's constant-lapse Taub coefficient.  Hence
the Taub balance uniquely fixes `x_minus` as a positive linear combination
of `x_plus` and `x_extra`, and the complete non-removable homogeneous source
cancels on the entire positive quadrant.

There are nine nonzero pair-frequency types: three self sums and the sum and
difference for each of the three unordered branch pairs.  Every homogeneous
channel has a displayed algebraic correction.  For both polar outputs
`ell=2,4`, exact minimal-polynomial witnesses prove that neither the `p` nor
the `q` shell is hit.  The remaining zero-frequency `ell=2,4` sources are
removed by the printed exact inverses.

Thus every point of this declared two-parameter face, with arbitrary
constant relative phases, has a smooth spatially periodic finite
quasiperiodic second-order correction.  Other `m`, parity, extra
polarization, and higher-`ell` faces remain open.
