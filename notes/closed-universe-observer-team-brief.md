# Closed-universe observer team brief

## Commission

Answer one question:

\[
\boxed{
\text{Can a globally collapsed physical state description support nontrivial
physics for internal observers?}
}
\]

Do not begin by assuming that our compact zero-charge reduction and the
one-state closed-universe results in quantum gravity are the same mechanism.
Construct the strongest possible case that the apparent parallel is false,
then determine whether any precise structural bridge survives.

The desired output is either:

1. an exact common reduction principle with one shared certified fixture; or
2. a scoped no-go explaining why the two collapses concern different objects
   and cannot be identified.

Both outcomes are successful.  A suggestive analogy without a typed map,
fixture, and verifier is not a result.

## The two starting points

### This programme

On the selected compact, boundary-free, Taub-zero free sector, the isolated
one-particle complex is zero after the declared residual quotient.  Two
degree-four centered classes, \([W_+^2]\) and \([W_-^2]\), remain with pairing
\(I_2\); they are deformation or vertex classes, not particles.

The Berger-clock branch gives a different but adjacent fact.  The raw
cylinder generator \(D\) is linearly presymplectically null on the declared
fixed-coupling sector, while a clock field changes and relational frequency
observables are nontrivial.  The background-preserving nonlinear Cartan
generator is

\[
K_{\rm Berger}=D-\omega R,
\]

not affine raw \(D\).  The current interaction theorem is therefore a
\(K_{\rm Berger}\)-Cartan result.

The current classical observer assets include:

- a positive Berger phase clock;
- the fixed-coupling charge/nullity theorem;
- a compact spatially averaged relational Maxwell redshift fixture;
- a complete local gravity--clock--Maxwell \(q_2\);
- a certified Maxwell causal contraction and transferred mixed vertex; and
- a compact neutral source with a nonzero retarded Maxwell signal;
- two rank-two clock-labelled detector memories at probe order; and
- six exact global detector-indexed Berger rods with a conserved finite-mode
  compact stress-source export.

The original detector transfer uses homogeneous compact emitters.  A later
constraint-preserving Cauchy localization now supplies two receiver-adjacent
spatially localized conserved emitter currents with an exact triangular
rank-two record matrix; a common Hopf emitter at clock zero remains open.  The
global rods correct the prospective
apparatus carrier from 78 to 84 rows and close the missing-global-source input
to the compact solvability gate.  Exact retained-mode primitives now show
that the complete global rod stress has zero compact Taub projection and
admits gravitational compensation through order \(\epsilon_R^2\).  The
authoritative 84-row forward handoff fixes the complete row order and pairing,
bulk clock-transported memory category, detector-channel locality, independent
rod/readout gradings, exact profile two-jet through \(q_3\), external-emitter
boundary, and physical two-detector \(\Phi_2\) synthesis.  An all-orders
backreacted branch, emitter recoil, the full 84-row interacting/causal
complex, and a quantum observer state remain open.  The subsequent
`BERGER_84_ROW_UNARY_PAIRING_GREEN_GATE` closes the exact two-channel
memory--Maxwell unary and finite advanced/retarded inverse, giving a certified
72-row causal subcomplex on the non-rod indices.  It also closes the shifted
Euler equations on the separate \((0,0)\), \((\epsilon_R^2,0)\), and
\((0,\kappa)\) axes.  At that stage, full 84-row promotion was correctly blocked on the
nonzero rod diffeomorphism action and the missing gravity--rod Hessian,
cotangent-adjoint, shifted-metric-Hessian, and causal-witness payloads.
`BERGER_84_ROW_ROD_GRAVITY_UNARY` now supplies those six clock-dressed gauge
blocks, their exact odd-pairing adjoints, the covariant action Hessian, the
`q2(Phi2,-)` base shift, and a coupled formal causal witness.  A correction
audit finds that `q2(Phi2,-)` is a fourth-order diagonal principal deformation,
not the previously stated order-two subprincipal term; the exact physical
contraction witness is `623/81` on an `e3^4 h_hat_00` coefficient.  The Schur--Laurent
formula therefore certifies only a formal coefficientwise same-sided inverse,
not finite-parameter Green hyperbolicity.  A further bidegree audit corrects
`delta_r T`: since the memory kinetic term has no `kappa`, it belongs to `Q10`,
not `Q11`.  `BERGER_84_ROW_MIXED_R_KAPPA_UNARY_GATE` computes this transport,
its frozen-pairing adjoint, and the first formal Green correction exactly.  It
then returns the first normalized mixed obstruction: the handoff never defines
the metric dependence of the normalized detector densities `chi_0,chi_1`, so
`delta_r B_a` and hence `Q11` are underdetermined.  At that preflight stage the
result was deliberately not promoted to an unqualified 84-row theorem.

`BERGER_84_ROW_NORMALIZED_PROFILE_MIXED_UNARY` now supplies the missing
covariant normalization.  On each clock slice it uses the three assigned rods
as oriented transverse coordinates and defines
`chi_a=f_a(Theta) rho_a(R_a) sqrt(det G_a)`, where
`G_a^{IJ}=Pi_g(dR_aI,dR_aJ)`.  Thus
`sigma_a=delta_r log chi_a=1/2 tr(G_a^{-1} delta_r G_a)`; at both certified
event charts `d1+sigma_a=-Phi2_00/2`.  Exactly four `Q11` operator blocks are
nonzero, their frozen-pairing adjoints are included, and the all-84-row mixed
nilpotency/cyclicity defects vanish.  The bivariate Green coefficient is exact
over the formal Schur--Laurent coefficient field.  This closes the
coefficientwise mixed unary first jet, not finite-`r` Green hyperbolicity or an
all-orders apparatus theorem.

`BERGER_84_ROW_APPARATUS_Q2_Q3_K_GATE` now constructs the apparatus cubic and
quartic action tensors as exact Frechet-derivative families, including every
rod, memory-transport, normalized readout, scalar-BV, and cyclic cotangent
block.  Cyclicity and the declared arity-two/three identities close at the
unshifted background, and the arity-two identity closes through the first
backreacted coefficient.  The formal unary detector matrix remains rank two.
The ordinary `K_Berger` action, however, does not fix the rod/metric
background: `e0 Rbar` and `e0 Phi2` are nonzero.  Its honest affine
arity-three identity needs `q4(K0,-,-,-)`, which is not determined by the
imported `q1,q2,q3` or the profile two-jet.  An exact span audit also rules out
repair by a constant internal rotation of the existing six rods: their rank-6
span has rank-8 time-translation closure.  Thus the first observer morphism is
input-blocked on `q4`, not on signal distinguishability.

`BERGER_AFFINE_K_OBSERVER_MORPHISM` closes precisely the required slice
without overclaiming full `q4`.  Differentiated simultaneous `K` invariance
fixes `q4(K0,-,-,-)=-[K1,q3]`; an independent fifth-derivative audit and an
exact translated-detector integral verify the Ward identity and record
covariance.  The two persistent memories therefore define a rank-two
coefficientwise affine-`K` family morphism through arity three.  This is not a
linear quotient at one fixed apparatus background or a finite-parameter
theorem.

`BERGER_CG4_TWO_RECORD_POISSON_ALGEBRA` now imports C-G4 without redefining
its signal.  The two detector polarizations evaluate its circular phase plane
through a moment matrix whose determinant is a strictly positive double
average of `cos(beta(t-s))` over the actual windows.  Thus the C-G4
quadratures are linear and its relational Hamiltonian/redshift energy is
quadratic in the persistent memories.  Their localized polynomial algebra is
closed under multiplication and the transported nonzero Poisson bracket.

`BERGER_LOCALIZED_EMITTER_RANK_TWO_TRANSFER` closes the immediate localization
gate without requiring an explicit Berger Green kernel.  Since
`H1(S3)=H2(S3)=0`, the C-G4 cosine-mode constraint data admit spatial
potentials.  Bumping those potentials and differentiating again preserves the
constraints, while finite propagation preserves the mode on each detector
window.  Staggering `J_1` after `D0` gives
`M=[[-beta S0,0],[mu,beta C1]]`, so
`det M=-beta^2 S0 C1 != 0` independently of the unknown tail `mu`.

`BERGER_DYNAMICAL_EMITTER_RECOIL_INPUT_GATE` now shows that recoil is not a
coefficient waiting inside the external-current data.  Two cyclic local
two-form emitter completions with the same `J=delta Kbar` give exact recoil
coefficients `1/2` and `1/5` on the same test mode.  A typed emitter carrier,
action, preparation, pairing, causal Green operator, and symmetry action are
therefore mandatory new inputs.  The nonzero localized determinant still
proves formal rank-two stability for every compatible completion.

`BERGER_POLARIZATION_TWO_FORM_EMITTER_HANDOFF` now makes the required theory
choice.  It selects two standard massive two-forms on `gHat`, switched by
compact functions of the dynamical clock.  The current
`J_b=g_b delta(h_b K_b)` is conserved off shell, while
`(delta d+m_b^2)K_b=g_b h_b dA` supplies reciprocal recoil.  The massive
constraint reduction is normally hyperbolic and the carrier grows from 84 to
108 rows with degree ranks `(6,48,48,6)`.

`BERGER_108_ROW_POLARIZATION_EMITTER_UNARY_FIRST_RECOIL` now explicitly
appends the 24 emitter field/antifield rows, verifies their odd pairing has
rank 24, and exports all six new row-indexed unary blocks.  The new Maxwell
gauge and Noether paths close by `d^2=delta^2=0`, and Hessian adjointness closes
unary cyclicity.  The massive two-form Green formula is exact, while the
coupled Neumann inverse gives the first formal Maxwell recoil self-energy
through order `g^2`.  At that gate this was deliberately not called a full
108-row BV causal chain contraction because the chain homotopy had not yet
been exported.

`BERGER_108_ROW_POLARIZATION_EMITTER_CAUSAL_CHAIN_HOMOTOPY` now closes the
needed coefficientwise causal-chain identity.  With
`W_K(K_b_plus)=K_b`, the wave operator
`P_108=q_108 W_108+W_108 q_108` has the coupled Euler blocks above, and
`Lambda_108,+/-=W_108 G_P108,+/-` satisfies
`q_108 Lambda+Lambda q_108=1` through `g^2`.  A gauge-complete graded fixture
includes the Maxwell ghost and ghost antifield and detects deletion of either
the quadratic Green coefficient or an emitter witness.  This closes the
formal coefficientwise chain step, not finite-parameter or all-orders Green
hyperbolicity.

`BERGER_DYNAMICAL_EMITTER_CAUCHY_RANK_TWO` now closes the preparation gate at
leading coupling order.  Green adjunction makes each detector response a
Cauchy covector on the free massive two-form data.  A constraint-compatible
local polarization has `p^mu K_mu nu=0` and nonzero clock contraction, so the
covector is nonzero on a receiver-adjacent patch.  Constraint-potential
localization then supplies compact data `u_0,u_1`.  With nonzero `g_0,g_1` and
the second switch after `D0`,
`M^(K)=[[kappa_0,0],[mu,kappa_1]]` has
`det M^(K)=kappa_0 kappa_1 !=0`.  These are actual dynamical-emitter
preparations, not two externally declared Maxwell currents.

`BERGER_DYNAMICAL_EMITTER_RECOIL_ORDER_AND_INPUT_GATE` corrects the next
order count.  Since the coupled unary is bipartite between `A` and `K_b`, an
emitter-data record has only odd absolute coupling orders: the leading term is
`g`, the absolute `g^2` term is zero, and the first recoil is absolute `g^3`
(relative `g^2`).  The exact operator is the retarded chain
`K_b^(0)->A^(1)->K_c^(2)->A^(3)->Q_a`; the leading determinant remains the
nonzero constant term in the relative-recoil formal ring.  A numerical
coefficient is not yet defined by the artifacts because the rank theorem
exports an existence-level basis rule rather than serialized detector-selected
compact Cauchy data and evaluated massive Green images.

`BERGER_EXACT_NORMALIZED_EMITTER_SWITCH_PROFILES` now serializes the two
previously open switches.  Each is a standard nonnegative flat bump divided
by its exact clock radius and the positive core integral, hence has compact
`C-infinity` support and unit clock integral.  Exact conversion with
`dTheta/dt=3/4` puts `h_0` before `D0` and `h_1` strictly between `D0` and
`D1`, with three physical margins `1/24` (clock margins `1/32`).  This leaves
the detector-selected compact massive-two-form Cauchy profiles and their
massive Green images open.  An arbitrary explicit bump is not sufficient,
because it could lie in the kernel of the advanced detector covector.

`BERGER_EXACT_DETECTOR_SMEARINGS_AND_ADVANCED_COVECTORS` now fixes the
previously arbitrary detector functions as unit-integral flat clock bumps and
unit-integral radial flat bumps in each local rod chart.  It retains
`0<epsilon_a<r_chart,a<=1/64`, rather than inventing a numerical injectivity
radius absent from the local inverse-function certificate.  Formal Green
adjunction exports the exact chain
`chi_a P_a -> G_A,adv -> h_a d G_A,adv -> G_Ea,adv -> ell_a`.
Neither Green image nor a coordinate-level positive-energy Cauchy dual is yet
evaluated.

`BERGER_POSITIVE_ENERGY_DETECTOR_SELECTED_EMITTER_PROFILES` now fixes that
dual at operator level.  If `v_a=(q_a,p_a)` is the advanced covector data,
then `u_a=(-p_a,L_a q_a)` has response
`||p_a||^2+<q_a,L_a q_a>>0`.  The construction preserves the co-closed
constraint and causal support and uses no response normalization.  Only the
Berger harmonic coefficients and Green integrals remain unevaluated.

`BERGER_EMITTER_STRESS_CLOCK_BACKREACTION_LEDGER` closes the independent
first backreaction jet without pretending to evaluate that missing recoil
integral.  Direct variation of the selected action gives the free emitter
stress, the metric stress of the switched emitter--Maxwell coupling, and the
reciprocal source `-sum_b g_b h_b'(Theta)<K_b,dA>` in the clock equation.
They occupy the existing metric and clock antifield rows, with their cyclic
partners in the Maxwell and emitter cotangent rows.  Representative common-
action third derivatives have zero cyclicity defect, and an exact reduced
fixture verifies the off-shell clock energy-exchange identity.  The complete
componentwise 108-row `q1 q2` replay, higher emitter jets, and a solved
backreacted gravity-clock branch remain open.

`BERGER_108_ROW_EMITTER_Q1_Q2_MASTER_IDENTITY` now closes the covariant
version of that arity-two gate over the imported apparatus `r`-first-jet
coefficient ring.  The cubic master-equation coefficient `{S_2,S_3}=0`
combines the imported 84-row identity with the emitter Maxwell, clock, Weyl,
Diff-cotangent, stress, and cyclic orbits.  All output rows 0--107 are
classified exactly once and all 108 covariant row defects are zero.  This is
not yet a sparse support-local PBW payload or a component-coefficient PBW
replay; those remain the algebraic refinement before a solved backreacted
branch.

### Immediate priority after the polarization-emitter handoff

1. Expand the certified exact `h_a`, `chi_a`, advanced covectors, and
   positive-energy-selected `u_0,u_1` in Berger Peter--Weyl modes with
   validated quadrature and spectral-tail bounds, and then compute the absolute-`g^3`
   (relative-`g^2`) detector recoil coefficient.  In parallel, use the
   exported emitter stress
   and clock-switch source to refine the certified covariant 108-row
   `q1 q2` identity into a support-local PBW payload before solving a
   backreacted gravity-clock branch.
   Treat realization of both preparations at the original common Hopf event
   as a stronger, separate localization target.
2. Keep the full apparatus Dirac bracket, complete harmonic signal algebra,
   full `q4`, fixed-background linear-`K` descent, finite-parameter
   Green hyperbolicity, and the optional 88-row co-rotating replacement as
   distinct later gates.

The repaired bivariate first-jet coefficient and formal causal correction are
sufficient input for scoped first-jet tests, but they must not be advertised
as a finite-parameter Green theorem or fully interacting apparatus theorem.

### The adjacent closed-universe programme

The comparison sources are:

- [the island rule and quantum extremal surfaces](https://arxiv.org/abs/1908.10996);
- [closed universes in two-dimensional gravity](https://arxiv.org/abs/2402.00098);
- [closed universes, factorization, and ensemble averaging](https://arxiv.org/abs/2403.13047);
- [Maldacena on real observers in Euclidean gravity](https://arxiv.org/abs/2412.14014); and
- [Harlow--Usatyuk--Zhao on observers in a closed universe](https://arxiv.org/abs/2501.02359).

These works motivate or argue for a one-dimensional fundamental
closed-universe Hilbert space in a fixed theory or sector, while an internal
observer can possess a much richer approximate quantum description.  Their
mechanisms involve quantum extremal surfaces, gravitational path integrals,
factorization, Gram-matrix rank, encoding maps, and observer-relative
algebras.

Do not rewrite that statement as a Taub-zero or \(D\)-quotient theorem unless
an explicit equivalence is proved.

## Mandatory no-conflation protocol

Every claim must be indexed by

\[
(\text{theory},\text{background},\text{state object},\text{quotient},
\text{observer},\text{boundaries},\text{lifecycle}).
\]

The first report must distinguish at least these objects:

| Object | Question it answers | Current programme status |
|---|---|---|
| Fundamental Hilbert space | How many exact quantum states exist? | Not constructed |
| BRST/BV state cohomology | Which quantum states survive gauge reduction? | Classical inputs only; quantum open |
| One-particle complex | Which isolated linear excitations survive? | Zero on one selected compact free sector |
| Residual degree-four cohomology | Which centered deformation classes survive? | \(\mathbb C^2\) with pairing \(I_2\) on the declared sector |
| Covariant phase space | Which classical solutions and charges survive? | Background- and charge-sector dependent |
| Observable algebra | What can an internal observer measure? | Relational classical fixtures; full algebra open |
| Observer-effective Hilbert space | What approximate quantum mechanics describes an observer? | Not constructed |

Also distinguish:

```text
factorization                 != Taub-zero charge restriction
gravitational path integral  != classical BV quotient
one-dimensional Hilbert space != zero one-particle cohomology
closed spatial slice         != a fully closed Euclidean spacetime
internal observer algebra    != adding an external boundary
relational clock             != gauge fixing alone
```

Any proposed equivalence must specify a functor or map and show which
structure it preserves: differential, product, involution, pairing, support,
charge, or probability interpretation.

## Work package O0: literature reconstruction and claim typing

Reproduce the logical chain of each source without relying on review prose:

1. the quantum-extremal-surface argument for rank-one encoding;
2. the path-integral/Gram-matrix rank criterion;
3. the fixed-theory factorization argument;
4. the observer or cloned-observer construction;
5. the distinction between fundamental and observer-effective state spaces.

For every step record:

- theory and spacetime dimension;
- topology and boundary conditions;
- whether the statement is fixed-theory, ensemble-averaged, or sectoral;
- the state object and inner product being ranked;
- the exact role of an observer or reference system;
- perturbative versus nonperturbative status; and
- assumptions that have no current analogue in our programme.

The literature ledger must quote equations and theorem locations sparingly,
link primary sources, and mark interpretive inferences as inferences.

## Work package O1: exact external fixture

Independently reproduce one finite closed-universe fixture.  Prefer the
smallest model in which the following are all explicit:

\[
\operatorname{rank}G_{ij}=1,
\qquad
\dim\mathcal H_{\rm global}=1,
\qquad
\dim\mathcal H_{\rm observer}>1
\quad\text{approximately or effectively}.
\]

The reproduction must not hard-code rank one as the answer.  Generate the
Gram or encoding matrix from declared input data, reduce it exactly, and
retain determinant/minor witnesses.  Then add mutation tests that break:

- factorization;
- the observer channel;
- the pointer-basis or reference-system assumption; and
- the rank-one conclusion.

Store the model, schema, certificate, independent verifier, source-version
ledger, and exact commands in an isolated namespace such as
`closed_universe_observers/`.

## Work package O2: our internal-observer map

Using imported classical certificates, define a candidate map

\[
\mathfrak R_{\rm Ob}:
\operatorname{Obs}_{\rm global}
\longrightarrow
\operatorname{Obs}_{\rm Ob}
\]

for the Berger clock.  Do not reconstruct the clock, Green homotopy, Maxwell
unary differential, or mixed vertex.  Import each by path, result identifier,
content hash, and declared claim boundary.

At minimum, determine whether the observer algebra can contain:

1. the clock-phase condition \(T=\tau\);
2. a relational Maxwell frequency or redshift observable;
3. a retarded source-to-field observable;
4. two distinguishable clock-labelled records; and
5. a nontrivial causal response while total \(D\) remains null on the tested
   tangent sector.

Test gauge invariance under Diff, Weyl, Maxwell gauge, and the relevant
residual generator separately.  Keep raw \(D\) and
\(K_{\rm Berger}=D-\omega R\) distinct.

The strongest counterexample is that every proposed observer record becomes
BRST exact, charge-forbidden, pairing-radical, acausal, or dependent on an
external structure that changes the closed problem.

## Work package O3: construct the common fixture

Only after O1 and O2 pass independently, put both reductions in one diagram:

\[
\begin{array}{ccc}
\mathcal S_{\rm effective} & \xrightarrow{\;\Pi_{\rm global}\;} &
\mathcal S_{\rm global}\\
\downarrow \mathfrak R_{\rm Ob} && \downarrow\\
\mathcal S_{\rm observer} & \xrightarrow{} &
\mathcal D_{\rm records}.
\end{array}
\]

Here \(\mathcal S\) is deliberately untyped notation.  Replace it with the
actual carrier in each construction and report if no common category exists.

Test four possible explanations of the apparent parallel:

1. **Common gauge reduction:** both collapses are derived quotients of a
   larger redundant carrier.
2. **Common absence of external observables:** the global object is small
   because no external reference algebra exists.
3. **Observer dressing:** an internal clock/reference system selects a
   nontrivial subalgebra without enlarging the exact global state space.
4. **Accidental analogy:** compactness is the only common ingredient and the
   mathematical reductions are inequivalent.

For each explanation provide either a commuting map with preserved structure
or an explicit defect.

## Work package O4: interaction and quantum gates

Do not duplicate the nonlinear or quantum teams.

The nonlinear team supplies whether observer records and the relevant
quotient survive mixed \(q_3\), backreaction, and higher brackets.  This team
imports those results and asks whether the observer map remains a morphism.

The quantum team supplies the BRST-compatible state framework, complete
zero-frequency carrier, renormalized products, and QME disposition.  Until
those gates pass, the only legal quantum verdict is
`QUANTUM_COMPARISON_NOT_YET_DEFINED`.

After they pass, test:

1. whether the actual global physical Hilbert space is one-dimensional;
2. whether only the one-particle subspace collapses;
3. whether the observer algebra admits a positive state;
4. whether its effective inner product is induced from the global theory;
5. whether observer-relative evolution is compatible with the quantum
   Cartan/Ward identities; and
6. whether anomaly, zero modes, or boundary data destroy the comparison.

Every quantum row must carry at least one existing dependency tag:
`LOCAL-ALGEBRAIC`, `EUCLIDEAN-SPECTRAL`, `REDUCED-MODE`, or
`LORENTZIAN-CAUSAL`.  The first three do not establish a Lorentzian observer
theory.

## Work package O5: theorem extraction and adjacent consequence

If a common fixture survives, extract an abstract statement of the form:

> A globally reduced state object may be trivial in a declared degree while
> an internal reference system supports a nontrivial causal observable
> algebra, provided specified reduction, dressing, and pairing conditions
> hold.

State exactly whether “trivial” means one-dimensional Hilbert space, vanishing
one-particle cohomology, or something else.  Do not erase that distinction in
the title or abstract.

If the comparison fails, extract the no-go:

> The closed-universe factorization collapse and the Taub-zero residual
> collapse cannot be instances of one functor because a specified carrier,
> product, pairing, or observer map fails to match.

Prepare a two-page bridge note only after one of these statements has an exact
fixture and verifier.  Do not open a new paper from the literature dictionary
alone.

## Lifecycle

Use these states without implicit promotion:

```text
DICTIONARY_COMPLETE
EXTERNAL_FIXTURE_REPRODUCED
CLASSICAL_OBSERVER_MAP_CERTIFIED
COMMON_FIXTURE_CERTIFIED
QUANTUM_COMPARISON_LEGAL
BRIDGE_THEOREM_FROZEN
```

A later state requires every earlier dependency actually used by the claim.
In particular, `EXTERNAL_FIXTURE_REPRODUCED` is not evidence for
`QUANTUM_COMPARISON_LEGAL`.

Every comparison cell receives one verdict:

```text
STRUCTURAL_BRIDGE_CERTIFIED
ANALOGY_ONLY_OBJECT_MISMATCH
OBSERVER_RECOVERY_OBSTRUCTED
QUANTUM_COMPARISON_NOT_YET_DEFINED
INSUFFICIENT_EXTERNAL_REPRODUCTION
```

## Shared comparison matrix

| Setting | Global carrier | Reduction mechanism | Surviving state object | Observer algebra | Causal response | Pairing/probability | Verdict |
|---|---|---|---|---|---|---|---|
| Selected vacuum cylinder | classical BV/one-particle/residual carriers kept separate | Taub-zero plus residual quotient | zero isolated one-particle sector; degree-four \(\mathbb C^2\) | no physical clock | classical causal complex | residual \(I_2\), not a quantum norm | compute |
| Berger clock | fixed-coupling classical phase space | presymplectic nullity of raw \(D\); \(K\)-Cartan through tested order | classical relational sector | clock/redshift/retarded signal partial | certified classical retarded signal | classical pairing only | compute |
| External closed-universe toy model | exact model carrier | QES/path integral/factorization as reproduced | rank-one global fixture | observer-effective carrier | model-dependent | reproduce exactly | compute |
| Full quantum Berger theory | not yet constructed | not yet defined | open | open | open | open | `QUANTUM_COMPARISON_NOT_YET_DEFINED` |
| dS/AdS or asymptotic boundary | boundary phase space required | charged symmetries may replace quotient | open | boundary/internal observer distinction | open | open | not tested |

## First 24-hour objective

Do not attempt the full theorem immediately.  Deliver:

1. a source-accurate claim ledger for the five primary references;
2. the mandatory object dictionary and comparison matrix;
3. one exact rank-one external toy fixture with an independent verifier;
4. a content-addressed import manifest for the Berger clock, redshift, Maxwell
   causal, and retarded-signal certificates;
5. a precise definition or obstruction for the first classical observer map;
6. a report identifying the first point where the analogy survives or fails.

If the external fixture cannot be reproduced in matched conventions, stop at
`INSUFFICIENT_EXTERNAL_REPRODUCTION`.  If the classical observer map cannot be
defined without adding an external boundary or changing the charge sector,
report that as the primary result.

## Cross-team contract

- **Classical team:** owns the clock, relational observables, Maxwell causal
  chain, and localized emitter/receiver construction.  Import; do not fork.
- **Nonlinear team:** owns mixed higher brackets and interaction stability of
  the observer map.
- **Quantum team:** owns the physical state, QME, Hadamard/zero-mode, and
  quantum inner-product gates.  Never infer them from the toy model.
- **Einstein team:** owns boundary charges, Einstein/Weyl relative sectors,
  and the distinction between compact internal observers and asymptotic
  observers.

Submit any result bearing on the \(D\)-quotient through the
[`d_quotient_programme/`](../d_quotient_programme/README.md) contribution
contract.  Keep external reproductions and bridge-only machinery isolated
until they acquire an internal certified consumer.

## Required handoff

The team must leave:

- one human-readable report;
- a machine-readable claim/dictionary ledger;
- exact generated fixture data rather than a hard-coded conclusion;
- a strict schema and independent verifier;
- mutation-sensitive expected-failure tests;
- content hashes for every internal and external input;
- exact commands, elapsed times, and test tiers;
- explicit missing-object and assumption ledgers;
- one lifecycle state and one verdict per comparison row; and
- a recommendation: continue to a common theorem, publish a scoped no-go, or
  wait for the quantum gate.

The team succeeds by locating the first exact bridge or the first exact
obstruction.  It does not succeed by arguing that two closed universes sound
similar.
